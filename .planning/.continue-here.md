---
context: default
phase: null
task: Wave 3 of 4
total_tasks: 4
status: in_progress
last_updated: 2026-06-16 13:23 CST
---

# Redesign Frontend — Diseño Aprobado Luis

No hay PLAN.md de GSD. Rediseño conversacional aprobado directamente por el usuario basado en `docs/diseño_aprobado_luis.pdf`.

<current_state>
Rama: redesign/frontend-v2
Waves 1 y 2 completadas y aprobadas. Pausado antes de Wave 3 por límite de contexto.
</current_state>

<completed_work>

- Wave 1 DONE — CSS: nav TA badge + TALENT AGENCY, tabs Sora uppercase, KPI premium cards .kpi-t (ícono SVG + borde izquierdo coloreado)
- Wave 2 DONE — Talent header (TA+AGENCY+divider+nombre+fecha), medal cards (badge redondo + Talento 70%), tabla campañas con header row
</completed_work>

<remaining_work>

Wave 3 (JS renderers):
1. renderIncomeProjection — valores etiquetados DENTRO de cada segmento de barra apilada + total sobre la barra
2. renderTalentFunnel — cabecera con prospectos totales/calificados/% + barras con TRATOS + VALOR MXN
3. renderLostOpportunities — cambiar lista a donut chart conic-gradient + leyenda con % y razón

Wave 4 (Polish):
- Talent header name truncation fix (nombre "Do..." en 390px)
- Calendar nodes: círculos de color sólido con SVG calendar en lugar de emoji 📅
- Responsive cleanup general
</remaining_work>

<decisions_made>

- .kpi-t es la nueva clase para KPI talent cards (no toca .kpi del Resumen tab)
- Talento 70% calculado en frontend: Math.round(deal.amount * 0.70)
- formatMXN abreviado se mantiene ($2.9M) por restricción de ancho móvil
- CSS se agrega al FINAL de styles.css como bloques Wave 1/Wave 2
- Servidor: uv run uvicorn app.main:app --host 0.0.0.0 --port 8765
</decisions_made>

<blockers>
Ninguno técnico. Usuario aprueba screenshot de cada wave antes de continuar.
</blockers>

## Required Reading
1. docs/diseño_aprobado_luis.pdf — referencia visual exacta (2 págs). Leer ANTES de Wave 3.
2. frontend/js/dashboard.js — renderIncomeProjection (~L760), renderTalentFunnel (~L541), renderLostOpportunities (~L677), renderBrandDonut (~L611)
3. frontend/css/styles.css — bloques WAVE 1 y WAVE 2 al final del archivo

## Infrastructure State
- Branch: redesign/frontend-v2 (no mergeada a main)
- Servidor: uv run uvicorn app.main:app --host 0.0.0.0 --port 8765 &
- Credenciales: santillan@talentagency.mx / ChangeMe123!
- Playwright instalado (npx playwright, Chromium headless)
- Archivos sin commit: frontend/css/styles.css, frontend/index.html, frontend/js/dashboard.js

<context>
Cliente Luis Santillan aprobo el diseno en docs/diseno_aprobado_luis.pdf. Traducir ese diseno (fondo blanco) a dark mode premium. Waves 1 y 2 aprobadas visualmente. Wave 3 es la mas compleja: reescribir renderers de grafica de proyeccion, funnel y oportunidades perdidas.
</context>

<next_action>
1. cd seg-dashboard && uv run uvicorn app.main:app --host 0.0.0.0 --port 8765 &
2. Leer dashboard.js funciones renderIncomeProjection, renderTalentFunnel, renderLostOpportunities
3. Ejecutar Wave 3 — reescribir los 3 renderers
4. Screenshot, mostrar al usuario, aprobar, Wave 4
</next_action>
