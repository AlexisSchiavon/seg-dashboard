---
phase: 05-ai-generated-pdf-reports
plan: 01
subsystem: reports
tags: [reports, schema, model, migration, test-infrastructure, anthropic, weasyprint, jinja2]
dependency_graph:
  requires: []
  provides:
    - app/models.py::Report (SQLAlchemy model, reports table)
    - app/schemas/reports.py (ReportGenerate, NarrativeSections, ReportOut, ReportHistoryItem)
    - tests/test_reports.py (Wave 0 failing stubs for REPORT-01, REPORT-02, DASH-05)
    - tests/conftest.py::mock_anthropic (monkeypatches app.services.reports.anthropic.Anthropic)
    - tests/conftest.py::mock_weasyprint (monkeypatches app.services.reports.HTML)
  affects:
    - alembic/versions/ (new migration afc2f8425aa0_add_reports_table)
    - pyproject.toml (anthropic, weasyprint, jinja2 added)
    - tests/conftest.py (Report added to model import; 2 new fixtures added)
tech_stack:
  added:
    - anthropic==0.109.2 (Claude API client for report narrative generation)
    - weasyprint==69.0 (HTML/CSS to PDF rendering; mocked in tests)
    - jinja2==3.1.6 (HTML templating for PDF reports; transitively installed via FastAPI before)
  patterns:
    - SQLAlchemy 2.0 declarative style with UniqueConstraint in __table_args__
    - Pydantic BaseModel with Field(pattern=) for month input validation (T-05-VAL)
    - monkeypatch fixtures for external service isolation (mirrors mock_sheets_rows pattern)
    - Wave 0 test stubs: pytest.fail("not implemented — Wave N") per plan
key_files:
  created:
    - app/schemas/reports.py
    - tests/test_reports.py
    - alembic/versions/afc2f8425aa0_add_reports_table.py
  modified:
    - app/models.py (added Report class and UniqueConstraint import)
    - tests/conftest.py (added Report import on line 29; added mock_anthropic and mock_weasyprint fixtures)
    - pyproject.toml (added anthropic, weasyprint, jinja2 to dependencies)
    - uv.lock (updated with 16 new packages)
decisions:
  - "D-63: Report model fields confirmed — id, talent_id (FK, index), month (str YYYY-MM, index), generated_at (DateTime server_default), file_path (String), file_size_bytes (Integer default=0)"
  - "D-64: UniqueConstraint(talent_id, month, name=uq_report_talent_month) — one report per talent+month, overwrite semantics (05-02 generate_report will upsert)"
  - "T-05-VAL: ReportGenerate.month validated with Field(pattern=r'^\\d{4}-\\d{2}$') — rejects malformed inputs at schema boundary before they reach file-path construction"
  - "Mock target paths: app.services.reports.anthropic.Anthropic and app.services.reports.HTML — fixtures only patch when used, Wave-2 tests will exercise them"
metrics:
  duration: "~15 minutes"
  completed: "2026-06-15"
  tasks_completed: 4
  files_created: 3
  files_modified: 4
---

# Phase 5 Plan 01: Phase 5 Foundation — Dependencies, Report Model, Schemas, and Test Stubs Summary

**One-liner:** Report model + Alembic migration, 4 Pydantic schemas with month-pattern validation, 17 Wave-0 test stubs, and mock_anthropic/mock_weasyprint fixtures — contracts-first foundation for AI-generated PDF reports.

## What Was Built

### Task 1: Package Legitimacy Checkpoint (human-approved)
All three PyPI packages confirmed legitimate before installation. anthropic (Anthropic official), weasyprint (Kozea project), jinja2 (Pallets project).

### Task 2: Install Dependencies
Added `anthropic>=0.79,<1.0`, `weasyprint>=66,<70`, `jinja2>=3.1` to `pyproject.toml`. Installed via `uv add`. anthropic 0.109.2, weasyprint 69.0, jinja2 3.1.6 confirmed importable. `reports/` already in `.gitignore` line 16 — no change needed. WeasyPrint may emit OSError locally about Pango system libs (macOS) — does not block tests since mock_weasyprint patches it.

### Task 3: Report Model + Alembic Migration
Added `class Report(Base)` to `app/models.py` with all 6 required fields (id, talent_id, month, generated_at, file_path, file_size_bytes) plus `UniqueConstraint("talent_id", "month", name="uq_report_talent_month")` in `__table_args__` and a `talent` relationship (lazy="select"). Imported `UniqueConstraint` from sqlalchemy. Autogenerated migration `afc2f8425aa0_add_reports_table` and applied with `alembic upgrade head`. Table verified with 6 columns + unique constraint confirmed via `inspect(engine).get_unique_constraints('reports')`.

### Task 4: Schemas, Test Stubs, Mock Fixtures
- **app/schemas/reports.py**: 4 Pydantic BaseModel classes — `ReportGenerate` (month field with `Field(pattern=r'^\d{4}-\d{2}$')` for T-05-VAL), `NarrativeSections` (3 Claude prose sections), `ReportOut` (full response with narrative), `ReportHistoryItem` (list view). All use `T | None` not `Optional[T]`.
- **tests/test_reports.py**: 17 failing stubs across 7 test classes — `TestAvailableMonths`, `TestGenerateReportPayload`, `TestPdfWrittenToDisk`, `TestGenerateEndpoint`, `TestListReports`, `TestDownloadReport`, `TestReportesTabExists`. All fail with `pytest.fail("not implemented — Wave N")`.
- **tests/conftest.py**: Added `Report` to model import (line 29). Added `mock_anthropic` (patches `app.services.reports.anthropic.Anthropic`) and `mock_weasyprint` (patches `app.services.reports.HTML`) fixtures after Phase 4 fixtures.

## Commits

| Task | Commit | Description |
|------|--------|-------------|
| Task 1 | (checkpoint — no code) | Human verified package legitimacy |
| Task 2 | 005d9d0 | chore(05-01): install anthropic, weasyprint, jinja2 dependencies |
| Task 3 | 7f8ed79 | feat(05-01): add Report model and Alembic migration for reports table |
| Task 4 | 0d8b097 | feat(05-01): add report schemas, Wave 0 test stubs, and mock fixtures |

## Deviations from Plan

None — plan executed exactly as written. The conftest.py import sequence was verified (line 29) and `os` was already imported at the top of conftest.py (line 1), so `os.makedirs` in `mock_weasyprint` works without additional imports.

## Known Stubs

The following stubs exist by design (Wave 0 — contracts-first):

| Stub | File | Reason |
|------|------|--------|
| `app.services.reports` module | (does not exist yet) | Created in Plan 05-02 (Wave 1) |
| `app/routers/reports.py` | (does not exist yet) | Created in Plan 05-03 (Wave 2) |
| All 17 test functions in test_reports.py | tests/test_reports.py | Wave 0 stubs; implemented in 05-02 and 05-03 |

These stubs are intentional — this plan establishes the contracts that downstream plans implement against.

## Threat Surface Scan

No new network endpoints, auth paths, or trust boundaries introduced in this plan. The `ReportGenerate.month` field validates at schema boundary (T-05-VAL mitigated). The `reports/` directory is gitignored (prevents accidental PDF commit). No threat flags beyond what was planned.

## Self-Check: PASSED

- [x] `app/schemas/reports.py` — exists, imports correctly, `ReportGenerate(talent_id=1, month='bad')` raises ValidationError
- [x] `tests/test_reports.py` — exists, 17 tests collected, all fail as expected (Wave 0)
- [x] `alembic/versions/afc2f8425aa0_add_reports_table.py` — exists
- [x] `app/models.py` — `from app.models import Report` succeeds; reports table has 6 columns + uq_report_talent_month constraint
- [x] `tests/conftest.py` — `def mock_anthropic` present, `def mock_weasyprint` present, `Report` in model import
- [x] Commits 005d9d0, 7f8ed79, 0d8b097 exist in git log
