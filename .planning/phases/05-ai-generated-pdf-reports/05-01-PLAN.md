---
phase: 05-ai-generated-pdf-reports
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - app/models.py
  - app/schemas/reports.py
  - alembic/versions/
  - tests/conftest.py
  - tests/test_reports.py
  - .gitignore
autonomous: false
requirements: [REPORT-01]
user_setup:
  - service: anthropic
    why: "Claude API for report narrative generation (M5)"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "console.anthropic.com -> Settings -> API Keys (value must be set in .env; key name already declared in app/config.py line 21)"

must_haves:
  truths:
    - "anthropic and weasyprint are installed and importable"
    - "A Report row can be created with talent_id, month, generated_at, file_path, file_size_bytes"
    - "tests/test_reports.py exists with failing stubs for all Phase 5 behaviors"
    - "mock_anthropic and mock_weasyprint fixtures exist in conftest.py"
  artifacts:
    - path: "app/models.py"
      provides: "Report SQLAlchemy model"
      contains: "class Report"
    - path: "app/schemas/reports.py"
      provides: "ReportGenerate, NarrativeSections, ReportOut, ReportHistoryItem schemas"
      exports: ["ReportGenerate", "NarrativeSections", "ReportOut", "ReportHistoryItem"]
    - path: "tests/test_reports.py"
      provides: "Failing test stubs for REPORT-01/02 and DASH-05"
      min_lines: 40
    - path: "tests/conftest.py"
      provides: "mock_anthropic and mock_weasyprint fixtures"
      contains: "def mock_anthropic"
  key_links:
    - from: "tests/conftest.py"
      to: "app.services.reports"
      via: "monkeypatch.setattr target paths"
      pattern: "app\\.services\\.reports\\.(anthropic\\.Anthropic|HTML)"
    - from: "app/models.py Report"
      to: "talents.id"
      via: "ForeignKey"
      pattern: "ForeignKey\\(\"talents\\.id\"\\)"
---

<objective>
Establish the Phase 5 foundation: install the report-generation dependencies, define the `Report` data model + Alembic migration, define the Pydantic schemas that the service and router will use as contracts, and stand up the Wave 0 test infrastructure (failing test stubs + mock fixtures) so every later plan has a deterministic verification target.

Purpose: Downstream plans (05-02 generation, 05-03 history/download, 05-04 UI) implement against the schemas and model defined here, and run against the test stubs created here. This is the contracts-first task per interface-first ordering.
Output: Installed deps, `Report` model, migration, `app/schemas/reports.py`, `tests/test_reports.py` stubs, `mock_anthropic`/`mock_weasyprint` fixtures, `.gitignore` confirms `reports/`.
</objective>

<execution_context>
@/Users/alexiszamoraia/Developer/projects/lumixia/clients/santillan-ent/seg-dashboard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexiszamoraia/Developer/projects/lumixia/clients/santillan-ent/seg-dashboard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ai-generated-pdf-reports/05-CONTEXT.md
@.planning/phases/05-ai-generated-pdf-reports/05-RESEARCH.md
@.planning/phases/05-ai-generated-pdf-reports/05-PATTERNS.md
@.planning/phases/05-ai-generated-pdf-reports/05-VALIDATION.md

<interfaces>
<!-- Existing model patterns the Report model must mirror. From app/models.py. -->
SQLAlchemy 2.0 declarative style: `Mapped[T]` + `mapped_column()`, `from app.database import Base`.
Existing analogs: `class Lead(Base)` (lines 92-118), `class TrelloCard(Base)` (lines 121-150).
`Deal.add_time: Mapped[str | None]` (line 64) — string "YYYY-MM-DD HH:MM:SS".
Imports available: `String, Boolean, Float, Integer, ForeignKey, DateTime, Date, func` from sqlalchemy; `Mapped, mapped_column, relationship` from sqlalchemy.orm.

Report model fields (D-63): id (pk), talent_id (FK talents.id, index), month (str "YYYY-MM", index),
generated_at (DateTime server_default=func.now()), file_path (str), file_size_bytes (Integer default=0),
plus relationship: talent (lazy="select"). Add UniqueConstraint on (talent_id, month) per D-64.

Existing conftest import line (line 29): `from app.models import Deal, DealStageEvent, Lead, Talent, TalentProduct, User`
— must add `Report`. The `_clean_tables` autouse fixture iterates Base.metadata.sorted_tables (no change needed once Report is registered).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify package legitimacy before install</name>
  <action>Pause for human verification of package legitimacy before any install runs. No code changes in this task — the human confirms the three packages on PyPI per the steps below, then approves.</action>
  <what-built>Per the Package Legitimacy Gate, RESEARCH.md tagged anthropic, weasyprint, and jinja2 as [ASSUMED] (slopcheck was unavailable at research time). These are well-established packages but require human verification before install per the security gate.</what-built>
  <how-to-verify>
    1. Confirm anthropic at https://pypi.org/project/anthropic/ — published by Anthropic, source github.com/anthropics/anthropic-sdk-python, version ~0.109.x.
    2. Confirm weasyprint at https://pypi.org/project/weasyprint/ — Kozea project, source github.com/Kozea/WeasyPrint, version 69.0.
    3. Confirm jinja2 at https://pypi.org/project/jinja2/ — Pallets project (already transitively installed via FastAPI), version 3.1.x.
    All three are long-standing, high-download packages with official repos.
  </how-to-verify>
  <resume-signal>Type "approved" to allow install, or describe concerns</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install dependencies and confirm imports</name>
  <files>pyproject.toml, .gitignore</files>
  <read_first>
    - pyproject.toml (current dependencies array, starts line 7)
    - .gitignore (confirm line 16 already has `reports/`)
    - .planning/phases/05-ai-generated-pdf-reports/05-RESEARCH.md (Standard Stack, Installation section)
  </read_first>
  <action>Add `anthropic>=0.79,<1.0`, `weasyprint>=66,<70`, and `jinja2>=3.1` to the `dependencies` array in pyproject.toml (preserving existing entries). Run `uv add "anthropic>=0.79,<1.0" "weasyprint>=66,<70" "jinja2>=3.1"`. Confirm `reports/` is already in `.gitignore` (line 16) — no change needed if present; add it if missing. Do NOT create `app/services/reports.py` in this plan — only confirm the packages import. WeasyPrint requires Pango system libs locally (macOS: `brew install pango`); if `import weasyprint` raises OSError locally, note it in the SUMMARY as a local-dev environment item (Phase 7 Dockerfile handles the Linux libs) — it does not block this plan because tests mock WeasyPrint.</action>
  <verify>
    <automated>uv run python -c "import anthropic, jinja2; print('anthropic', anthropic.__version__); print('jinja2', jinja2.__version__)" && grep -q '^reports/$' .gitignore && echo GITIGNORE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `import anthropic` and `import jinja2` succeed and print versions
    - pyproject.toml `dependencies` contains anthropic, weasyprint, jinja2 entries
    - `.gitignore` contains a `reports/` line
  </acceptance_criteria>
  <done>anthropic, weasyprint, jinja2 are in pyproject.toml and importable (weasyprint import may warn locally about Pango — acceptable); `reports/` is gitignored.</done>
</task>

<task type="auto">
  <name>Task 3: Add Report model + Alembic migration</name>
  <files>app/models.py, alembic/versions/</files>
  <read_first>
    - app/models.py (lines 1-6 imports, lines 92-118 Lead, lines 121-150 TrelloCard — mirror these)
    - alembic/versions/ee55974a0232_add_trello_cards_table.py (most recent migration — mirror upgrade/downgrade structure)
    - .planning/phases/05-ai-generated-pdf-reports/05-PATTERNS.md (Report model section, lines 59-74)
  </read_first>
  <action>Add `class Report(Base)` to app/models.py with `__tablename__ = "reports"` and fields per D-63: `id` (Mapped[int] primary_key), `talent_id` (Mapped[int], ForeignKey("talents.id"), index=True), `month` (Mapped[str], String, index=True — "YYYY-MM"), `generated_at` (Mapped[datetime], DateTime, server_default=func.now()), `file_path` (Mapped[str], String), `file_size_bytes` (Mapped[int], Integer, default=0). Add `talent: Mapped["Talent"] = relationship("Talent", lazy="select")`. Add a `UniqueConstraint("talent_id", "month", name="uq_report_talent_month")` in `__table_args__` to enforce D-64 (one report per talent+month, overwrite not version). Import `UniqueConstraint` from sqlalchemy if not already imported. Generate the migration: `uv run alembic revision --autogenerate -m "add_reports_table"`, then run `uv run alembic upgrade head`. Verify the autogenerated migration creates the `reports` table with the unique constraint.</action>
  <verify>
    <automated>uv run alembic upgrade head && uv run python -c "from app.models import Report; from app.database import engine; from sqlalchemy import inspect; cols={c['name'] for c in inspect(engine).get_columns('reports')}; assert {'id','talent_id','month','generated_at','file_path','file_size_bytes'} <= cols, cols; print('REPORTS_TABLE_OK', cols)"</automated>
  </verify>
  <acceptance_criteria>
    - `from app.models import Report` succeeds
    - `reports` table exists after `alembic upgrade head` with columns: id, talent_id, month, generated_at, file_path, file_size_bytes
    - A unique constraint named `uq_report_talent_month` exists on (talent_id, month)
    - New migration file exists under alembic/versions/ referencing the reports table
  </acceptance_criteria>
  <done>Report model added; migration autogenerated and applied; reports table has all 6 columns and the (talent_id, month) unique constraint.</done>
</task>

<task type="auto">
  <name>Task 4: Add schemas, Wave 0 test stubs, and mock fixtures</name>
  <files>app/schemas/reports.py, tests/test_reports.py, tests/conftest.py</files>
  <read_first>
    - app/schemas/leads.py (header + BaseModel pattern — exact analog)
    - tests/test_leads.py (test class organization, auth_client + db_session usage)
    - tests/conftest.py (lines 1-40 imports including line 29 model import; lines 416-506 mock_sheets_rows/mock_trello_transport fixture pattern; end of file for fixture placement)
    - .planning/phases/05-ai-generated-pdf-reports/05-PATTERNS.md (schemas section lines 105-137, conftest fixtures lines 596-631)
    - .planning/phases/05-ai-generated-pdf-reports/05-VALIDATION.md (Per-Task Verification Map — the 7 test function names)
  </read_first>
  <action>Create `app/schemas/reports.py` with plain Pydantic BaseModel classes (no from_attributes — same convention as leads.py): `ReportGenerate` (talent_id: int; month: str with `Field(pattern=r'^\d{4}-\d{2}$')` per V5 input validation), `NarrativeSections` (resumen_ejecutivo: str, deals_destacados: str, recomendacion: str), `ReportOut` (id, talent_id, talent_name: str, month, generated_at: datetime, file_path, file_size_bytes, narrative: NarrativeSections), `ReportHistoryItem` (id, talent_id, talent_name: str, month, generated_at: datetime, file_size_bytes). Use `T | None` not `Optional[T]`. Create `tests/test_reports.py` with FAILING stubs (use `pytest.fail("not implemented — Wave N")` or assertions against not-yet-existing functions) for the 7 functions named in VALIDATION.md: `test_available_months`, `test_generate_report_payload`, `test_pdf_written_to_disk`, `test_generate_endpoint`, `test_list_reports`, `test_download_report`, plus a DASH-05 smoke check `test_reportes_tab_exists` (grep page-reportes in frontend/index.html). Add `Report` to the model import on conftest.py line 29. Add the `mock_anthropic` and `mock_weasyprint` fixtures to conftest.py per 05-PATTERNS.md lines 596-631 (monkeypatch targets: `app.services.reports.anthropic.Anthropic` and `app.services.reports.HTML`). The fixtures reference `app.services.reports` which does not exist yet — that is expected; the fixtures only patch when used, and Wave-2 tests will use them. Ensure `import os` is present in conftest if the weasyprint fixture uses os.makedirs.</action>
  <verify>
    <automated>uv run python -c "from app.schemas.reports import ReportGenerate, NarrativeSections, ReportOut, ReportHistoryItem; ReportGenerate(talent_id=1, month='2025-05'); print('SCHEMAS_OK')" && grep -q "def mock_anthropic" tests/conftest.py && grep -q "def mock_weasyprint" tests/conftest.py && grep -cE "def test_(available_months|generate_report_payload|pdf_written_to_disk|generate_endpoint|list_reports|download_report)" tests/test_reports.py && uv run pytest tests/conftest.py --collect-only -q >/dev/null && echo CONFTEST_IMPORTS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `app.schemas.reports` imports the 4 classes; `ReportGenerate(talent_id=1, month='2025-05')` validates; `ReportGenerate(talent_id=1, month='bad')` raises ValidationError
    - `tests/conftest.py` defines `mock_anthropic` and `mock_weasyprint`, and line 29 model import includes `Report`
    - `tests/test_reports.py` contains the 6 named test functions plus the tab smoke check
    - conftest.py collects without ImportError (`pytest --collect-only` succeeds)
  </acceptance_criteria>
  <done>Schemas defined; conftest has both mock fixtures and Report imported; test_reports.py has failing stubs for all 7 behaviors; conftest collects cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package registry → local env | Installing new PyPI packages introduces supply-chain trust |
| Pydantic schema → service/router | `month` input crosses into file-path and query logic downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-SC | Tampering | pyproject.toml installs (anthropic, weasyprint, jinja2) | mitigate | Blocking human-verify checkpoint (Task 1) confirms all three on PyPI with official source repos before install. Severity: MEDIUM. |
| T-05-VAL | Tampering | `ReportGenerate.month` field | mitigate | Pydantic `Field(pattern=r'^\d{4}-\d{2}$')` rejects malformed month strings at the schema boundary before they reach file-path construction (defends T-path-traversal at the API edge). Severity: HIGH (input feeds file path in 05-02). |
</threat_model>

<verification>
- `uv run python -c "import anthropic, jinja2"` succeeds
- `uv run alembic upgrade head` creates the reports table
- `from app.schemas.reports import ReportGenerate, NarrativeSections, ReportOut, ReportHistoryItem` succeeds
- `uv run pytest tests/test_reports.py -x` runs (stubs fail as expected — they are placeholders)
- `uv run pytest tests/conftest.py --collect-only` succeeds (no import errors)
</verification>

<success_criteria>
- anthropic, weasyprint, jinja2 installed and pinned in pyproject.toml
- Report model + migration applied; reports table has 6 columns + (talent_id, month) unique constraint
- 4 report schemas defined with month-pattern validation
- mock_anthropic + mock_weasyprint fixtures in conftest; Report added to conftest model import
- test_reports.py has failing stubs for all Phase 5 behaviors
</success_criteria>

<output>
Create `.planning/phases/05-ai-generated-pdf-reports/05-01-SUMMARY.md` when done
</output>
