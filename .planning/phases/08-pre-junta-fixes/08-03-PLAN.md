---
phase: 08-pre-junta-fixes
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - frontend/index.html
  - frontend/js/dashboard.js
  - frontend/css/styles.css
autonomous: false
requirements: [FIX-03]
must_haves:
  truths:
    - "The funnel stages 'En ejecución' and 'Cobranza' display a 'vía Trello' badge in both the global funnel and the per-talent funnel"
    - "The revenue section title reads 'Histórico de ingresos por mes' (not 'Proyección de ingresos por mes')"
    - "The revenue chart no longer presents future projections as if they were certain — segment labels read as honest historical/in-progress states"
  artifacts:
    - path: "frontend/js/dashboard.js"
      provides: "TRELLO_STAGES badge logic in renderFunnel and renderTalentFunnel; honest revenue labels in renderIncomeProjection"
      contains: "trello-src-badge"
    - path: "frontend/css/styles.css"
      provides: ".trello-src-badge styling"
      contains: ".trello-src-badge"
    - path: "frontend/index.html"
      provides: "Renamed revenue section title"
      contains: "Histórico de ingresos por mes"
  key_links:
    - from: "frontend/js/dashboard.js renderFunnel()/renderTalentFunnel()"
      to: ".trello-src-badge span"
      via: "TRELLO_STAGES.includes(stage.stage) conditional"
      pattern: "trello-src-badge"
---

<objective>
Implement FIX-03 honesty fixes (decisions D-03 and D-04): (a) annotate the Trello-sourced funnel stages "En ejecución" and "Cobranza" with a "vía Trello" badge so their zeros don't read as bugs, and (b) rename "Proyección de ingresos por mes" to "Histórico de ingresos por mes" and relabel the revenue chart segments to honest states instead of presenting projections as certain.

Purpose: Removes the two places where the deployed dashboard implies data/projections that Pipedrive does not actually provide — critical for credibility in the June 25 Luis Santillán meeting.
Output: A frontend-only slice across HTML (title), JS (badges + honest labels), and CSS (badge style).
</objective>

<execution_context>
@/Users/alexiszamoraia/Developer/projects/lumixia/clients/santillan-ent/seg-dashboard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexiszamoraia/Developer/projects/lumixia/clients/santillan-ent/seg-dashboard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-pre-junta-fixes/08-CONTEXT.md
@.planning/phases/08-pre-junta-fixes/08-RESEARCH.md
@.planning/phases/08-pre-junta-fixes/08-PATTERNS.md

<interfaces>
<!-- Confirmed from source. This plan shares frontend files with 08-02 — apply on top of 08-02's changes. -->

From frontend/js/dashboard.js:
  renderFunnel() lines 397-424 — global funnel, label at line ~413: <span class="f-label">${escHtml(stage.stage)}</span>
  renderTalentFunnel() lines 589-637 — per-talent funnel, label at line ~628: <span class="f-label">${escHtml(stage.stage)}</span>
  renderIncomeProjection() lines 930-979 — legend label "Firmado (Proyección)" at line ~976; bar sublabel `isCurrent ? "(Real)" : "(Proyección)"` at line ~955.
  Backend STAGES (funnel.py lines 19-26): ["Llamada","Cotización","Negociación","Contrato","En ejecución","Cobranza"]. Stages 5 & 6 are Trello-only and always 0 from Pipedrive.

From frontend/index.html:
  Line ~126-127: <div class="section-title">Proyección de ingresos por mes</div> — rename to "Histórico de ingresos por mes".
  Line 8 / dashboard.js include: ?v= cache-buster — ensure it is ?v=20260625a (08-02 already bumps it; if not yet bumped, bump here).

From frontend/css/styles.css:
  .filter-pill.active.ejecucion (line ~1795): background rgba(14,165,176,0.12); border-color #0ea5b0; color #40d4de — reuse these teal values for the badge.
  escHtml() defined at lines 55-60 — all API-sourced strings must pass through it (badge text is a literal, safe).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "vía Trello" badge to Trello-sourced funnel stages</name>
  <files>frontend/js/dashboard.js, frontend/css/styles.css</files>
  <read_first>
    - frontend/js/dashboard.js (renderFunnel lines 397-424; renderTalentFunnel lines 589-637; locate any existing module-level constants like FUNNEL_COLORS to place TRELLO_STAGES alongside; escHtml lines 55-60)
    - frontend/css/styles.css (.filter-pill.active.ejecucion teal values around line 1795; end of file for append point)
  </read_first>
  <action>
    In frontend/js/dashboard.js, define a module-level constant TRELLO_STAGES = ["En ejecución", "Cobranza"] near the existing funnel constants (e.g. next to FUNNEL_COLORS). In renderFunnel() (line ~413) and renderTalentFunnel() (line ~628), compute const trelloBadge = TRELLO_STAGES.includes(stage.stage) ? '<span class="trello-src-badge">vía Trello</span>' : '' and append ${trelloBadge} immediately after ${escHtml(stage.stage)} inside the existing <span class="f-label"> element. Do not change the backend STAGES list and do not alter the stage counts/amounts — this is a label-only change. Keep escHtml() on the stage name.

    In frontend/css/styles.css, append a .trello-src-badge rule: display:inline-block; font-size:9px; font-weight:700; font-family:'Sora',sans-serif; letter-spacing:0.3px; text-transform:uppercase; padding:1px 5px; border-radius:4px; background:rgba(14,165,176,0.12); border:1px solid rgba(14,165,176,0.3); color:#40d4de; margin-left:5px; vertical-align:middle; line-height:1.4 — reusing the teal Trello color values from .filter-pill.active.ejecucion.
  </action>
  <verify>
    <automated>grep -q "TRELLO_STAGES" frontend/js/dashboard.js && grep -c "trello-src-badge" frontend/js/dashboard.js | grep -qE "[2-9]|1[0-9]" && grep -q ".trello-src-badge" frontend/css/styles.css && echo "OK: badge present in both funnels + CSS"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/js/dashboard.js contains TRELLO_STAGES = ["En ejecución", "Cobranza"]
    - frontend/js/dashboard.js references trello-src-badge in BOTH renderFunnel and renderTalentFunnel (badge string appears at least twice)
    - frontend/css/styles.css contains a .trello-src-badge rule using color:#40d4de
    - Backend funnel.py STAGES list is unchanged (no edits to app/services/funnel.py)
  </acceptance_criteria>
  <done>Both funnels annotate "En ejecución" and "Cobranza" with a "vía Trello" badge; counts unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Rename revenue section title + honest segment labels</name>
  <files>frontend/index.html, frontend/js/dashboard.js</files>
  <read_first>
    - frontend/index.html (section-title line ~126-127; cache-buster line 8)
    - frontend/js/dashboard.js (renderIncomeProjection lines 930-979 — legend label line ~976 "Firmado (Proyección)"; bar sublabel line ~955 `isCurrent ? "(Real)" : "(Proyección)"`)
    - .planning/phases/08-pre-junta-fixes/08-RESEARCH.md (FIX-03 section, esp. assumption A3: D-04 "historical only" is implemented as rename + honest labels, NOT removal of segments, pending Luis confirmation post-junta)
  </read_first>
  <action>
    In frontend/index.html, change the revenue section title text from "Proyección de ingresos por mes" to "Histórico de ingresos por mes". Ensure the CSS/JS cache-buster is ?v=20260625a (08-02 already sets this; if for any reason it is still ?v=20260622b, bump it here).

    In frontend/js/dashboard.js renderIncomeProjection(), rename the misleading legend label "Firmado (Proyección)" (line ~976) to "En campaña", and change the bar sublabel at line ~955 from `isCurrent ? "(Real)" : "(Proyección)"` to `isCurrent ? "(Real)" : "(Estimado)"`. Do NOT remove the cobrado/ejecucion/cobranza segments and do NOT invent future dates — keep the existing 4-month window and data source (per D-04 honesty: relabel to truthful states, do not fabricate projections). The green "Cobrado" segment label stays "Cobrado".
  </action>
  <verify>
    <automated>grep -q "Histórico de ingresos por mes" frontend/index.html && ! grep -q "Proyección de ingresos por mes" frontend/index.html && grep -q "En campaña" frontend/js/dashboard.js && ! grep -q "Firmado (Proyección)" frontend/js/dashboard.js && echo "OK: titles and labels renamed"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/index.html contains "Histórico de ingresos por mes" and does NOT contain "Proyección de ingresos por mes"
    - frontend/js/dashboard.js contains "En campaña" and does NOT contain "Firmado (Proyección)"
    - frontend/js/dashboard.js bar sublabel uses "(Estimado)" instead of "(Proyección)"
    - No future-dated projection is fabricated (data source/window unchanged)
  </acceptance_criteria>
  <done>Revenue section renamed to historical and segment labels are honest.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify funnel badges and revenue honesty in the browser</name>
  <files>frontend/index.html, frontend/js/dashboard.js, frontend/css/styles.css (verification only)</files>
  <action>Verification-only checkpoint. Confirm "via Trello" badges appear on "En ejecucion" and "Cobranza" in both the global and per-talent funnels (and nowhere else), and that the revenue section reads "Historico de ingresos por mes" with honest labels and no fabricated future projection (see how-to-verify). No code is written in this task.</action>
  <what-built>
    "vía Trello" badges on the two Trello-sourced funnel stages (global + per-talent), and an honestly-labeled "Histórico de ingresos por mes" revenue chart.
  </what-built>
  <how-to-verify>
    1. Open the Funnel tab. Confirm "En ejecución" and "Cobranza" each show a small "vía Trello" badge; the first four Pipedrive stages do not.
    2. Open Por Talento for any talent and confirm the per-talent funnel shows the same two badges.
    3. Confirm zeros on the two Trello stages now read as "sourced from Trello" rather than looking like a bug.
    4. In Por Talento, confirm the revenue section title reads "Histórico de ingresos por mes" and the legend/sublabels read "En campaña" / "(Estimado)" — no "Proyección" wording remains.
    5. Confirm no future months are invented and no fabricated projection values appear.
  </how-to-verify>
  <verify>
    <human-check>Both funnels show "vía Trello" on the two Trello stages only; revenue section titled "Histórico de ingresos por mes" with honest labels and no fabricated future projection.</human-check>
  </verify>
  <acceptance_criteria>
    - "vía Trello" badge appears on "En ejecución" and "Cobranza" in both global and per-talent funnels, and on no other stage
    - Revenue section title reads "Histórico de ingresos por mes"
    - No "Proyección" wording remains in the revenue chart; no fabricated future months
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe what still looks misleading.</resume-signal>
  <done>Both funnels show the Trello badge on the two Trello stages only; the revenue section is titled "Historico de ingresos por mes" with honest labels and no invented future projection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite/API → funnel & revenue DOM | Stage names and revenue figures rendered into the dashboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | Injection (XSS) | funnel badge + label rendering | mitigate | Badge text is a static literal; stage names continue through escHtml(). No new unescaped API string is introduced. |
| T-08-08 | Repudiation / Misrepresentation | revenue "Proyección" labels | mitigate | This plan's entire purpose is honesty: relabel to truthful states and remove the misleading "Proyección" title so the UI does not imply data Pipedrive lacks. |
| T-08-09 | Tampering | frontend-only changes | accept | No backend/integration writes; CLAUDE.md read-only rule unaffected. |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this plan; nothing to audit. |
</threat_model>

<verification>
- Grep confirms TRELLO_STAGES + trello-src-badge in JS (twice) and CSS.
- Grep confirms "Histórico de ingresos por mes" present and "Proyección de ingresos por mes" absent.
- Grep confirms "En campaña" present and "Firmado (Proyección)" absent.
- Human verification of funnel badges and revenue honesty (Task 3).
</verification>

<success_criteria>
- FIX-03 success criteria met: the funnel visually distinguishes the 4 Pipedrive stages from the 2 Trello-sourced stages, and the revenue chart is renamed "Histórico de ingresos por mes" showing only honest historical/in-progress data with no invented future projections.
</success_criteria>

<output>
Create `.planning/phases/08-pre-junta-fixes/08-03-SUMMARY.md` when done.
</output>
