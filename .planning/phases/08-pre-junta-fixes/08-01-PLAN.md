---
phase: 08-pre-junta-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/sync/jobs.py
  - tests/conftest.py
  - tests/test_sync.py
autonomous: false
requirements: [FIX-01]
must_haves:
  truths:
    - "sync_pipedrive writes Deal.loss_reason from the Pipedrive standard top-level field deal['lost_reason']"
    - "After a re-sync, lost deals in SQLite have loss_reason populated with the real reason text (not NULL)"
    - "The 'Oportunidades perdidas' donut in Por Talento shows real loss-reason percentages, not 'Sin razón — 100%'"
  artifacts:
    - path: "app/sync/jobs.py"
      provides: "Standard-field read of lost_reason during Pipedrive sync"
      contains: "deal.get(\"lost_reason\")"
    - path: "tests/test_sync.py"
      provides: "Regression test proving loss_reason is synced from the standard field"
      contains: "loss_reason"
  key_links:
    - from: "app/sync/jobs.py"
      to: "deals.loss_reason column"
      via: "existing_deal.loss_reason = loss_reason"
      pattern: "existing_deal\\.loss_reason = loss_reason"
    - from: "app/services/kpis.py talent_detail()"
      to: "deals.loss_reason"
      via: "deal.loss_reason or 'Sin razón'"
      pattern: "loss_reason or"
---

<objective>
Fix the FIX-01 critical data bug: the "Oportunidades perdidas" donut shows "Sin razón — 100%" because the Pipedrive sync reads `lost_reason` as a custom field (always None) instead of as a standard top-level deal field.

Purpose: This is the highest-priority pre-junta fix. Luis Santillán's approved PDF expects real loss-reason breakdowns; the current display is visibly broken.
Output: A one-line root-cause fix in `app/sync/jobs.py`, a regression test, and a re-sync so the data backfills before the June 25 meeting.
</objective>

<execution_context>
@/Users/alexiszamoraia/Developer/projects/lumixia/clients/santillan-ent/seg-dashboard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexiszamoraia/Developer/projects/lumixia/clients/santillan-ent/seg-dashboard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-pre-junta-fixes/08-CONTEXT.md
@.planning/phases/08-pre-junta-fixes/08-RESEARCH.md
@.planning/phases/08-pre-junta-fixes/08-PATTERNS.md

<interfaces>
<!-- Confirmed from source. The column name is loss_reason (correct). The bug is purely in how the field is READ during sync. -->

From app/models.py (line 60) — column name is correct, do NOT rename:
  loss_reason: Mapped[str | None] = mapped_column(String, nullable=True)

From app/sync/jobs.py — the bug site:
  Line 80:   loss_reason_key = key_by_name.get("Razón de pérdida")   # custom-field lookup, always None for a standard field
  Lines 146-149:
    loss_reason = (
        pipedrive.resolve_custom_field(deal, loss_reason_key, option_labels)
        if loss_reason_key
        else None
    )
  Line 197: existing_deal.loss_reason = loss_reason   # correct, unchanged

  Standard-field read pattern already used in the same loop (copy this style):
    value = float(deal.get("value") or 0)   # line 130
    status = deal["status"]                 # line 133

From app/services/kpis.py (lines 203-204) — already correct, NO change needed:
  reason = deal.loss_reason or "Sin razón"
  reason_counts[reason] = reason_counts.get(reason, 0) + 1

From tests/conftest.py:
  mock_pipedrive_transport fixture serves PIPEDRIVE_DEALS_PAGE_1 / PIPEDRIVE_DEALS_PAGE_2 (lines 286, 319).
  These mock deal dicts currently have NO "lost_reason" key — the test must add one to a lost deal.
  Existing tests use: db_session, seed_talent_products, mock_pipedrive_transport, monkeypatch.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — failing regression test for lost_reason standard-field read</name>
  <files>tests/conftest.py, tests/test_sync.py</files>
  <read_first>
    - tests/conftest.py (read lines 246-340 for PIPEDRIVE_DEAL_FIELDS, PIPEDRIVE_DEALS_PAGE_1, PIPEDRIVE_DEALS_PAGE_2 shapes; lines 476-554 for mock_pipedrive_transport handler)
    - tests/test_sync.py (read existing test patterns — test_sync_paginates_and_upserts_deals at line 12 shows the fixture wiring: db_session, seed_talent_products, mock_pipedrive_transport, monkeypatch)
    - app/sync/jobs.py (current broken state at lines 80, 146-149, 197)
  </read_first>
  <behavior>
    - Add a top-level "lost_reason" key with value "Ya no contestó" to at least one deal whose "status" is "lost" inside the PIPEDRIVE_DEALS_PAGE_1 (or PAGE_2) mock constant in tests/conftest.py.
    - Test test_lost_reason_synced_from_standard_field: after running sync_pipedrive with mock_pipedrive_transport, the corresponding Deal row in SQLite has loss_reason == "Ya no contestó" (not None).
    - Test asserts at least one synced Deal has a non-null loss_reason equal to the mock value.
    - This test MUST FAIL initially (RED) because jobs.py currently reads the field via the custom-field resolver and writes None.
  </behavior>
  <action>
    In tests/conftest.py, locate a "lost"-status deal dict inside PIPEDRIVE_DEALS_PAGE_1 or PIPEDRIVE_DEALS_PAGE_2 (the constants defined at lines 286 and 319). Add a top-level key "lost_reason" with the string value "Ya no contestó" to that deal dict. If no lost-status deal exists in the mock pages, add the key to an existing deal AND set its "status" to "lost" so the field has a place to land. Do NOT add "lost_reason" into any custom_fields sub-dict — it must be a top-level standard field to reproduce the production reality.

    In tests/test_sync.py, add a new test function test_lost_reason_synced_from_standard_field(db_session, seed_talent_products, mock_pipedrive_transport, monkeypatch) mirroring the fixture wiring and monkeypatch usage of test_sync_paginates_and_upserts_deals (line 12). After calling the sync (use the same sync invocation the existing tests use), query the Deal table for the deal id that carries the mock lost_reason and assert that its loss_reason attribute equals "Ya no contestó".

    Run the test and confirm it FAILS with loss_reason being None.
  </action>
  <verify>
    <automated>uv run pytest tests/test_sync.py -x -k lost_reason 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_sync.py contains def test_lost_reason_synced_from_standard_field(
    - tests/conftest.py contains the literal string "Ya no contestó" inside a deal dict with "status": "lost"
    - `uv run pytest tests/test_sync.py -x -k lost_reason` FAILS (RED state) before Task 2, with the failure being loss_reason == None rather than the expected reason
  </acceptance_criteria>
  <done>New test exists and fails because loss_reason is None.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix sync/jobs.py to read lost_reason as a standard field (GREEN)</name>
  <files>app/sync/jobs.py</files>
  <read_first>
    - app/sync/jobs.py (lines 75-200: the field-map setup at line 80, the deal loop standard-field reads at lines 130-135, the custom-field loss_reason resolution at lines 146-149, the write at line 197, and the error-handling wrapper at lines 216-222)
    - app/integrations/pipedrive.py (resolve_custom_field lines 67-96 — confirms it reads only deal["custom_fields"], which is why standard lost_reason is never found)
    - CLAUDE.md (CRITICAL constraints: read-only integrations; TRELLO_AUTO_CREATE_ENABLED must remain False; do not touch that flag at line ~37 of jobs.py)
  </read_first>
  <action>
    Replace the custom-field resolution of loss_reason (lines 146-149) with a direct standard top-level read: assign loss_reason = deal.get("lost_reason") or None. This mirrors the existing standard-field read pattern in the same loop (value = float(deal.get("value") or 0) at line 130, status = deal["status"] at line 133).

    Remove the now-dead loss_reason_key lookup at line 80 (loss_reason_key = key_by_name.get("Razón de pérdida")) — it is no longer referenced. Leave brand_category_key and expected_collection_date_key lookups untouched (those remain legitimate custom-field lookups).

    Do NOT change line 197 (existing_deal.loss_reason = loss_reason) — it is already correct. Do NOT touch TRELLO_AUTO_CREATE_ENABLED. Do NOT change app/models.py, app/services/kpis.py, or any frontend file — they are already correct.

    Run the Task 1 test and confirm it now PASSES.
  </action>
  <verify>
    <automated>uv run pytest tests/test_sync.py -x -k lost_reason 2>&1 | tail -10 && uv run pytest tests/test_sync.py tests/test_kpis.py -q 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - app/sync/jobs.py contains deal.get("lost_reason")
    - app/sync/jobs.py does NOT contain the string key_by_name.get("Razón de pérdida")
    - app/sync/jobs.py still contains TRELLO_AUTO_CREATE_ENABLED = False (flag untouched)
    - `uv run pytest tests/test_sync.py -x -k lost_reason` exits 0 (GREEN)
    - `uv run pytest tests/test_sync.py tests/test_kpis.py -q` passes with no regressions
  </acceptance_criteria>
  <done>loss_reason is read from the standard field; the regression test passes and no existing sync/kpis tests break.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Trigger production re-sync and verify donut shows real loss reasons</name>
  <files>app/sync/jobs.py (re-sync trigger; verification only — no code change in this task)</files>
  <action>Verification-only checkpoint. Trigger a production re-sync and confirm the donut now shows real loss reasons (see how-to-verify). If still "Sin razón — 100%", force a full re-sync by clearing the last successful SyncLog finished_at so updated_since becomes None, then re-sync. No code is written in this task.</action>
  <what-built>
    The sync now reads lost_reason from the Pipedrive standard field. Existing lost deals in production still have loss_reason = NULL until a sync re-fetches them. Because the sync is incremental (only deals updated since the last SyncLog are fetched), old lost deals may not be re-fetched by an ordinary "Sincronizar ahora".
  </what-built>
  <how-to-verify>
    1. Trigger a re-sync in production. First attempt: click "Sincronizar ahora" in the dashboard.
    2. Open Por Talento for a talent known to have lost deals. Confirm the "Oportunidades perdidas" donut now shows one or more of the 5 real reasons ("Bateado por Talento", "Ya no contestó", "Cambió su estrategia de campaña", "Ya no se hizo la campaña o evento", "Se le hizo caro") with real percentages — NOT "Sin razón — 100%".
    3. If the donut still shows "Sin razón — 100%" after the first sync: the incremental filter did not re-fetch old lost deals. Force a full re-sync by nullifying/clearing the last successful SyncLog finished_at (so updated_since becomes None) and sync again. Re-check the donut.
    4. Confirm no Pipedrive/Trello/Sheets data was modified (this remains read-only — only local SQLite was written).
  </how-to-verify>
  <verify>
    <human-check>Donut shows real loss reasons with non-uniform percentages for at least one talent; "Sin razón — 100%" no longer appears for talents with categorized lost deals.</human-check>
  </verify>
  <acceptance_criteria>
    - "Oportunidades perdidas" donut in Por Talento renders at least one of the 5 real loss reasons for a talent with lost deals
    - "Sin razón — 100%" is no longer the sole slice for talents whose lost deals have a reason in Pipedrive
  </acceptance_criteria>
  <resume-signal>Type "approved" once the donut shows real reasons, or describe what still shows "Sin razón".</resume-signal>
  <done>Production donut shows the real 5 loss reasons with correct percentages; "Sin razón — 100%" no longer appears for talents with categorized lost deals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pipedrive API → sync/jobs.py | Untrusted external string `lost_reason` enters local SQLite |
| SQLite → frontend donut | Stored loss_reason rendered into the Por Talento donut |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `existing_deal.loss_reason = deal.get("lost_reason")` | accept | Pipedrive is a trusted first-party source; the value is one of 5 known options. Written only to local SQLite; integration stays read-only. |
| T-08-02 | Injection (XSS) | frontend donut rendering of loss_reason | mitigate | Frontend renderLostOpportunities already routes API strings through escHtml(); no new sink introduced by this plan. |
| T-08-03 | Information Disclosure | re-sync writes only to seg.db | accept | No Pipedrive/Trello/Sheets writes; CLAUDE.md read-only rule honored. TRELLO_AUTO_CREATE_ENABLED left False. |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this plan; nothing to audit. |
</threat_model>

<verification>
- `uv run pytest tests/test_sync.py tests/test_kpis.py -q` passes.
- Grep confirms `deal.get("lost_reason")` present and `key_by_name.get("Razón de pérdida")` absent in app/sync/jobs.py.
- Grep confirms `TRELLO_AUTO_CREATE_ENABLED = False` still present.
- Human verification of the production donut (Task 3).
</verification>

<success_criteria>
- FIX-01 success criterion met: the "Oportunidades perdidas" donut shows the real loss reasons with correct percentages, not "Sin razón — 100%".
- No existing tests regress; no external integration data modified.
</success_criteria>

<output>
Create `.planning/phases/08-pre-junta-fixes/08-01-SUMMARY.md` when done.
</output>
