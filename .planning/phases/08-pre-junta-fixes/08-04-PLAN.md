---
phase: 08-pre-junta-fixes
plan: 04
type: execute
wave: 4
depends_on: ["08-03"]
files_modified:
  - frontend/js/dashboard.js
  - frontend/css/styles.css
autonomous: false
requirements: [FIX-04]
must_haves:
  truths:
    - "The 'Sin talento asignado' row in the Resumen ranking is visually distinct and more actionable than a plain ranking row"
    - "APScheduler is confirmed running every 30 minutes in production and writing SyncLog rows per source"
  artifacts:
    - path: "frontend/js/dashboard.js"
      provides: "Enhanced sin-talento ranking row presentation"
      contains: "is_sin_talento"
  key_links:
    - from: "frontend/js/dashboard.js renderRanking()"
      to: ".rank-row sin-talento styling"
      via: "isSinTalento branch"
      pattern: "isSinTalento"
---

<objective>
OPTIONAL (Wave 4 — only if Waves 1-3 are complete with time remaining Wednesday). Implement FIX-04 polish per decision D-05: (a) make the "Sin talento asignado" row in the Resumen ranking more actionable/visible, and (b) verify APScheduler is running every 30 minutes in production and logging to SyncLog.

Purpose: Two low-priority quality improvements for the June 25 meeting. Skip entirely if Waves 1-3 consume the available time.
Output: A small frontend enhancement to the sin-talento ranking row and an operational verification of the scheduler.
</objective>

<execution_context>
@/Users/alexiszamoraia/Developer/projects/lumixia/clients/santillan-ent/seg-dashboard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexiszamoraia/Developer/projects/lumixia/clients/santillan-ent/seg-dashboard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-pre-junta-fixes/08-CONTEXT.md
@.planning/phases/08-pre-junta-fixes/08-RESEARCH.md
@.planning/phases/08-pre-junta-fixes/08-PATTERNS.md

<interfaces>
<!-- Confirmed from source. -->

From frontend/js/dashboard.js renderRanking() (lines 320-360):
  The sin-talento row is the ranking row where row.is_sin_talento is true.
  Current styling: avatarStyle = style="background:var(--bg5);color:var(--text3);" and avatarContent "?".
  Row markup uses .rank-row / .rank-num / .rank-avatar / .rank-info / .rank-name / .rank-nicho / .rank-val.
  All API strings already pass through escHtml().

From app/sync/scheduler.py:
  BackgroundScheduler with add_job(_run_all_syncs, "interval", minutes=30, id="sync_all", replace_existing=True).
  _run_all_syncs runs sync_pipedrive → sync_sheets → sync_trello, each writing its own SyncLog(source=...) row.
  Started/stopped via app/main.py lifespan (main.py lines 34/36).

From app/models.py:
  class SyncLog (line 81) — has source, status, started_at, finished_at columns.

From frontend/css/styles.css:
  Existing .rank-row family rules style the ranking list; append a .rank-row.sin-talento modifier if needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the "Sin talento asignado" ranking row more actionable</name>
  <files>frontend/js/dashboard.js, frontend/css/styles.css</files>
  <read_first>
    - frontend/js/dashboard.js (renderRanking lines 320-360 — the isSinTalento branch, avatar/subcopy construction, .rank-row template)
    - frontend/css/styles.css (existing .rank-row / .rank-avatar / .rank-nicho rules; end of file for append point; reuse the amber accent variables used elsewhere, e.g. --amber/--amberT)
  </read_first>
  <action>
    In frontend/js/dashboard.js renderRanking(), when isSinTalento is true: add a CSS class "sin-talento" to the rank-row container (className becomes "rank-row sin-talento") and make the row read as an actionable callout — change the rank-nicho subcopy for the sin-talento row to surface the count and total clearly (e.g. "{deal_count} deals sin asignar · {formatMXN(revenue)}") using the existing row.deal_count and row.revenue values and formatMXN/escHtml helpers. Keep the "?" avatar. Do not change the data or the ordering (sin-talento still renders last as it already does). Do not introduce new API fields.

    In frontend/css/styles.css, append a .rank-row.sin-talento rule that visually distinguishes the row (e.g. a subtle amber left border or tinted background using existing variables such as --amber / rgba amber tint and --bg variables) so it reads as a "needs attention" callout rather than a normal ranking entry. Keep it consistent with the dark-mode design system; no new color literals beyond the existing amber palette.
  </action>
  <verify>
    <automated>grep -q "rank-row sin-talento\|sin-talento" frontend/js/dashboard.js && grep -q ".rank-row.sin-talento\|.sin-talento" frontend/css/styles.css && echo "OK: sin-talento styling present"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/js/dashboard.js applies a "sin-talento" class to the sin-talento rank-row and surfaces deal_count + revenue in its subcopy
    - frontend/css/styles.css contains a .sin-talento modifier rule using the existing amber palette
    - No new API fields introduced; sin-talento row still renders last
  </acceptance_criteria>
  <done>The sin-talento row is visually distinct and shows actionable count/amount.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify APScheduler 30-min sync + sin-talento card in production</name>
  <files>frontend/js/dashboard.js, app/sync/scheduler.py, app/models.py (verification only)</files>
  <action>Verification-only checkpoint. Confirm the "Sin talento asignado" row renders as an amber actionable callout with count + amount, and verify the production SyncLog has recent per-source rows ~30 min apart confirming APScheduler is live (see how-to-verify). No code is written in this task.</action>
  <what-built>
    The sin-talento ranking row is now an actionable callout. Separately, this task verifies the existing 30-minute APScheduler sync is alive in production and logging per-source SyncLog rows.
  </what-built>
  <how-to-verify>
    1. Load the Resumen tab. Confirm the "Sin talento asignado" row stands out (amber callout) and shows the deals-sin-asignar count and total amount.
    2. Verify the scheduler in production: confirm app/sync/scheduler.py runs add_job(..., "interval", minutes=30, ...) and is started by the app lifespan.
    3. Inspect SyncLog: confirm recent rows exist for each source (pipedrive, sheets, trello) with timestamps roughly 30 minutes apart and status reflecting success. Either query the SyncLog table directly in production or use the "Última sync" indicator in the UI to confirm a recent successful sync.
    4. Confirm no Pipedrive/Trello/Sheets data was modified by the verification (read-only respected).
  </how-to-verify>
  <verify>
    <human-check>Sin-talento row renders as an amber actionable callout; SyncLog shows recent per-source rows ~30 min apart confirming the scheduler is live.</human-check>
  </verify>
  <acceptance_criteria>
    - "Sin talento asignado" row in Resumen is visually distinct and shows count + amount
    - Production SyncLog contains recent rows for pipedrive/sheets/trello consistent with a 30-minute interval
    - No external integration data modified during verification
  </acceptance_criteria>
  <resume-signal>Type "approved" once the card and scheduler are verified, or describe what is missing.</resume-signal>
  <done>Sin-talento row is an amber actionable callout with count + amount; production SyncLog shows recent per-source rows consistent with a 30-minute interval.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite → Resumen ranking DOM | sin-talento count/amount rendered into the ranking row |
| Scheduler → SQLite SyncLog | Background job writes only local SyncLog rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | Injection (XSS) | sin-talento subcopy rendering | mitigate | deal_count is numeric and revenue goes through formatMXN; any string passes through escHtml() as in the existing row. |
| T-08-11 | Denial of Service | 30-min scheduler interval | accept | Interval unchanged (minutes=30); no new load introduced. Verification is read-only. |
| T-08-12 | Tampering | scheduler writes only SyncLog | accept | Background job writes only to local SQLite; integrations remain read-only; TRELLO_AUTO_CREATE_ENABLED untouched. |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this plan; nothing to audit. |
</threat_model>

<verification>
- Grep confirms sin-talento class in JS and CSS.
- Human verification of the sin-talento callout and production SyncLog/scheduler (Task 2).
</verification>

<success_criteria>
- FIX-04 (optional) success criteria met: the "Sin talento asignado" card is more actionable, and APScheduler is confirmed running every 30 minutes and logging to SyncLog in production.
</success_criteria>

<output>
Create `.planning/phases/08-pre-junta-fixes/08-04-SUMMARY.md` when done.
</output>
