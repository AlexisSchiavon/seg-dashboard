---
phase: 08-pre-junta-fixes
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - app/services/kpis.py
  - app/schemas/dashboard.py
  - app/routers/dashboard.py
  - frontend/index.html
  - frontend/js/dashboard.js
  - frontend/css/styles.css
  - tests/test_kpis.py
  - tests/test_dashboard.py
autonomous: false
requirements: [FIX-02]
must_haves:
  truths:
    - "Por Talento shows a segmented control above the KPI cards with two views: 'Flujo de dinero' (default) and 'Operativa'"
    - "'Flujo de dinero' view shows 3 tiles: Campañas firmadas (azul), Cobrado (verde), Pendiente por cobrar (naranja)"
    - "'Operativa' view shows the existing KPI cards unchanged"
    - "GET /dashboard/talents/{id} response includes a flujo_dinero field with the 3 tiles"
  artifacts:
    - path: "app/services/kpis.py"
      provides: "flujo_dinero_kpis(db, talent_id) computing Campañas firmadas / Cobrado / Pendiente"
      contains: "def flujo_dinero_kpis"
    - path: "app/schemas/dashboard.py"
      provides: "flujo_dinero optional field on TalentDetail"
      contains: "flujo_dinero"
    - path: "frontend/js/dashboard.js"
      provides: "setKpiView toggle handler + cached talent detail"
      contains: "function setKpiView"
    - path: "frontend/css/styles.css"
      provides: ".kpi-toggle segmented control styles"
      contains: ".kpi-toggle"
  key_links:
    - from: "app/routers/dashboard.py get_talent_detail()"
      to: "kpi_service.flujo_dinero_kpis"
      via: "flujo_dinero=flujo_dinero_tiles in TalentDetail constructor"
      pattern: "flujo_dinero_kpis"
    - from: "frontend/js/dashboard.js setKpiView()"
      to: "_talentDetailData.flujo_dinero"
      via: "renderKpisInto(...flujo_dinero..., 'talent-kpis')"
      pattern: "flujo_dinero"
---

<objective>
Implement FIX-02: a segmented control (toggle) above the Por Talento KPI cards offering two views — "Flujo de dinero" (PDF-aligned, default) and "Operativa" (current KPIs) — per decision D-02.

Purpose: Aligns the Por Talento view with the PDF Luis Santillán approved (azul/verde/naranja money-flow tiles) while preserving the existing operational KPIs.
Output: A new backend KPI computation, a schema/router field, and a full frontend slice (HTML toggle + JS state + CSS) — one complete vertical slice.
</objective>

<execution_context>
@/Users/alexiszamoraia/Developer/projects/lumixia/clients/santillan-ent/seg-dashboard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexiszamoraia/Developer/projects/lumixia/clients/santillan-ent/seg-dashboard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-pre-junta-fixes/08-CONTEXT.md
@.planning/phases/08-pre-junta-fixes/08-RESEARCH.md
@.planning/phases/08-pre-junta-fixes/08-PATTERNS.md

<interfaces>
<!-- Confirmed from source. Reuse these exact identifiers; no codebase exploration needed. -->

From app/schemas/dashboard.py:
  class KpiTile(BaseModel)  (line 11) — already defined. flujo_dinero reuses list[KpiTile].
  class TalentDetail(BaseModel) (line 112): has kpis, funnel, lost_summary, lost_opportunities,
    brand_categories, income_projection|None (line 122). Add: flujo_dinero: list[KpiTile] | None = None

From app/services/kpis.py:
  Two-column aggregate pattern (talent_detail, ~lines 174-183):
    row = db.query(func.count(Deal.id), func.coalesce(func.sum(Deal.value), 0.0))\
            .filter(Deal.talent_id == talent_id, ...).one()
  KPI tile shape: {"label": str, "value": float, "count": int|None, "variant": str}
  CRITICAL: stage_name is normalized to "Contrato" (NOT "Contrato y factura") by _STAGE_CANONICAL in jobs.py line 92.

From app/models.py:
  TrelloCard has deal_id FK and list_state column. "Cobrado" = TrelloCard.list_state == "cerrado".

From app/routers/dashboard.py get_talent_detail() (lines 126-182):
  kpi_service imported at line 33. Phase-4 pattern at lines 162-168 builds typed lists, e.g.
  income_proj = [MonthProjection(**p) for p in proj_dicts] if proj_dicts else None
  TalentDetail(...) constructor at lines 170-182 — add flujo_dinero=flujo_dinero_tiles.

From frontend/js/dashboard.js:
  Module state pattern (lines 17-21): let _campaignFilter = 'all';
  setCampaignFilter pattern (lines 811-824) — copy structure for setKpiView.
  loadTalentDetail (lines 1196-1234): line 1214 calls renderKpisInto(data.kpis, "talent-kpis").
  renderKpisInto already escapes labels via escHtml (line ~577).

From frontend/index.html:
  Line 8: CSS link with ?v=20260622b cache-buster — bump to ?v=20260625a.
  Lines 119-122: <div class="section"><div class="kpi-row-3" id="talent-kpis"></div></div> — insert toggle above #talent-kpis.

From frontend/css/styles.css:
  .filter-pill pattern (lines 1756-1798) and .kpi-t variants (lines 1424-1506: .kpi-t.blue/.green/.amber).
  Variables available: --bg4, --bg2, --border, --borderM, --r, --text, --text2.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend — flujo_dinero_kpis service + schema field + router wiring (with tests)</name>
  <files>app/services/kpis.py, app/schemas/dashboard.py, app/routers/dashboard.py, tests/test_kpis.py, tests/test_dashboard.py</files>
  <read_first>
    - app/services/kpis.py (lines 1-17 imports; talent_detail lines 147-249 for the two-column aggregate query pattern and KPI tile shape; coalesce guard usage at lines 29-31, 44-47)
    - app/schemas/dashboard.py (KpiTile at line 11; TalentDetail at lines 112-124, note income_projection|None pattern at line 122)
    - app/routers/dashboard.py (get_talent_detail lines 126-182; Phase-4 typed-list build at lines 162-168; TalentDetail constructor lines 170-182)
    - app/models.py (TrelloCard model: deal_id FK, list_state column)
    - tests/conftest.py (seed_deals fixture lines 155-355 — note deal_lost has stage_name="Negociación"; the test must seed a "Contrato"-stage deal and a TrelloCard with list_state="cerrado")
    - tests/test_kpis.py (lines 1-13 imports; existing talent_detail tests for fixture wiring)
    - tests/test_dashboard.py (test_talent_detail tests at lines 170+ for auth_client usage)
  </read_first>
  <behavior>
    - test_flujo_dinero_kpis: given a talent with a Deal in stage_name "Contrato" status "open" of value 100000 and a second "Contrato"/"won" deal of value 50000, and one TrelloCard with list_state "cerrado" linked to a deal of value 30000 → flujo_dinero_kpis returns 3 tiles: Campañas firmadas value 150000 count 2 variant "blue"; Cobrado value 30000 variant "green"; Pendiente por cobrar value 120000 variant "amber".
    - Pendiente never goes negative (max(0.0, firmadas - cobrado)).
    - test_talent_detail_includes_flujo_dinero (integration): GET /dashboard/talents/{id} response JSON contains a "flujo_dinero" key that is a list of 3 tiles.
  </behavior>
  <action>
    In app/services/kpis.py add def flujo_dinero_kpis(db: Session, talent_id: int) -> dict returning {"kpis": [...]} with three tiles. Campañas firmadas: a two-column aggregate (func.count(Deal.id), func.coalesce(func.sum(Deal.value), 0.0)) filtered by Deal.talent_id == talent_id, Deal.stage_name == "Contrato" (normalized name — NOT "Contrato y factura"), Deal.status.in_(["won", "open"]); tile label "Campañas firmadas", variant "blue", count = the count. Cobrado: func.coalesce(func.sum(Deal.value), 0.0) joined to TrelloCard on TrelloCard.deal_id == Deal.id filtered by Deal.talent_id == talent_id and TrelloCard.list_state == "cerrado", guard with `or 0.0` after .scalar(); tile label "Cobrado", variant "green", count None. Pendiente por cobrar: pendiente = max(0.0, firmadas_value - cobrado_value); tile label "Pendiente por cobrar", variant "amber", count None. Import TrelloCard locally inside the function (mirrors existing service pattern).

    In app/schemas/dashboard.py add to TalentDetail: flujo_dinero: list[KpiTile] | None = None (Optional so existing tests that construct TalentDetail without it keep passing).

    In app/routers/dashboard.py get_talent_detail(), after the Phase-4 typed-list block (~line 168) and before the TalentDetail(...) return, add: flujo_data = kpi_service.flujo_dinero_kpis(db, talent_id); flujo_dinero_tiles = [KpiTile(**k) for k in flujo_data["kpis"]]. Add flujo_dinero=flujo_dinero_tiles as a keyword arg to the TalentDetail(...) constructor. Confirm KpiTile is imported in this router (it is used for kpi_tiles already).

    Add the two tests described in <behavior> to tests/test_kpis.py and tests/test_dashboard.py.
  </action>
  <verify>
    <automated>uv run pytest tests/test_kpis.py tests/test_dashboard.py -x -k "flujo or talent_detail" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - app/services/kpis.py contains def flujo_dinero_kpis(
    - app/services/kpis.py contains Deal.stage_name == "Contrato" (normalized, not "Contrato y factura")
    - app/schemas/dashboard.py contains flujo_dinero: list[KpiTile] | None = None
    - app/routers/dashboard.py contains flujo_dinero_kpis( and flujo_dinero=flujo_dinero_tiles
    - `uv run pytest tests/test_kpis.py -x -k flujo` exits 0
    - `uv run pytest tests/test_dashboard.py -x -k flujo` exits 0 with flujo_dinero present in the response JSON
  </acceptance_criteria>
  <done>Service returns the 3 money-flow tiles; endpoint exposes flujo_dinero; tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend — toggle HTML + setKpiView JS + .kpi-toggle CSS</name>
  <files>frontend/index.html, frontend/js/dashboard.js, frontend/css/styles.css</files>
  <read_first>
    - frontend/index.html (line 8 CSS cache-buster; lines 119-127: #talent-kpis section + section-title)
    - frontend/js/dashboard.js (module state lines 17-21; setCampaignFilter lines 811-824; loadTalentDetail lines 1196-1234, esp. line 1214 renderKpisInto; renderKpisInto + escHtml ~line 577)
    - frontend/css/styles.css (.filter-pill lines 1756-1798; .kpi-t variants lines 1424-1506)
  </read_first>
  <action>
    In frontend/index.html, inside the existing <div class="section"> at lines 119-122, insert ABOVE <div class="kpi-row-3" id="talent-kpis"></div> a <div class="kpi-toggle" id="kpi-toggle"> containing two buttons: class "kpi-toggle-btn active" with onclick="setKpiView('flujo')" data-view="flujo" text "Flujo de dinero", and class "kpi-toggle-btn" with onclick="setKpiView('operativa')" data-view="operativa" text "Operativa". Bump the CSS cache-buster on line 8 from ?v=20260622b to ?v=20260625a (also bump any matching JS ?v= params on the dashboard.js include so the toggle JS loads fresh).

    In frontend/js/dashboard.js, add module-level state near the existing let _campaignFilter (lines 17-21): let _kpiView = 'flujo'; and let _talentDetailData = null;. Add function setKpiView(view) mirroring setCampaignFilter: set _kpiView, toggle the .active class on each .kpi-toggle-btn by matching btn.dataset.view === view, guard `if (!_talentDetailData) return;`, then call renderKpisInto(_talentDetailData.flujo_dinero || [], 'talent-kpis') when view === 'flujo' else renderKpisInto(_talentDetailData.kpis, 'talent-kpis'). In loadTalentDetail (around line 1206-1214), after `const data = await res.json();` set _talentDetailData = data; _kpiView = 'flujo'; reset the toggle buttons' active state to the flujo button; and replace the line renderKpisInto(data.kpis, "talent-kpis") with renderKpisInto(data.flujo_dinero || data.kpis, 'talent-kpis') so the default view is Flujo de dinero.

    In frontend/css/styles.css, append a .kpi-toggle block (display:flex; gap:4px; padding:4px; background:var(--bg4); border:1px solid var(--border); border-radius:var(--r); margin-bottom:12px) and a .kpi-toggle-btn block (flex:1; padding:7px 12px; font-size:12px; font-weight:500; font-family:'DM Sans',sans-serif; border-radius:7px; border:none; background:transparent; color:var(--text2); cursor:pointer; transition:all 0.15s) plus .kpi-toggle-btn:hover {color:var(--text)} and .kpi-toggle-btn.active {background:var(--bg2); border:1px solid var(--borderM); color:var(--text)}. Keep it mobile-responsive (flex:1 makes the two buttons split evenly). No JS frameworks.
  </action>
  <verify>
    <automated>grep -q "kpi-toggle" frontend/index.html && grep -q "function setKpiView" frontend/js/dashboard.js && grep -q ".kpi-toggle-btn" frontend/css/styles.css && grep -q "20260625a" frontend/index.html && echo "OK: all toggle pieces present"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/index.html contains id="kpi-toggle" with two buttons calling setKpiView('flujo') and setKpiView('operativa')
    - frontend/index.html line 8 CSS link uses ?v=20260625a (cache-bust bumped)
    - frontend/js/dashboard.js contains function setKpiView and the strings _kpiView and _talentDetailData
    - frontend/js/dashboard.js loadTalentDetail caches data into _talentDetailData and defaults render to data.flujo_dinero
    - frontend/css/styles.css contains .kpi-toggle and .kpi-toggle-btn.active rules
  </acceptance_criteria>
  <done>Toggle markup, JS handler, and CSS all present; cache-buster bumped.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify toggle renders and switches views in the browser</name>
  <files>frontend/index.html, frontend/js/dashboard.js, frontend/css/styles.css (verification only)</files>
  <action>Verification-only checkpoint. Load the dashboard and confirm the Por Talento KPI toggle renders, defaults to Flujo de dinero, switches to Operativa and back without reload, shows azul/verde/naranja tiles with correct arithmetic, and is mobile-responsive (see how-to-verify). No code is written in this task.</action>
  <what-built>
    A segmented control above the Por Talento KPI cards. "Flujo de dinero" (default) shows Campañas firmadas (azul), Cobrado (verde), Pendiente por cobrar (naranja); "Operativa" shows the original KPIs.
  </what-built>
  <how-to-verify>
    1. Load the dashboard and open Por Talento for a talent with signed campaigns.
    2. Confirm the toggle appears above the KPI tiles with "Flujo de dinero" active by default.
    3. Confirm the three Flujo de dinero tiles render with the correct colors: Campañas firmadas azul, Cobrado verde, Pendiente por cobrar naranja. Confirm Pendiente = Campañas firmadas − Cobrado.
    4. Click "Operativa" — the original KPI cards return without a page reload. Click "Flujo de dinero" — money-flow tiles return.
    5. Switch to a different talent — confirm the view resets to "Flujo de dinero" and tiles update.
    6. Verify on a narrow (mobile) viewport that the two toggle buttons split the width evenly and remain usable.
  </how-to-verify>
  <verify>
    <human-check>Toggle defaults to Flujo de dinero, renders 3 colored tiles, switches to Operativa and back without reload, and is mobile-responsive.</human-check>
  </verify>
  <acceptance_criteria>
    - Toggle visible above #talent-kpis with "Flujo de dinero" active on load
    - Flujo de dinero shows azul/verde/naranja tiles with correct arithmetic (Pendiente = Firmadas − Cobrado)
    - Clicking "Operativa" restores original KPIs without a reload; clicking back restores money-flow
    - Toggle remains usable on a mobile-width viewport
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe rendering/switching issues.</resume-signal>
  <done>Toggle defaults to Flujo de dinero, renders 3 correctly-colored tiles, switches to Operativa and back without reload, and is mobile-responsive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /dashboard/talents/{id} | Authenticated request; talent_id path param is untrusted input |
| SQLite → frontend tiles | flujo_dinero tile labels/values rendered into DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering | talent_id path param in flujo_dinero_kpis | mitigate | Reuse the existing parameterized SQLAlchemy query binding (no string interpolation); same auth as the existing talent-detail endpoint. |
| T-08-05 | Injection (XSS) | renderKpisInto rendering flujo_dinero labels | mitigate | Tile labels are server-controlled literals; renderKpisInto already routes label text through escHtml(). No raw API string bypasses escaping. |
| T-08-06 | Elevation of Privilege | new endpoint field | accept | flujo_dinero is exposed on the already-auth-protected GET /dashboard/talents/{id}; no new route, no new auth surface. |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this plan; nothing to audit. |
</threat_model>

<verification>
- `uv run pytest tests/test_kpis.py tests/test_dashboard.py -x -k "flujo or talent_detail"` passes.
- Grep confirms toggle HTML, setKpiView JS, .kpi-toggle CSS, and the bumped ?v=20260625a cache-buster.
- Human verification of in-browser toggle behavior (Task 3).
</verification>

<success_criteria>
- FIX-02 success criterion met: Por Talento has a visible toggle between "Flujo de dinero" (PDF-aligned) and "Operativa" (current) KPI views, defaulting to Flujo de dinero with azul/verde/naranja tiles.
- No JS frameworks added; mobile-responsive.
</success_criteria>

<output>
Create `.planning/phases/08-pre-junta-fixes/08-02-SUMMARY.md` when done.
</output>
