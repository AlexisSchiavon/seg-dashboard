---
phase: 04-trello-integration-collection-automation
plan: 04
type: execute
wave: 4
depends_on: ["04-02"]
files_modified:
  - app/services/trello_service.py
  - app/schemas/dashboard.py
  - app/routers/dashboard.py
  - frontend/js/dashboard.js
  - tests/test_trello.py
  - tests/test_dashboard.py
autonomous: false
requirements: [DASH-02]
must_haves:
  truths:
    - "income_projection returns 4 months (1 prior, current, 2 future) of cobrado/proyeccion/pendiente per talent"
    - "GET /dashboard/talents/{id} response includes income_projection, payment_calendar, and deals"
    - "The Por talento tab renders the stacked projection bars, payment calendar, top 3 campaigns, and the campaign table from real deal data"
  artifacts:
    - path: "app/services/trello_service.py"
      provides: "income_projection, payment_calendar, deals_for_talent"
      contains: "def income_projection"
    - path: "app/schemas/dashboard.py"
      provides: "MonthProjection, CalendarEntry, DealRow and extended TalentDetail"
      contains: "class MonthProjection"
    - path: "app/routers/dashboard.py"
      provides: "get_talent_detail extended with the three new fields"
      contains: "income_projection"
  key_links:
    - from: "app/routers/dashboard.py get_talent_detail"
      to: "app.services.trello_service.income_projection"
      via: "service call"
      pattern: "income_projection"
    - from: "frontend/js/dashboard.js loadTalentDetail"
      to: "data.deals"
      via: "renderTopCampaigns and renderCampaignTable"
      pattern: "data.deals"
---

<objective>
Dashboard slice (DASH-02): implement the revenue projection math in trello_service, extend the TalentDetail schema and the GET /dashboard/talents/{id} endpoint with income_projection, payment_calendar, and individual deals, and wire the already-built frontend render functions to the new data. Completes the Por talento tab.

Purpose: Delivers DASH-02 — the monthly revenue projection stacked bars (cobrado/proyeccion/pendiente), collection calendar, top 3 campaigns, and full campaign table — using real Trello+Pipedrive data.
Output: income_projection/payment_calendar/deals_for_talent service functions, extended schema + endpoint, frontend render wiring, green DASH-02 tests.
</objective>

<execution_context>
@/Users/alexiszamoraia/Developer/projects/lumixia/clients/santillan-ent/seg-dashboard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexiszamoraia/Developer/projects/lumixia/clients/santillan-ent/seg-dashboard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-trello-integration-collection-automation/04-RESEARCH.md
@.planning/phases/04-trello-integration-collection-automation/04-PATTERNS.md
@.planning/phases/04-trello-integration-collection-automation/04-UI-SPEC.md
@.planning/phases/04-trello-integration-collection-automation/04-02-SUMMARY.md

<interfaces>
From Plan 04-02 (now exists):
  app/services/trello_service.py has _month_helpers, _normalize, resolve_collection_date already may exist;
  app/models.py TrelloCard: deal_id (FK deals.id), list_state (ejecucion/cobranza/cerrado), collection_date (date)
  app/models.py Deal: id, value (float, this is venta_total per D-47), talent_id, add_time, title, status

From app/schemas/dashboard.py (current TalentDetail, lines 83-92):
  class TalentDetail(BaseModel): talent_id:int; name:str; category:str|None=None; kpis:list[KpiTile];
    funnel:list[StageBucket]; lost_summary:list[LostReasonSummary];
    lost_opportunities:list[LostOpportunity]; brand_categories:list[BrandCategorySlice]

From app/routers/dashboard.py get_talent_detail (lines 122-166):
  404 guard via db.get(Talent, talent_id); try detail = kpi_service.talent_detail(db, talent_id);
  builds typed lists then returns TalentDetail(...). Router already protected by get_current_user.

From frontend/js/dashboard.js:
  line 967: renderIncomeProjection(data.income_projection || null)  -- already called
  line 968: renderPaymentCalendar(data.payment_calendar || null)    -- already called
  lines 977-979 CURRENTLY: const activeStages = (data.funnel||[]).filter(s=>s.count>0);
    renderTopCampaigns(activeStages); renderCampaignTable(activeStages, data.lost_opportunities);
  renderTopCampaigns and renderCampaignTable currently expect {stage,count,amount}; must accept {title,amount,list_state}.
  escHtml() must wrap every API string (CR-02).

Projection contract (D-47, D-49, D-50, RESEARCH.md Pattern 4):
  income_projection item: {month: "Jun 2026", cobrado: float, proyeccion: float, pendiente: float}
  payment_calendar item: {month: "Jun 2026", amount: float}
  month label uses calendar.month_abbr (English 3-letter) e.g. "Jun 2026" (Pitfall 3).
  4-month sliding window anchored today: prior month, current, plus 2 future.
  cobrado = cards list_state cerrado; proyeccion = ejecucion; pendiente = cobranza. Amount = Deal.value (venta_total).
  Group by collection_date month; null collection_date falls back to Deal.add_time month plus 2 (already in resolver).
  DealRow: {title, amount, list_state, trello_card_id|None}. list_state may also be perdido for lost deals (UI sbadge).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: projection + calendar + deals service functions</name>
  <files>app/services/trello_service.py, tests/test_trello.py</files>
  <read_first>
    - app/services/trello_service.py (existing helpers from Plan 04-02: _month_label or add if missing, resolve_collection_date)
    - app/models.py (TrelloCard, Deal fields)
    - .planning/phases/04-trello-integration-collection-automation/04-RESEARCH.md (Pattern 4 income_projection)
    - tests/test_trello.py (stub test_income_projection_math to fill in)
  </read_first>
  <behavior>
    - income_projection(db, talent_id) returns exactly 4 dicts in window order, each {month, cobrado, proyeccion, pendiente}
    - A cerrado card for the talent adds its deal value to cobrado in the card collection_date month; ejecucion adds to proyeccion; cobranza adds to pendiente
    - Cards whose resolved month falls outside the 4-month window are excluded
    - payment_calendar(db, talent_id) returns 4 dicts {month, amount} where amount is cobrado+proyeccion+pendiente (or the pendiente layer per UI-SPEC — follow UI-SPEC; default to total expected collection per month)
    - deals_for_talent(db, talent_id) returns DealRow-shaped dicts {title, amount, list_state, trello_card_id} sorted by amount desc
  </behavior>
  <action>
    In app/services/trello_service.py add (or reuse from 04-02) _month_label(d) returning month_abbr index plus space plus year, and _sliding_window_months(anchor) returning the four first-of-month dates for deltas minus1, 0, 1, 2 using divmod month arithmetic. Implement income_projection(db, talent_id): build the window and a result dict keyed by month label with zeroed cobrado/proyeccion/pendiente; query TrelloCard joined to Deal on TrelloCard.deal_id equals Deal.id filtered by Deal.talent_id equals talent_id; for each (card, deal) resolve the month using card.collection_date if set else the resolve_collection_date fallback on deal, take its first-of-month label, skip when outside the window, then add deal.value (venta_total per D-47) to cobrado when list_state cerrado, proyeccion when ejecucion, pendiente when cobranza; return the list of dicts in window order. Implement payment_calendar(db, talent_id) returning {month, amount} per window month where amount is the sum of the three layers for that month (follow 04-UI-SPEC if it specifies a single layer; otherwise total). Implement deals_for_talent(db, talent_id) returning a list of {title, amount, list_state, trello_card_id} from the talent's TrelloCard-linked deals (and include lost deals as list_state perdido if the UI-SPEC campaign table shows them), sorted by amount descending. Fill in test_income_projection_math in tests/test_trello.py using seed_deals + seed_trello_cards to assert the layer sums for a known month, and remove its pytest.skip.
  </action>
  <verify>
    <automated>cd /Users/alexiszamoraia/Developer/projects/lumixia/clients/santillan-ent/seg-dashboard && python3 -m pytest tests/test_trello.py -k income_projection_math -x -q && python3 -c "from app.services.trello_service import income_projection, payment_calendar, deals_for_talent; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - app/services/trello_service.py contains "def income_projection(", "def payment_calendar(", "def deals_for_talent("
    - income_projection returns a list of length 4 in unit test
    - month labels use the English 3-letter abbreviation format (e.g. matches the regex for three letters, space, four digits)
    - python3 -m pytest tests/test_trello.py -k income_projection_math -x -q passes (1 passed)
  </acceptance_criteria>
  <done>The three service functions exist and return the documented shapes; income_projection_math test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: extend TalentDetail schema and endpoint</name>
  <files>app/schemas/dashboard.py, app/routers/dashboard.py, tests/test_dashboard.py</files>
  <read_first>
    - app/schemas/dashboard.py (BaseModel patterns; current TalentDetail lines 83-92)
    - app/routers/dashboard.py (get_talent_detail lines 122-166; imports lines 18-32)
    - app/services/trello_service.py (income_projection, payment_calendar, deals_for_talent from Task 1)
    - tests/test_dashboard.py (existing talent-detail tests; add test_talent_detail_includes_income_projection)
  </read_first>
  <behavior>
    - TalentDetail gains income_projection, payment_calendar, deals — all Optional defaulting to None (Pitfall 4: must not break the 111 existing tests)
    - GET /dashboard/talents/{id} for a talent with trello data returns income_projection as a 4-item list and includes payment_calendar and deals keys
    - GET /dashboard/talents/{id} for a talent with no trello data still returns 200 with the three fields null or empty (backward compatible)
  </behavior>
  <action>
    In app/schemas/dashboard.py add three new BaseModel classes before TalentDetail: MonthProjection with month str, cobrado float, proyeccion float, pendiente float; CalendarEntry with month str and amount float; DealRow with title str, amount float, list_state str, trello_card_id str-or-None default None. Add three new fields to TalentDetail, each Optional defaulting to None: income_projection of list MonthProjection or None, payment_calendar of list CalendarEntry or None, deals of list DealRow or None. In app/routers/dashboard.py import trello_service from app.services and import MonthProjection, CalendarEntry, DealRow from app.schemas.dashboard; inside get_talent_detail, after building the existing typed lists and within the same try, call trello_service.income_projection(db, talent_id), trello_service.payment_calendar(db, talent_id), and trello_service.deals_for_talent(db, talent_id); add to the TalentDetail return: income_projection as a list of MonthProjection built from each dict when the result is non-empty else None, payment_calendar likewise as CalendarEntry, deals likewise as DealRow. Add test_talent_detail_includes_income_projection in tests/test_dashboard.py that seeds a talent with deals + trello cards and asserts the response json contains the income_projection key with 4 entries.
  </action>
  <verify>
    <automated>cd /Users/alexiszamoraia/Developer/projects/lumixia/clients/santillan-ent/seg-dashboard && python3 -m pytest tests/test_dashboard.py -k "talent_detail" -x -q && python3 -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - app/schemas/dashboard.py contains "class MonthProjection", "class CalendarEntry", "class DealRow"
    - TalentDetail declares income_projection, payment_calendar, deals all with a None default (grep finds "income_projection" with "None")
    - python3 -m pytest tests/test_dashboard.py -k talent_detail -x -q passes including test_talent_detail_includes_income_projection
    - python3 -m pytest -q full suite green (the prior 111 tests not broken)
  </acceptance_criteria>
  <done>TalentDetail extended with three Optional fields; endpoint populates them; new integration test green; no regression.</done>
</task>

<task type="auto">
  <name>Task 3: wire frontend render functions to deal data</name>
  <files>frontend/js/dashboard.js</files>
  <read_first>
    - frontend/js/dashboard.js (loadTalentDetail lines 943-979; renderTopCampaigns line 817; renderCampaignTable line 847; escHtml usage)
    - .planning/phases/04-trello-integration-collection-automation/04-UI-SPEC.md (deal row shape, sbadge classes, top-3 rule)
  </read_first>
  <behavior>
    - loadTalentDetail passes data.deals (individual deal objects) to renderTopCampaigns and renderCampaignTable instead of funnel stage aggregates
    - renderTopCampaigns renders the top 3 deals by amount descending using fields title, amount, list_state
    - renderCampaignTable renders one row per deal with title, amount, and an sbadge whose class derives from list_state (ejecucion/cobranza/cerrado/perdido)
    - When data.deals is null/empty both functions show their existing empty-state message (no funnel fallback)
    - Every API string is wrapped in escHtml (CR-02 / XSS mitigation)
  </behavior>
  <action>
    In frontend/js/dashboard.js loadTalentDetail, replace the lines that compute activeStages from data.funnel and pass them to renderTopCampaigns and renderCampaignTable with code that uses const activeDeals = data.deals || []; renderTopCampaigns(activeDeals); renderCampaignTable(activeDeals, data.lost_opportunities). Update renderTopCampaigns(deals) to sort the deals by amount descending, take the first 3, and render each medal-card using escHtml on title and a formatted amount; render the existing empty-state when the array is empty. Update renderCampaignTable(deals, lostOpps) to render one ctable-row per deal with escHtml(title), the formatted amount, and an sbadge element whose CSS class maps from list_state (ejecucion, cobranza, cerrado, perdido) per 04-UI-SPEC; keep the existing empty-state path. Do not add new CSS — the medal-card, ctable-row, and sbadge classes already exist. Leave renderIncomeProjection and renderPaymentCalendar unchanged (they already consume the new fields).
  </action>
  <verify>
    <automated>cd /Users/alexiszamoraia/Developer/projects/lumixia/clients/santillan-ent/seg-dashboard && node --check frontend/js/dashboard.js && grep -q 'data.deals' frontend/js/dashboard.js && echo ok</automated>
  </verify>
  <acceptance_criteria>
    - node --check frontend/js/dashboard.js exits 0 (valid JS)
    - frontend/js/dashboard.js loadTalentDetail contains "data.deals" passed to renderTopCampaigns and renderCampaignTable (no longer activeStages from data.funnel)
    - renderCampaignTable derives the sbadge class from list_state and wraps title in escHtml
    - grep finds escHtml applied around the deal title in renderTopCampaigns and renderCampaignTable
  </acceptance_criteria>
  <done>Frontend renders top 3 campaigns and the campaign table from data.deals with escHtml; projection and calendar already wired; valid JS.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: visual verification of Por talento tab</name>
  <what-built>The Por talento tab now renders the monthly revenue projection (cobrado/proyeccion/pendiente stacked bars), the collection calendar, the top 3 campaigns, and the full campaign table from real Trello+Pipedrive data via the extended /dashboard/talents/{id} endpoint.</what-built>
  <how-to-verify>
    1. Start the app (uvicorn app.main:app --reload) and log in.
    2. Open the dashboard, go to the Por talento tab, and select a talent that has Trello cards (e.g. one with deals in Cobrar/Finalizados).
    3. Confirm the income projection chart shows stacked bars across ~4 months with green (cobrado), blue (proyeccion), and amber (pendiente) segments.
    4. Confirm the payment calendar timeline shows month nodes with amounts.
    5. Confirm Top 3 campaigns shows 3 medal cards with real deal titles and amounts, sorted by amount.
    6. Confirm the campaign table lists individual deals with status badges (ejecucion/cobranza/cerrado).
    7. Select a talent with no Trello data and confirm placeholders/empty states render without errors (no console errors).
  </how-to-verify>
  <resume-signal>Type "approved" or describe what rendered incorrectly</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client to /dashboard/talents/{id} | Authenticated browser request; talent_id is user-supplied path param |
| API strings to DOM | Deal titles (originally from Pipedrive/Trello) rendered into the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Spoofing / Access | get_talent_detail | mitigate | Inherited router dependency get_current_user; unauthenticated requests get 401 |
| T-04-11 | Tampering (XSS) | renderTopCampaigns / renderCampaignTable | mitigate | All API-sourced strings (deal title) wrapped in escHtml before insertion into the DOM (CR-02) |
| T-04-12 | Information Disclosure | TalentDetail response | mitigate | response_model=TalentDetail validates and filters output shape; new fields are explicit typed schemas, no raw DB rows leaked |
| T-04-13 | Tampering | talent_id path param | mitigate | FastAPI coerces talent_id to int (422 on non-int); 404 via db.get guard |
| T-04-SC | Tampering | pip installs | accept | Zero new packages installed this phase |
</threat_model>

<verification>
- python3 -m pytest tests/test_trello.py -k income_projection_math -x -q green
- python3 -m pytest tests/test_dashboard.py -k talent_detail -x -q green (incl. test_talent_detail_includes_income_projection)
- python3 -m pytest -q full suite green
- node --check frontend/js/dashboard.js valid
- Human checkpoint: Por talento tab renders all four DASH-02 components correctly
</verification>

<success_criteria>
- income_projection/payment_calendar/deals_for_talent return the documented shapes
- GET /dashboard/talents/{id} includes the three new Optional fields
- Por talento tab renders stacked projection bars, calendar, top 3 campaigns, and campaign table from real data
- Full suite green; existing 111 tests unbroken
</success_criteria>

<output>
Create .planning/phases/04-trello-integration-collection-automation/04-04-SUMMARY.md when done
</output>
