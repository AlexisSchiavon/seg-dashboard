---
phase: 04-trello-integration-collection-automation
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - app/sync/jobs.py
  - app/services/trello_service.py
  - tests/test_trello.py
autonomous: true
requirements: [TRELLO-03]
must_haves:
  truths:
    - "A won Pipedrive deal with no linked Trello card gets a card created in the Contrato list"
    - "The created card description contains the marker with the Pipedrive deal id"
    - "A won deal that already has a linked Trello card is not duplicated on the next sync"
  artifacts:
    - path: "app/sync/jobs.py"
      provides: "reconciliation step in sync_trello that creates cards for won deals"
      contains: "create_card"
    - path: "app/services/trello_service.py"
      provides: "_make_card_desc deal-id header writer"
      contains: "def _make_card_desc"
  key_links:
    - from: "app/sync/jobs.py sync_trello"
      to: "app.integrations.trello.create_card"
      via: "POST new card for won deal"
      pattern: "create_card"
    - from: "app/sync/jobs.py reconciliation"
      to: "trello_cards.pipedrive_deal_id_desc"
      via: "idempotency guard before POST"
      pattern: "pipedrive_deal_id_desc"
---

<objective>
Auto-creation slice: extend sync_trello with a reconciliation step that finds won Pipedrive deals (status equals won) lacking a linked TrelloCard and creates one Trello card in the Contrato list, writing the deal-id marker into the card description and immediately upserting the new TrelloCard row. The guard ensures at most one card per deal across sync runs.

Purpose: Delivers TRELLO-03 (auto-create on deal won) via polling reconciliation rather than a one-shot webhook, matching the existing sync architecture. This is the only write to an external system in the project.
Output: _make_card_desc + reconciliation logic, idempotent card creation, green tests for create + no-duplicate + desc-header.
</objective>

<execution_context>
@/Users/alexiszamoraia/Developer/projects/lumixia/clients/santillan-ent/seg-dashboard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexiszamoraia/Developer/projects/lumixia/clients/santillan-ent/seg-dashboard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-trello-integration-collection-automation/04-RESEARCH.md
@.planning/phases/04-trello-integration-collection-automation/04-02-SUMMARY.md

<interfaces>
From Plan 04-01 and 04-02 (now exist):
  app/integrations/trello.py: create_card(client, list_id, name, desc="", due=None) returns dict;
    CONTRATO_LIST_ID equals 69312ac640ae158381706ff8 (the entry list, D-46)
  app/sync/jobs.py: sync_trello(db) reads/upserts cards by trello_card_id; has SyncLog source trello guard
  app/services/trello_service.py: resolve_deal_id, resolve_collection_date, _extract_deal_id_from_desc

From app/models.py:
  Deal: id (PK), pipedrive_id (int unique), title (str), status (str open/won/lost)
  TrelloCard: trello_card_id (unique), deal_id (FK deals.id), pipedrive_deal_id_desc (int), list_id, list_name, list_state

Description marker format (RESEARCH.md, free-plan fallback for D-43):
  first line of card desc is the literal bracket seg colon deal_id equals N bracket, e.g. for deal 12345
  the marker text is bracket seg colon deal_id equals 12345 bracket.

Idempotency (RESEARCH.md Pitfall 1): before POST, check the TrelloCard table for a row whose
  pipedrive_deal_id_desc equals deal.pipedrive_id OR whose deal_id equals deal.id; if found, skip creation.
  Write the new TrelloCard row (with trello_card_id from the API response) before db.commit so a commit
  failure still leaves the created card discoverable on the next sync via its desc marker.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _make_card_desc deal-id header writer</name>
  <files>app/services/trello_service.py, tests/test_trello.py</files>
  <read_first>
    - app/services/trello_service.py (existing _extract_deal_id_from_desc and regex from Plan 04-02)
    - .planning/phases/04-trello-integration-collection-automation/04-RESEARCH.md (Code Examples: Writing Deal ID when Creating Card)
    - tests/test_trello.py (stub test_card_desc_contains_deal_id to fill in)
  </read_first>
  <behavior>
    - _make_card_desc(12345) returns a string whose first line is the deal-id marker for 12345
    - _make_card_desc(12345, "extra notes") returns the marker, a blank line, then the extra notes
    - _extract_deal_id_from_desc(_make_card_desc(12345)) round-trips to 12345 (writer and reader are consistent)
  </behavior>
  <action>
    In app/services/trello_service.py add def _make_card_desc(pipedrive_deal_id, extra_desc="") that builds the header string as the literal bracket seg colon deal_id equals the integer bracket, and if extra_desc is non-empty returns the header, two newlines, then extra_desc, otherwise just the header. Ensure the marker it writes matches the _DEAL_ID_RE used by _extract_deal_id_from_desc so a round-trip returns the same integer. Fill in test_card_desc_contains_deal_id in tests/test_trello.py asserting the round-trip and the first-line marker, and remove its pytest.skip.
  </action>
  <verify>
    <automated>cd /Users/alexiszamoraia/Developer/projects/lumixia/clients/santillan-ent/seg-dashboard && python3 -c "from app.services.trello_service import _make_card_desc as w, _extract_deal_id_from_desc as r; assert r(w(12345))==12345; print('ok')" && python3 -m pytest tests/test_trello.py -k card_desc_contains_deal_id -x -q</automated>
  </verify>
  <acceptance_criteria>
    - app/services/trello_service.py contains "def _make_card_desc("
    - the round-trip python assertion above exits 0
    - python3 -m pytest tests/test_trello.py -k card_desc_contains_deal_id -x -q passes (1 passed)
  </acceptance_criteria>
  <done>_make_card_desc writes a marker that _extract_deal_id_from_desc reads back; desc-header test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: reconciliation step creates cards for won deals</name>
  <files>app/sync/jobs.py, tests/test_trello.py</files>
  <read_first>
    - app/sync/jobs.py (sync_trello from Plan 04-02; the upsert loop and SyncLog success/error path)
    - app/integrations/trello.py (create_card, CONTRATO_LIST_ID)
    - app/services/trello_service.py (_make_card_desc, resolve_collection_date)
    - .planning/phases/04-trello-integration-collection-automation/04-RESEARCH.md (Pitfall 1 duplicate card creation)
    - tests/test_trello.py (stubs test_auto_create_card_for_won_deal, test_no_duplicate_card_creation)
  </read_first>
  <behavior>
    - After the card upsert loop, sync_trello queries deals with status equals won and, for each one with no existing TrelloCard linked by deal_id or by pipedrive_deal_id_desc, calls trello.create_card in the Contrato list with name equal to the deal title and desc containing the deal-id marker
    - The newly created card is immediately upserted into trello_cards (trello_card_id from the API response, deal_id set to the local deal id, pipedrive_deal_id_desc set to deal.pipedrive_id, list_state ejecucion, list_name Contrato)
    - A second sync run with the same won deal does not call create_card again (the existing TrelloCard row guards it)
    - Card creation count is included in records_synced
  </behavior>
  <action>
    In app/sync/jobs.py, inside sync_trello after the read/upsert loop and before the success commit, add a reconciliation block: build a set of already-linked deal pipedrive ids by querying TrelloCard.pipedrive_deal_id_desc (non-null) and TrelloCard.deal_id (mapped back to Deal.pipedrive_id), then query db for Deal rows with status equal to won; for each won deal whose pipedrive_id is not in that set and whose id is not already linked, build desc via trello_service._make_card_desc(deal.pipedrive_id), call trello.create_card(client, trello.CONTRATO_LIST_ID, deal.title, desc=desc), then create and add a TrelloCard row setting trello_card_id from the response id, name deal.title, list_id CONTRATO_LIST_ID, list_name "Contrato", list_state "ejecucion", deal_id deal.id, pipedrive_deal_id_desc deal.pipedrive_id, collection_date via trello_service.resolve_collection_date(response.get('due'), deal), and increment records_synced. Keep the whole block inside the existing try so a Trello error is captured as str(exc) in the SyncLog. The idempotency guard (skip when already linked) is mandatory per Pitfall 1. Fill in test_auto_create_card_for_won_deal (assert create_card is invoked once and a TrelloCard with the deal link and marker is written) and test_no_duplicate_card_creation (run sync twice, assert create_card invoked only on the first run) using mock_trello_transport plus a seeded won Deal, and remove their pytest.skip.
  </action>
  <verify>
    <automated>cd /Users/alexiszamoraia/Developer/projects/lumixia/clients/santillan-ent/seg-dashboard && python3 -m pytest tests/test_trello.py -k "auto_create or no_duplicate" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - app/sync/jobs.py sync_trello references trello.create_card and trello.CONTRATO_LIST_ID
    - app/sync/jobs.py reconciliation queries Deal status won and guards on pipedrive_deal_id_desc / deal_id before creating
    - python3 -m pytest tests/test_trello.py -k "auto_create or no_duplicate" -x -q passes (2 passed)
    - python3 -m pytest tests/test_trello.py -q full trello file green (no skips remain except the two DASH-02 / income tests owned by Plan 04)
  </acceptance_criteria>
  <done>sync_trello creates exactly one Contrato-list card per won deal lacking a link, writes the marker, and never duplicates; auto_create and no_duplicate tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sync_trello to Trello API (write) | The only outbound write in the system; creates cards on the live board |
| won-deal set to card creation | Reconciliation logic decides whether to POST; a bug causes write amplification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Elevation of Privilege / write amplification | reconciliation card POST | mitigate | Idempotency guard checks pipedrive_deal_id_desc and deal_id before create_card so at most one card per deal is created across runs (Pitfall 1) |
| T-04-08 | Tampering | card desc marker | mitigate | Marker is written by _make_card_desc with a numeric deal id only; the reader regex accepts only digits, so a malformed desc cannot mis-link a deal |
| T-04-09 | Information Disclosure | create_card error path | mitigate | The reconciliation block is inside the existing try; on error only str(exc) is persisted, never the trello client or response (carries key+token) |
| T-04-SC | Tampering | pip installs | accept | Zero new packages installed this phase |
</threat_model>

<verification>
- python3 -m pytest tests/test_trello.py -k "card_desc_contains_deal_id or auto_create or no_duplicate" -x -q green
- python3 -m pytest -q full suite green
- Manual (per VALIDATION.md): mark a test deal won, run sync, confirm a card appears in the En ejecucion / Contrato list with the deal-id marker in its description
</verification>

<success_criteria>
- Won deals without a linked card get exactly one Contrato-list card with the deal-id marker
- No duplicate cards on repeated syncs (idempotency guard)
- Errors captured as str(exc) only
</success_criteria>

<output>
Create .planning/phases/04-trello-integration-collection-automation/04-03-SUMMARY.md when done
</output>
