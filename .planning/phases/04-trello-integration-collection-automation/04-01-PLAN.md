---
phase: 04-trello-integration-collection-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models.py
  - alembic/versions/XXXX_add_trello_cards_table.py
  - app/integrations/trello.py
  - tests/test_trello.py
  - tests/conftest.py
autonomous: true
requirements: [TRELLO-01]
must_haves:
  truths:
    - "A trello_cards table exists in seg.db with the TrelloCard schema"
    - "trello.py can fetch cards from a Trello list and create a card via httpx"
    - "tests/test_trello.py exists with all Wave 0 test stubs (pending or green)"
  artifacts:
    - path: "app/models.py"
      provides: "TrelloCard SQLAlchemy model"
      contains: "class TrelloCard"
    - path: "app/integrations/trello.py"
      provides: "Trello httpx wrapper + LIST_STATE_MAP"
      contains: "LIST_STATE_MAP"
    - path: "alembic/versions/XXXX_add_trello_cards_table.py"
      provides: "trello_cards table migration"
      contains: "create_table"
    - path: "tests/test_trello.py"
      provides: "Wave 0 test stubs for all phase-4 requirements"
      contains: "def test_list_state_mapping"
  key_links:
    - from: "app/models.py TrelloCard.deal_id"
      to: "deals.id"
      via: "ForeignKey"
      pattern: "ForeignKey..deals.id."
---

<objective>
Foundation slice for Trello integration: create the TrelloCard SQLAlchemy model plus Alembic migration, the trello.py httpx wrapper (read cards, create card, hardcoded LIST_STATE_MAP), and the Wave 0 test stubs in tests/test_trello.py that all later waves turn green.

Purpose: Establish the data contract (TrelloCard table) and the I/O boundary (trello.py) so the sync service and projection math (waves 2-4) build against concrete interfaces. The test stubs make every phase-4 requirement testable before implementation.
Output: app/models.py (TrelloCard added), new Alembic migration, app/integrations/trello.py, tests/test_trello.py stubs, tests/conftest.py fixtures.
</objective>

<execution_context>
@/Users/alexiszamoraia/Developer/projects/lumixia/clients/santillan-ent/seg-dashboard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexiszamoraia/Developer/projects/lumixia/clients/santillan-ent/seg-dashboard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-trello-integration-collection-automation/04-RESEARCH.md
@.planning/phases/04-trello-integration-collection-automation/04-PATTERNS.md
@.planning/phases/04-trello-integration-collection-automation/04-VALIDATION.md

<interfaces>
Concrete contracts the executor must respect (extracted from codebase).

From app/models.py current imports (line 1-6):
  from datetime import datetime
  from sqlalchemy import String, Boolean, Float, Integer, ForeignKey, DateTime, func
  from sqlalchemy.orm import Mapped, mapped_column, relationship
  from app.database import Base
  Deal table PK is deals.id (int); Deal.pipedrive_id is the unique int key.
  Lead class (lines 92-119) is the style analog: Mapped plus mapped_column, nullable FK with index.

From app/integrations/pipedrive.py (wrapper analog):
  BASE_URL module constant; def _client() returns httpx.Client; uses get_with_retry(client, path, params)
  Pipedrive uses x-api-token HEADER. Trello uses key and token QUERY PARAMS instead.

From app/integrations/base.py:
  def get_with_retry(client, path, params, max_attempts=5) returns httpx.Response

From app/config.py:
  settings.TRELLO_API_KEY, settings.TRELLO_TOKEN, settings.TRELLO_BOARD_IDS  (all present in .env)

Verified Trello board (live API, 2026-06-14), board id 69312a9d5523703a1ce1a413.
LIST_STATE_MAP (list_id maps to state):
  69312ac640ae158381706ff8 maps to ejecucion  (Contrato)   <- CONTRATO_LIST_ID, entry list for new cards
  69312acb534b0e80508bf4e5 maps to ejecucion  (Firmar contrato todos)
  69312ad08fe346b82da12e1d maps to ejecucion  (Enviar factura)
  69312ad63829ef3ac9967d1a maps to cobranza   (Cobrar)
  69312adeac51905b84f53c35 maps to cobranza   (Enviar encuesta)
  69d8336e46709e935f4307fe maps to cerrado     (Finalizados)
  6996256c42ccdae7f69e4814 is ABSENT from map = ignored (Otros pendientes)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TrelloCard model and Alembic migration</name>
  <files>app/models.py, alembic/versions/XXXX_add_trello_cards_table.py</files>
  <read_first>
    - app/models.py (current state; Lead class lines 92-119 is the style analog; imports line 1-6)
    - alembic/versions/d48d69b17ea6_add_leads_table.py (migration analog; header, upgrade, downgrade)
    - app/database.py (Base declarative base)
  </read_first>
  <behavior>
    - TrelloCard maps to table trello_cards with columns: id (PK), trello_card_id (String unique index), name (String), list_id (String), list_name (String), list_state (String), deal_id (Integer FK deals.id nullable index), pipedrive_deal_id_desc (Integer nullable), collection_date (Date nullable), synced_at (DateTime server_default func.now onupdate func.now)
    - After alembic upgrade head, seg.db has a trello_cards table with a unique index on trello_card_id and an index on deal_id
  </behavior>
  <action>
    Extend app/models.py imports: change the datetime import to also import date, and add Date to the sqlalchemy import line (so it reads String, Boolean, Float, Integer, ForeignKey, DateTime, Date, func). Add a TrelloCard(Base) class after the Lead class with __tablename__ set to trello_cards and exactly these mapped columns: id primary_key; trello_card_id String unique index; name String; list_id String; list_name String; list_state String; deal_id as Mapped int-or-None mapped_column ForeignKey deals.id nullable True index True (FK targets deals.id the local PK, NOT deals.pipedrive_id); pipedrive_deal_id_desc as Mapped int-or-None mapped_column Integer nullable True; collection_date as Mapped date-or-None mapped_column Date nullable True; synced_at as Mapped datetime mapped_column DateTime server_default func.now onupdate func.now. Generate the migration with alembic revision --autogenerate -m "add trello_cards table" then confirm the file sets down_revision to d48d69b17ea6, calls op.create_table for trello_cards with a sa.Date collection_date column and a sa.ForeignKeyConstraint from deal_id to deals.id, creates a unique index ix_trello_cards_trello_card_id and a non-unique ix_trello_cards_deal_id, and the downgrade drops both indexes then the table. Run alembic upgrade head.
  </action>
  <verify>
    <automated>cd /Users/alexiszamoraia/Developer/projects/lumixia/clients/santillan-ent/seg-dashboard && alembic upgrade head && python3 -c "from app.models import TrelloCard; assert TrelloCard.__tablename__ == 'trello_cards'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - app/models.py contains the literal "class TrelloCard(Base):"
    - python3 -c "from app.models import TrelloCard; print(TrelloCard.__tablename__)" prints trello_cards
    - grep finds ForeignKey targeting deals.id in app/models.py
    - alembic upgrade head exits 0 and PRAGMA table_info(trello_cards) lists collection_date, list_state, deal_id, pipedrive_deal_id_desc, trello_card_id
    - The migration file down_revision equals d48d69b17ea6
  </acceptance_criteria>
  <done>trello_cards table exists in seg.db after migration; TrelloCard importable with FK to deals.id; migration chains from the leads migration.</done>
</task>

<task type="auto">
  <name>Task 2: Create trello.py httpx wrapper with LIST_STATE_MAP</name>
  <files>app/integrations/trello.py</files>
  <read_first>
    - app/integrations/pipedrive.py (wrapper structure analog; module docstring, _client, get_with_retry usage)
    - app/integrations/base.py (get_with_retry signature)
    - app/config.py (settings.TRELLO_API_KEY and TRELLO_TOKEN)
    - .planning/phases/04-trello-integration-collection-automation/04-RESEARCH.md (Pattern 2; Custom Fields Not Available finding)
  </read_first>
  <action>
    Create app/integrations/trello.py with: module constant BASE_URL set to https://api.trello.com/1; a security docstring mirroring pipedrive.py T-02-01 but stating never log repr of response.request because Trello query params carry key and token secrets; the hardcoded LIST_STATE_MAP dict mapping the 6 list IDs to states exactly as listed in the interfaces block above (Otros pendientes id 6996256c42ccdae7f69e4814 intentionally ABSENT = ignored); a CONTRATO_LIST_ID constant set to 69312ac640ae158381706ff8 (entry list for new cards per D-46); def _client returning httpx.Client with base_url BASE_URL and timeout 30.0 and NO auth header; def _auth_params returning a dict with key from settings.TRELLO_API_KEY and token from settings.TRELLO_TOKEN; def get_list_cards(client, list_id) returning a list of dict by calling get_with_retry on path /lists/{list_id}/cards with params combining _auth_params and fields id,name,due,desc then returning resp.json; def create_card(client, list_id, name, desc empty default, due None default) that POSTs to /cards with params combining _auth_params, idList list_id, name, and optional desc and due, then calls resp.raise_for_status and returns resp.json. Do NOT add any customFields code path because the free plan has Custom Fields disabled (RESEARCH.md confirmed). Import get_with_retry from app.integrations.base and settings from app.config.
  </action>
  <verify>
    <automated>cd /Users/alexiszamoraia/Developer/projects/lumixia/clients/santillan-ent/seg-dashboard && python3 -c "from app.integrations import trello; assert trello.LIST_STATE_MAP['69312ad63829ef3ac9967d1a']=='cobranza'; assert '6996256c42ccdae7f69e4814' not in trello.LIST_STATE_MAP; assert trello.CONTRATO_LIST_ID=='69312ac640ae158381706ff8'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - app/integrations/trello.py contains LIST_STATE_MAP with exactly 6 entries and no entry for key 6996256c42ccdae7f69e4814
    - app/integrations/trello.py contains "def get_list_cards(" and "def create_card("
    - grep finds the import "from app.integrations.base import get_with_retry" in the file
    - grep -v '^#' app/integrations/trello.py piped to grep -c customFields returns 0
    - The python import assertion above exits 0
  </acceptance_criteria>
  <done>trello.py exposes get_list_cards, create_card, LIST_STATE_MAP (6 states), CONTRATO_LIST_ID; auth via query params; no custom-field code.</done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 test stubs and conftest fixtures</name>
  <files>tests/test_trello.py, tests/conftest.py</files>
  <read_first>
    - tests/test_leads.py (test class naming, monkeypatch transport, source-isolated concurrency test analog)
    - tests/conftest.py (mock_pipedrive_transport fixture around lines 477-506, seed_deals fixture, _clean_tables autouse)
    - .planning/phases/04-trello-integration-collection-automation/04-VALIDATION.md (the 12-test map)
  </read_first>
  <action>
    Create tests/test_trello.py with a module docstring "Tests for the Trello integration (Phase 4)." and a Test Map listing the functions. Define these test functions named exactly per VALIDATION.md so later waves turn them green: test_sync_trello_upserts_cards, test_sync_trello_ignores_otros_pendientes, test_list_state_mapping, test_collection_date_from_due, test_collection_date_fallback, test_auto_create_card_for_won_deal, test_no_duplicate_card_creation, test_card_desc_contains_deal_id, test_income_projection_math, test_sync_trello_concurrency_guard, plus test_sync_trello_source_filter_isolated. Implement test_list_state_mapping fully now (it only needs trello.LIST_STATE_MAP which exists after Task 2; assert each of the 6 ids maps to its expected state and the Otros pendientes id is absent). For the other 10, write the arrange-act-assert intent in a docstring and a single pytest.skip call with reason "Wave 2/3/4: implemented when sync_trello / trello_service lands" so the file imports and collects cleanly (pending, not erroring). In tests/conftest.py add a mock_trello_transport fixture (mirror mock_pipedrive_transport using httpx.MockTransport; handler matches GET /lists/{id}/cards returning a small card list and POST /cards returning a created-card dict; ignore key and token query params) and a seed_trello_cards fixture (mirror seed_leads; insert TrelloCard rows linked to seed_deals deals). Do NOT modify the existing 111 tests.
  </action>
  <verify>
    <automated>cd /Users/alexiszamoraia/Developer/projects/lumixia/clients/santillan-ent/seg-dashboard && python3 -m pytest tests/test_trello.py -q && python3 -m pytest tests/test_trello.py -k list_state_mapping -x -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_trello.py collects without ImportError (pytest --collect-only -q exits 0 and lists at least 11 test functions)
    - python3 -m pytest tests/test_trello.py -k list_state_mapping -x -q passes (1 passed)
    - grep -c 'def test_' tests/test_trello.py returns at least 11
    - tests/conftest.py contains "def mock_trello_transport" and "def seed_trello_cards"
    - python3 -m pytest -q still passes the prior 111 tests plus the new ones (no existing test broken)
  </acceptance_criteria>
  <done>tests/test_trello.py collects cleanly; test_list_state_mapping passes; the other 10 skip with a Wave-N message; conftest has mock_trello_transport and seed_trello_cards; existing 111 tests untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app to Trello REST API | Outbound HTTP with key and token in query params; untrusted card data returns |
| Trello card data to SQLite | External strings (name, desc) persisted into trello_cards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | trello.py request logging | mitigate | Module docstring forbids logging repr(response.request); query params carry key and token secrets; only str(exc) may be persisted by callers |
| T-04-02 | Tampering | trello_card_id natural key | mitigate | trello_card_id is unique-indexed; upsert (Wave 2) is by this key so replayed card data cannot create duplicate rows |
| T-04-03 | Spoofing | Trello API endpoint | accept | BASE_URL is a static constant; list IDs are hardcoded from the env-verified board; no user-supplied URL so SSRF is not reachable |
| T-04-SC | Tampering | pip installs | accept | Phase 4 installs zero new packages (RESEARCH.md Package Legitimacy Audit not applicable); no supply-chain surface |
</threat_model>

<verification>
- alembic upgrade head succeeds and trello_cards table exists with correct columns and indexes
- python3 -c "from app.integrations import trello" imports cleanly with LIST_STATE_MAP (6 entries) and CONTRATO_LIST_ID
- python3 -m pytest tests/test_trello.py -q collects at least 11 tests; list_state_mapping passes, others skip
- python3 -m pytest -q full suite green (no regression to the existing 111 tests)
</verification>

<success_criteria>
- TrelloCard model and migration applied; trello_cards table queryable
- trello.py wrapper exposes get_list_cards and create_card with query-param auth and no custom-field code path
- tests/test_trello.py and conftest fixtures present and collecting; test_list_state_mapping green
</success_criteria>

<output>
Create .planning/phases/04-trello-integration-collection-automation/04-01-SUMMARY.md when done
</output>
