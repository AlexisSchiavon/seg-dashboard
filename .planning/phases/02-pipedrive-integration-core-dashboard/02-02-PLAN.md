---
phase: 02-pipedrive-integration-core-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/services/kpis.py
  - app/services/funnel.py
  - app/schemas/dashboard.py
  - app/routers/dashboard.py
  - app/main.py
  - frontend/index.html
  - frontend/css/styles.css
  - frontend/js/dashboard.js
  - tests/test_kpis.py
  - tests/test_funnel.py
  - tests/test_dashboard.py
  - tests/conftest.py
autonomous: false
requirements: [DASH-01, DASH-03]

must_haves:
  truths:
    - "After a sync, the Resumen tab shows Pipeline total, En negociación, Cerrados, En campaña KPIs computed from real Deal rows"
    - "The Resumen tab shows a talent ranking by revenue with a 'Sin talento asignado' row included in the global total (D-17/Pitfall 4)"
    - "The Resumen tab shows a recent-activity feed sourced from DealStageEvent ordered by detected_at desc (DASH-01)"
    - "The Funnel tab shows all 6 stages (Llamada → Cotización → Negociación → Contrato → En ejecución → Cobranza) with deal count and amount per stage"
    - "The Funnel tab shows a bottleneck alert (lowest stage-to-stage conversion) or 'Datos insuficientes para detectar cuellos de botella' when sample < 10 (DASH-03)"
    - "Before any successful sync, both tabs show the empty state 'Aún no hay datos de Pipedrive'"
    - "Sum of all per-talent ranking revenue + 'Sin talento asignado' equals the global Pipeline total KPI (no undercount)"
  artifacts:
    - path: "app/services/kpis.py"
      provides: "global_kpis(db), talent_ranking(db) including Sin-talento bucket"
      contains: "def global_kpis"
    - path: "app/services/funnel.py"
      provides: "funnel_overview(db) with 6-stage aggregation + bottleneck, recent_activity(db)"
      contains: "def funnel_overview"
    - path: "app/schemas/dashboard.py"
      provides: "DashboardSummary, FunnelOverview, RankingRow, ActivityItem, BottleneckInfo schemas"
      contains: "class DashboardSummary"
    - path: "app/routers/dashboard.py"
      provides: "GET /dashboard/summary + GET /dashboard/funnel, auth-protected"
      exports: ["router"]
    - path: "frontend/js/dashboard.js"
      provides: "loadSummary(), loadFunnel(), render functions for KPIs/ranking/activity/funnel"
      contains: "loadSummary"
  key_links:
    - from: "frontend/js/dashboard.js"
      to: "/dashboard/summary"
      via: "apiFetch GET on Resumen tab activation"
      pattern: "dashboard/summary"
    - from: "frontend/js/dashboard.js"
      to: "/dashboard/funnel"
      via: "apiFetch GET on Funnel tab activation"
      pattern: "dashboard/funnel"
    - from: "app/services/kpis.py"
      to: "Deal.talent_id IS NULL"
      via: "Sin-talento bucket query (no INNER JOIN drops null rows)"
      pattern: "talent_id.is_"
    - from: "app/routers/dashboard.py"
      to: "app/services/funnel.py"
      via: "funnel_overview / recent_activity delegation"
      pattern: "funnel_overview"
---

<objective>
Build the global dashboard read-layer and render the Resumen and Funnel tabs with real data computed from the Deal/DealStageEvent tables that Plan 02-01 populates. This is a vertical slice: SQLite (existing Deal/DealStageEvent) → services/kpis.py + services/funnel.py → schemas/dashboard.py + /dashboard/summary + /dashboard/funnel endpoints → frontend Resumen + Funnel tab rendering.

Purpose: DASH-01 (Resumen: global KPIs, talent ranking, recent activity feed) and DASH-03 (Funnel completo: 6 stages with count/amount + bottleneck detection) become live. After a sync, the user sees real funnel and revenue data instead of an empty shell.
Output: New `app/services/kpis.py` + `app/services/funnel.py` (read aggregates), `app/schemas/dashboard.py` (Pydantic response models), `app/routers/dashboard.py` (summary + funnel endpoints, wired into main.py), Resumen + Funnel tab rendering in `frontend/index.html` / `frontend/js/dashboard.js`, ported CSS component classes, and tests for kpis/funnel/dashboard.
</objective>

<execution_context>
@/Users/alexiszamoraia/Developer/projects/lumixia/clients/santillan-ent/seg-dashboard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexiszamoraia/Developer/projects/lumixia/clients/santillan-ent/seg-dashboard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pipedrive-integration-core-dashboard/02-RESEARCH.md
@.planning/phases/02-pipedrive-integration-core-dashboard/02-PATTERNS.md
@.planning/phases/02-pipedrive-integration-core-dashboard/02-UI-SPEC.md
@.planning/phases/02-pipedrive-integration-core-dashboard/02-VALIDATION.md
@.planning/phases/02-pipedrive-integration-core-dashboard/02-01-PLAN.md

<interfaces>
<!-- Contracts established by Plan 02-01 + Phase 1 that this plan builds against. -->

From app/models.py (created in Plan 02-01 — read directly, do NOT redefine):
- Deal(id, pipedrive_id, title, value: float, currency, stage_id: int, stage_name: str, status: str ["open"/"won"/"lost"], talent_id: int|None FK->talents.id, commission_amount: float, is_sin_cotizar: bool, loss_reason: str|None, brand_category: str|None, expected_collection_date: str|None, stage_entered_at: datetime|None, update_time: str, add_time: str|None)
- DealStageEvent(id, deal_pipedrive_id: int, talent_id: int|None, from_stage: str|None, to_stage: str, from_status: str|None, to_status: str, detected_at: datetime)
- SyncLog(id, source, started_at, finished_at, status, records_synced, error_message)
- Talent(id, name, active, category, photo_url, products)

From app/database.py:
- def get_db()  # FastAPI Depends() session yielding Session
- from sqlalchemy import func  # use for SUM/COUNT aggregates (already imported in models.py)

From app/auth/dependencies.py:
- def get_current_user(...)  # apply via APIRouter(dependencies=[Depends(get_current_user)])

From app/routers/talents.py (analog for router/auth/404 conventions — read, do not modify):
- router = APIRouter(prefix="/talents", tags=["talents"], dependencies=[Depends(get_current_user)])
- 404 pattern: talent = db.get(Talent, id); if talent is None: raise HTTPException(404, "Talent not found")

From app/schemas/talent.py (Pydantic v2 convention):
- model_config = {"from_attributes": True} on ORM-backed reads; plain BaseModel for computed aggregates; field: T | None = None for optionals

From frontend/js/auth.js:
- async function apiFetch(url, options)  # 401->/login.html interceptor; reuse, do not reimplement

From frontend/js/dashboard.js (created in Plan 02-01 — EXTEND, do not rewrite):
- setPage(name) tab switcher, loadSyncStatus(), triggerSync(), showToast() already exist — add loadSummary()/loadFunnel() and wire them into setPage

From app/main.py (Plan 02-01 already added lifespan + included sync.router; static mount is LAST):
- This plan adds app.include_router(dashboard.router) BEFORE the static mount line

The 6 funnel stages in order (PIPE-05, PROJECT.md): Llamada -> Cotización -> Negociación -> Contrato -> En ejecución -> Cobranza.
The 5 razones de pérdida and 6 categorías de marca are listed in 02-CONTEXT.md <specifics> (used in Plan 02-03, not here).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Global KPI + funnel + activity services with Pydantic schemas</name>
  <files>app/services/kpis.py, app/services/funnel.py, app/schemas/dashboard.py, tests/conftest.py, tests/test_kpis.py, tests/test_funnel.py</files>
  <read_first>
    - app/models.py (Deal/DealStageEvent/Talent column names — created by Plan 02-01)
    - app/routers/talents.py (Session query style: db.query(...).filter(...).all(); func usage)
    - app/schemas/talent.py (full file — Pydantic v2 model_config convention)
    - .planning/phases/02-pipedrive-integration-core-dashboard/02-PATTERNS.md (services/kpis.py + services/funnel.py + schemas/dashboard.py sections — Pitfall 4 query split, bottleneck Pattern 4)
    - .planning/phases/02-pipedrive-integration-core-dashboard/02-RESEARCH.md (Pattern 4 bottleneck heuristic; Pitfall 4 global-vs-per-talent; DASH-01/DASH-03 in Phase Requirements)
    - .planning/phases/02-pipedrive-integration-core-dashboard/02-UI-SPEC.md (Copywriting Contract: KPI labels, bottleneck copy, empty states)
    - tests/conftest.py (full file — db_session, client, auth_client fixtures; add seed_deals fixture here)
  </read_first>
  <behavior>
    - Test (test_kpis::test_commission_calculation): global_kpis computes Pipeline total = SUM(Deal.value) over ALL deals from stored values (PIPE-02 commission already stored at sync time); returns numeric tiles.
    - Test (test_kpis::test_global_includes_sin_talento): a Deal with talent_id=None is INCLUDED in global Pipeline total but excluded from any per-talent figure; sum of ranking rows' revenue + Sin-talento bucket revenue == global Pipeline total (Pitfall 4).
    - Test (test_kpis::test_ranking_has_sin_talento_row): talent_ranking returns a row labeled "Sin talento asignado" sourced from Deal.talent_id IS NULL when such deals exist, and omits it (or returns 0-count) when none exist.
    - Test (test_funnel::test_stage_mapping): funnel_overview returns exactly the 6 stages in order Llamada -> Cotización -> Negociación -> Contrato -> En ejecución -> Cobranza, each with count and amount (PIPE-05); stages with no deals show count 0 (not omitted).
    - Test (test_funnel::test_bottleneck_detection): with >=10 deals spread across stages, funnel_overview flags the adjacent stage pair with the lowest conversion ratio; with <10 total deals, bottleneck is None and insufficient_data is True.
    - Test (test_funnel::test_recent_activity_order): recent_activity returns DealStageEvent rows ordered by detected_at desc, limited to N (e.g. 20), each carrying deal title + to_stage + detected_at.
  </behavior>
  <action>
    Add a seed_deals fixture to tests/conftest.py (do not break existing fixtures): creates a couple of Talent rows, several Deal rows with known value/stage_name/status/talent_id (including at least one talent_id=None deal and one is_sin_cotizar=True $0 deal), and a few DealStageEvent rows with staggered detected_at. Reuse the existing db_session/StaticPool engine.

    Create app/services/kpis.py with plain functions taking db: Session as first param (NOT Depends-wired — per PATTERNS.md, Depends belongs in the router):
    - global_kpis(db) -> dict: Pipeline total = db.query(func.coalesce(func.sum(Deal.value), 0.0)).scalar() over ALL deals (NO join to Talent, so talent_id IS NULL rows count — Pitfall 4); "En negociación" = count/sum where stage_name == "Negociación" and status == "open"; "Cerrados" = count/sum where status == "won"; "En campaña" = count/sum where stage_name == "En ejecución" and status == "open". Return the 4 KPI tiles matching UI-SPEC labels (Pipeline total, En negociación, Cerrados, En campaña). Do NOT compute "Leads totales"/"Calificados" (no Phase 2 data source — UI-SPEC line 117 drops these tiles).
    - talent_ranking(db) -> list: db.query(Talent.id, Talent.name, Talent.category, func.coalesce(func.sum(Deal.value), 0.0), func.count(Deal.id)).outerjoin(Deal, Deal.talent_id == Talent.id).group_by(Talent.id).order_by(...desc()), PLUS a separate query for the "Sin talento asignado" bucket: db.query(func.count(Deal.id), func.coalesce(func.sum(Deal.value), 0.0)).filter(Deal.talent_id.is_(None)) (D-17). Append the Sin-talento bucket as the last ranking entry only when its count > 0, flagged so the frontend renders it without a medal.

    Create app/services/funnel.py with plain db: Session-first functions:
    - STAGES = ["Llamada", "Cotización", "Negociación", "Contrato", "En ejecución", "Cobranza"] module constant (PIPE-05 canonical order).
    - funnel_overview(db) -> dict: for each of the 6 STAGES compute count + amount via db.query(func.count(Deal.id), func.coalesce(func.sum(Deal.value),0.0)).filter(Deal.stage_name == stage).filter(Deal.status == "open"), always emitting all 6 stages in order (count 0 when none). Then compute bottleneck per RESEARCH.md Pattern 4: for each adjacent pair compute conversion_i = count(deals currently at stage >= i+1, status in ('open','won')) / count(deals currently at stage >= i, status in ('open','won','lost')); flag the lowest-conversion pair as {stage_a, stage_b, conversion_pct}. If total deal count < 10, return bottleneck=None with insufficient_data=True. Map stage_name -> order index via STAGES.index for the ">= i" comparisons.
    - recent_activity(db, limit=20) -> list: db.query(DealStageEvent).order_by(DealStageEvent.detected_at.desc()).limit(limit).all(), looking up the Deal title and Talent name for display; return items with deal title, to_stage, talent name (or "Sin talento"), detected_at.

    Create app/schemas/dashboard.py (Pydantic v2, plain BaseModel for computed aggregates per PATTERNS.md):
    - KpiTile(BaseModel): label: str, value: float, count: int | None = None, variant: str (accent/amber/green/purple).
    - RankingRow(BaseModel): talent_id: int | None, name: str, category: str | None = None, revenue: float, deal_count: int, is_sin_talento: bool = False.
    - ActivityItem(BaseModel): title: str, to_stage: str, talent_name: str, detected_at: datetime.
    - BottleneckInfo(BaseModel): stage_a: str, stage_b: str, conversion_pct: float.
    - StageBucket(BaseModel): stage: str, count: int, amount: float.
    - DashboardSummary(BaseModel): kpis: list[KpiTile], ranking: list[RankingRow], activity: list[ActivityItem], has_data: bool.
    - FunnelOverview(BaseModel): stages: list[StageBucket], bottleneck: BottleneckInfo | None = None, insufficient_data: bool = False, has_data: bool.

    Write the behavior tests above into tests/test_kpis.py and tests/test_funnel.py using the seed_deals fixture.
  </action>
  <verify>
    <automated>uv run pytest tests/test_kpis.py tests/test_funnel.py -x</automated>
  </verify>
  <acceptance_criteria>
    - app/services/kpis.py contains "def global_kpis" and "def talent_ranking"
    - app/services/kpis.py contains the literal "talent_id.is_(None)" (Sin-talento bucket query)
    - app/services/funnel.py contains "def funnel_overview", "def recent_activity", and a STAGES list with the 6 stage names in order
    - app/schemas/dashboard.py contains "class DashboardSummary" and "class FunnelOverview"
    - tests/conftest.py defines a seed_deals fixture creating Deal rows including one with talent_id=None
    - Test test_global_includes_sin_talento asserts ranking-revenue-sum + Sin-talento bucket == global Pipeline total — passes
    - Test test_stage_mapping asserts all 6 stages returned in canonical order — passes
    - Test test_bottleneck_detection asserts bottleneck is None + insufficient_data=True when <10 deals — passes
    - uv run pytest tests/test_kpis.py tests/test_funnel.py -x exits 0
  </acceptance_criteria>
  <done>Global KPI/ranking/funnel/bottleneck/activity services exist with correct Pitfall-4 query split and Pattern-4 bottleneck heuristic, backed by Pydantic response schemas and passing unit tests against seeded deals.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard router (/summary + /funnel) wired into main.py</name>
  <files>app/routers/dashboard.py, app/main.py, tests/test_dashboard.py</files>
  <read_first>
    - app/routers/talents.py (full file — APIRouter(dependencies=[Depends(get_current_user)]) + read-endpoint pattern + 404)
    - app/main.py (current router include order — Plan 02-01 added lifespan + sync.router; static mount MUST stay LAST)
    - app/services/kpis.py, app/services/funnel.py (functions from Task 1)
    - app/schemas/dashboard.py (response models from Task 1)
    - tests/conftest.py (client unauth fixture, auth_client, seed_deals from Task 1)
    - tests/test_talents.py (test_talents_require_auth pattern for the 401 assertion)
  </read_first>
  <action>
    Create app/routers/dashboard.py: router = APIRouter(prefix="/dashboard", tags=["dashboard"], dependencies=[Depends(get_current_user)]) (per PATTERNS.md Shared Auth pattern — identical to talents.py).
    - GET /dashboard/summary (response_model=DashboardSummary): delegate to kpis.global_kpis(db), kpis.talent_ranking(db), funnel.recent_activity(db); set has_data False when no successful SyncLog exists yet OR Deal count is 0 (drives the frontend empty state "Aún no hay datos de Pipedrive"). Assemble KpiTile list with the UI-SPEC variants: Pipeline total -> accent, En negociación -> amber, Cerrados -> purple, En campaña -> green.
    - GET /dashboard/funnel (response_model=FunnelOverview): delegate to funnel.funnel_overview(db); set has_data False when Deal count is 0.
    - Each endpoint signature: def get_summary(db: Session = Depends(get_db)). No path params, no 404 needed for these two (per-talent 404 lands in Plan 02-03).
    Edit app/main.py additively: add "from app.routers import dashboard" to the existing routers import and app.include_router(dashboard.router) AFTER app.include_router(sync.router) and BEFORE the app.mount("/", StaticFiles...) line (preserve the existing "static mount MUST come last" comment and ordering).
    Add to tests/test_dashboard.py: test_summary_requires_auth(client) asserts GET /dashboard/summary returns 401 unauthenticated; test_funnel_requires_auth(client) asserts 401; test_summary_endpoint(auth_client, seed_deals) asserts 200 + DashboardSummary shape (kpis is a list of 4 tiles with expected labels, ranking present, activity present, has_data True); test_funnel_endpoint(auth_client, seed_deals) asserts 200 + 6 stages in order + bottleneck/insufficient_data field present; test_summary_empty_state(auth_client) (no seeded deals) asserts has_data False.
  </action>
  <verify>
    <automated>uv run pytest tests/test_dashboard.py -x && uv run python -c "from app.main import app; print('app boots')"</automated>
  </verify>
  <acceptance_criteria>
    - app/routers/dashboard.py defines router with dependencies=[Depends(get_current_user)], a GET route at /summary, and a GET route at /funnel
    - app/routers/dashboard.py summary endpoint uses response_model=DashboardSummary; funnel endpoint uses response_model=FunnelOverview
    - app/main.py contains app.include_router(dashboard.router) and the app.mount("/", StaticFiles... line is still the LAST registration
    - test_summary_requires_auth returns 401; test_funnel_requires_auth returns 401
    - test_summary_endpoint returns 200 with 4 KPI tiles; test_summary_empty_state returns has_data False
    - test_funnel_endpoint returns 200 with exactly 6 stages in canonical order
    - uv run python -c "from app.main import app" exits 0
  </acceptance_criteria>
  <done>Auth-protected /dashboard/summary and /dashboard/funnel endpoints return validated DashboardSummary/FunnelOverview shapes from the Task 1 services, wired into main.py without breaking the static-mount-last ordering; endpoint contract tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Render Resumen + Funnel tabs (HTML/CSS/JS)</name>
  <files>frontend/index.html, frontend/css/styles.css, frontend/js/dashboard.js</files>
  <read_first>
    - frontend/index.html (Plan 02-01 shell — nav/tabbar/live-pill/Sincronizar-ahora button; EXTEND the Resumen + Funnel tab containers, do not rewrite the shell)
    - frontend/js/dashboard.js (Plan 02-01 — setPage/loadSyncStatus/triggerSync/showToast already exist; ADD loadSummary/loadFunnel + render fns and wire into setPage)
    - frontend/js/auth.js (apiFetch wrapper — reuse verbatim, do not redefine)
    - frontend/css/styles.css (existing :root tokens + .card padding from Plan 02-01)
    - .planning/phases/02-pipedrive-integration-core-dashboard/02-PATTERNS.md (frontend index.html / dashboard.js / styles.css sections — exact markup + the class list to port; UI-SPEC deviations)
    - .planning/phases/02-pipedrive-integration-core-dashboard/02-UI-SPEC.md (Copywriting Contract: KPI labels, bottleneck copy, empty states; Typography kpi-val weight 500; Spacing .card 16px / .kpi 12px 16px; Color: Pipeline total = accent, only one accent tile)
    - .planning/reference/mockup.html (kpi-grid / rank-row / funnel-row / activity-row / alert markup to port)
  </read_first>
  <action>
    Extend frontend/css/styles.css: port the component classes from PATTERNS.md styles.css section that are not already present from Plan 02-01 — .kpi-grid, .kpi, .kpi-val, .kpi-label, .kpi-sub, .rank-row, .rank-num, .rank-avatar, .rank-info, .rank-name, .rank-nicho, .funnel-row, .f-track, .f-fill, .f-label, .f-n, .activity-row, .act-icon, .act-text, .act-main, .act-time, .alert, .alert.warn, .alert.info, .alert-icon, .alert-text, .section, .section-title. Apply UI-SPEC deviations: .kpi-val font-weight 500 (NOT 600); .kpi padding 12px 16px; ensure .card padding 16px. Reuse existing :root var names (additive, identical names). Do not add a parallel .card/.kpi variant.

    Extend frontend/index.html: inside the Resumen tab container add (in order) — the existing "Sincronizar ahora" button (from 02-01), a KPI grid (.kpi-grid with 4 .kpi tiles: Pipeline total as .kpi.accent/.kpi-val.accent, En negociación amber, Cerrados purple, En campaña green), a "Ranking de talentos" section (.section + .section-title + .card containing .rank-row rows), and an "Actividad reciente" section (.section + .card containing .activity-row rows). Do NOT add "Leads totales"/"Calificados" KPI tiles or an "Insights IA" section (UI-SPEC line 117 — hidden entirely in Phase 2). Inside the Funnel tab container add a "Funnel completo" .section with a .card of 6 .funnel-row rows and an .alert slot for the bottleneck. Give each dynamic container a stable id (e.g. kpi-grid, ranking-list, activity-list, funnel-rows, bottleneck-slot) for JS targeting.

    Extend frontend/js/dashboard.js (reuse existing apiFetch/showToast/setPage):
    - loadSummary(): apiFetch("/dashboard/summary"); if !res return (401 redirected); parse JSON; if !data.has_data render the empty state "Aún no hay datos de Pipedrive" / body "Ejecuta la primera sincronización para ver el funnel, el ranking de talentos y las KPIs. Usa el botón 'Sincronizar ahora' arriba." into the Resumen container and return; else renderKpis(data.kpis) (each tile: label, value formatted as MXN currency, variant class), renderRanking(data.ranking) (medal classes gold/silver/bronze for ranks 1-3; for is_sin_talento rows use a dash numeral + --bg5 avatar with ? glyph + label "Sin talento asignado"), renderActivity(data.activity) (act-main "<brand> — <talent> pasó a <to_stage>", act-time relative "Hace X..." + "· Pipedrive").
    - loadFunnel(): apiFetch("/dashboard/funnel"); if !res return; if !data.has_data render the empty state; else render 6 .funnel-row (f-label = stage name, f-fill width = pct of max-count, f-n = count). Render bottleneck: if data.insufficient_data -> .alert.info "Datos insuficientes para detectar cuellos de botella"; else if data.bottleneck -> .alert.warn "Cuello de botella detectado: solo el {conversion_pct}% de los deals en {stage_a} avanzan a {stage_b}." Use EXACT copy from UI-SPEC Copywriting Contract — never the words "industria"/"promedio de la industria".
    - Wire into the existing setPage(name): call loadSummary() when the Resumen tab is shown and loadFunnel() when the Funnel tab is shown (call loadSummary once on initial page load too). Reuse the existing triggerSync completion path to re-call loadSummary/loadFunnel after a sync completes so KPIs refresh.
  </action>
  <verify>
    <automated>grep -q "dashboard/summary" frontend/js/dashboard.js && grep -q "dashboard/funnel" frontend/js/dashboard.js && grep -q "Aún no hay datos de Pipedrive" frontend/js/dashboard.js && grep -vE '^[[:space:]]*(/\*|\*|//)' frontend/css/styles.css | grep -Eq "kpi-val[^}]*font-weight:[[:space:]]*500"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/css/styles.css defines .kpi-grid, .kpi-val, .funnel-row, .f-fill, .activity-row, .alert classes
    - frontend/css/styles.css .kpi-val uses font-weight 500 (NOT 600) when comments are stripped; .card padding is 16px
    - frontend/index.html Resumen container has a .kpi-grid with exactly 4 .kpi tiles and the single accent tile is Pipeline total; no "Leads totales"/"Calificados"/"Insights IA" sections present
    - frontend/index.html Funnel container has 6 .funnel-row slots and a bottleneck .alert slot
    - frontend/js/dashboard.js defines loadSummary and loadFunnel, calls apiFetch("/dashboard/summary") and "/dashboard/funnel", and contains the empty-state copy "Aún no hay datos de Pipedrive"
    - frontend/js/dashboard.js bottleneck copy contains "Cuello de botella detectado" and "Datos insuficientes para detectar cuellos de botella" and contains neither "industria" nor "promedio de la industria"
    - setPage triggers loadSummary on the Resumen tab and loadFunnel on the Funnel tab
  </acceptance_criteria>
  <done>Resumen tab renders live KPIs (Pipeline total accent + 3 semantic tiles), talent ranking with Sin-talento row, and the activity feed; Funnel tab renders all 6 stages plus the bottleneck/insufficient-data alert; both show the empty state before first sync; CSS honors the UI-SPEC weight/padding deviations.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The Resumen and Funnel dashboard tabs rendering real synced Pipedrive data: global KPIs (Pipeline total + En negociación/Cerrados/En campaña), talent ranking with the "Sin talento asignado" bucket, the recent-activity feed from DealStageEvent, and the full 6-stage funnel with bottleneck detection. This checkpoint confirms the rendered tabs match the UI-SPEC visual contract and that the numbers are consistent.</what-built>
  <how-to-verify>
    1. Start the app: uv run uvicorn app.main:app --reload. Log in and open the dashboard at /.
    2. If no deals are synced yet, confirm both Resumen and Funnel show the empty state "Aún no hay datos de Pipedrive" with the "Ejecuta la primera sincronización..." body. Then click "Sincronizar ahora" (from Plan 02-01) and wait for completion.
    3. Resumen tab: confirm 4 KPI tiles render — Pipeline total in ORANGE (accent), the other three in amber/purple/green. Confirm only Pipeline total uses accent. Confirm the talent ranking lists talents by revenue with gold/silver/bronze on the top 3, and a "Sin talento asignado" row (dash numeral, ? avatar) appears LAST if any unmapped-product deals exist.
    4. Sanity-check the math: the sum of all ranking-row revenues plus the "Sin talento asignado" row should equal the Pipeline total KPI (Pitfall 4 — no undercount).
    5. Confirm the "Actividad reciente" feed shows recent stage-change events (e.g. "<marca> — <talento> pasó a <etapa>", "Hace X · Pipedrive"). On a brand-new database this may be empty (first sync creates no events per Pitfall 3) — that is expected.
    6. Funnel tab: confirm all 6 stages render in order (Llamada → Cotización → Negociación → Contrato → En ejecución → Cobranza) with counts. Confirm the bottleneck alert shows either "Cuello de botella detectado: solo el X% de los deals en {EtapaA} avanzan a {EtapaB}" (amber) OR "Datos insuficientes para detectar cuellos de botella" (blue info) if fewer than 10 deals. Confirm NO "industria"/"promedio de la industria" text appears anywhere.
    7. Confirm KPI values use DM Mono at weight 500 (not bold 600) and cards use comfortable 16px padding per the UI-SPEC.
  </how-to-verify>
  <resume-signal>Type "approved" once the Resumen and Funnel tabs render correct live data (KPIs add up, single accent tile, 6 stages in order, bottleneck copy without "industria"), or describe issues (wrong totals, missing Sin-talento row, wrong stage order, wrong colors/weights) to fix before proceeding to Plan 02-03.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ /dashboard/summary, /dashboard/funnel | Authenticated user reads aggregated revenue/funnel data; JWT cookie crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02B-01 | Information Disclosure | Unauthenticated access to /dashboard/* exposing revenue data | mitigate | app/routers/dashboard.py uses dependencies=[Depends(get_current_user)] (ASVS V4); test_summary_requires_auth + test_funnel_requires_auth assert 401 without a session cookie. |
| T-02B-02 | Tampering | Malformed service output bypasses response contract | mitigate | All endpoints declare response_model (DashboardSummary/FunnelOverview); Pydantic validates shape at the boundary (ASVS V5). |
| T-02B-03 | Information Disclosure | "Sin talento asignado" deals leak via an unexpected per-talent path | accept | This plan exposes only global aggregates; per-talent endpoint lands in Plan 02-03. talent_id IS NULL rows are aggregated into a labeled bucket, not individually exposed here. Low risk, single-tenant internal tool. |
| T-02B-04 | Denial of Service | Unbounded activity feed / ranking query on large Deal table | mitigate | recent_activity uses an explicit limit (e.g. 20); ranking is grouped/aggregated. At 21-talent scale deal volume is far below any concern; no pagination needed in Phase 2. |

No new package installs in this plan (apscheduler/rapidfuzz already added in Plan 02-01) — no T-02-SC supply-chain checkpoint required.
</threat_model>

<verification>
- uv run pytest tests/test_kpis.py tests/test_funnel.py tests/test_dashboard.py -x green.
- uv run pytest full suite green (no regression in Plan 02-01 sync tests).
- App boots with dashboard.router included; /dashboard/summary and /dashboard/funnel are auth-protected (401 without cookie, 200 with).
- Human checkpoint: Resumen + Funnel tabs render correct live data with the UI-SPEC visual contract.
</verification>

<success_criteria>
- DASH-01: Resumen tab shows global KPIs, talent ranking (with Sin-talento bucket in global total), and an activity feed from real Pipedrive-synced data.
- DASH-03: Funnel tab shows all 6 stages with count/amount and bottleneck detection (or insufficient-data fallback).
- Pitfall 4 honored: global totals include talent_id IS NULL deals; per-talent figures exclude them; the two reconcile.
- Empty state shown before the first successful sync; UI-SPEC copy/typography/spacing/color contract honored.
</success_criteria>

<output>
Create `.planning/phases/02-pipedrive-integration-core-dashboard/02-02-SUMMARY.md` when done.
</output>
