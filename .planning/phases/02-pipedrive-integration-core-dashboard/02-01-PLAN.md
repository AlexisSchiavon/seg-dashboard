---
phase: 02-pipedrive-integration-core-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - app/models.py
  - alembic/versions/
  - app/integrations/base.py
  - app/integrations/pipedrive.py
  - app/sync/__init__.py
  - app/sync/jobs.py
  - app/sync/scheduler.py
  - app/scripts/match_talent_products.py
  - app/routers/sync.py
  - app/main.py
  - frontend/index.html
  - frontend/css/styles.css
  - frontend/js/dashboard.js
  - tests/conftest.py
  - tests/test_pipedrive_integration.py
  - tests/test_sync.py
autonomous: false
requirements: [PIPE-01, PIPE-02, PIPE-03, PIPE-04, PIPE-05]
user_setup:
  - service: pipedrive
    why: "Live deal sync requires a real Pipedrive API token + company domain"
    env_vars:
      - name: PIPEDRIVE_API_TOKEN
        source: "Pipedrive → Settings → Personal preferences → API → Your personal API token"
      - name: PIPEDRIVE_DOMAIN
        source: "Pipedrive company subdomain, e.g. `segroup` from segroup.pipedrive.com"

must_haves:
  truths:
    - "Clicking 'Sincronizar ahora' triggers an async Pipedrive sync that returns immediately (202)"
    - "After a successful sync, Deal rows exist in SQLite with stage, value, product→talent mapping, 70% commission, and custom fields"
    - "Deals valued at $0 MXN are flagged is_sin_cotizar=True"
    - "Deals whose product matches no talent are synced with talent_id=NULL (not dropped)"
    - "The 'Última sync: hace X min' indicator reflects the most recent successful SyncLog"
    - "A second sync request while one is running is a no-op (concurrency guard)"
  artifacts:
    - path: "app/models.py"
      provides: "Deal, DealStageEvent, SyncLog ORM models"
      contains: "class Deal"
    - path: "app/integrations/pipedrive.py"
      provides: "Pipedrive v2 client with x-api-token header, cursor pagination, field resolution"
      contains: "x-api-token"
    - path: "app/sync/jobs.py"
      provides: "sync_pipedrive(db) orchestration with commission/Sin-cotizar/event-diffing"
      contains: "def sync_pipedrive"
    - path: "app/routers/sync.py"
      provides: "POST /sync/pipedrive async trigger + GET sync status"
      exports: ["router"]
    - path: "app/scripts/match_talent_products.py"
      provides: "One-time rapidfuzz talent↔product auto-match + unmapped report"
      contains: "process.extractOne"
  key_links:
    - from: "frontend/js/dashboard.js"
      to: "/sync/pipedrive"
      via: "apiFetch POST on Sincronizar ahora click"
      pattern: "sync/pipedrive"
    - from: "app/sync/jobs.py"
      to: "talent_products.pipedrive_product_id"
      via: "product→talent resolution at sync write"
      pattern: "pipedrive_product_id"
    - from: "app/main.py"
      to: "app/sync/scheduler.py"
      via: "lifespan starts BackgroundScheduler"
      pattern: "lifespan"
---

<objective>
Deliver the foundational Pipedrive sync vertical slice: a user clicks "Sincronizar ahora", deal data flows from Pipedrive API v2 into local SQLite (stage, value, product→talent, 70% commission, Sin cotizar, custom fields, stage-change events), and the dashboard shell shows the "Última sync" indicator updating. Also runs an hourly background sync (APScheduler) and a one-time talent↔product auto-match script.

Purpose: Every dashboard tab in Plans 02/03 reads from the local Deal/DealStageEvent tables this slice populates. Without it, there is no real data to display.
Output: New `Deal`/`DealStageEvent`/`SyncLog` models + migration, `app/integrations/pipedrive.py` (v2 client), `app/sync/` (jobs + scheduler), `app/scripts/match_talent_products.py`, `app/routers/sync.py`, dashboard shell HTML/CSS/JS with sync button + last-sync indicator, and Wave 0 test scaffolding.
</objective>

<execution_context>
@/Users/alexiszamoraia/Developer/projects/lumixia/clients/santillan-ent/seg-dashboard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexiszamoraia/Developer/projects/lumixia/clients/santillan-ent/seg-dashboard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pipedrive-integration-core-dashboard/02-RESEARCH.md
@.planning/phases/02-pipedrive-integration-core-dashboard/02-PATTERNS.md
@.planning/phases/02-pipedrive-integration-core-dashboard/02-UI-SPEC.md
@.planning/phases/02-pipedrive-integration-core-dashboard/02-VALIDATION.md

<interfaces>
<!-- Existing Phase 1 contracts the executor must build against. -->

From app/config.py (already defines, no changes needed):
- settings.PIPEDRIVE_API_TOKEN: str   # empty-string default
- settings.PIPEDRIVE_DOMAIN: str      # empty-string default

From app/database.py:
- SessionLocal = sessionmaker(...)     # for background/script code: db = SessionLocal(); try/finally db.close()
- Base                                  # declarative base for new models
- def get_db()                          # FastAPI Depends() session

From app/models.py (existing tables — do NOT modify, only add):
- Talent(id, name, active, category, photo_url, products)
- TalentProduct(id, talent_id FK→talents.id, pipedrive_product_id: int|None)

From app/auth/dependencies.py:
- def get_current_user(...)             # apply via APIRouter(dependencies=[Depends(get_current_user)])

From frontend/js/auth.js:
- async function apiFetch(url, options) # 401→/login.html interceptor; reuse, do not reimplement

RESEARCH.md Pattern 3 is the canonical schema for Deal / DealStageEvent / SyncLog — follow column names exactly.
RESEARCH.md Pattern 1/2 are the canonical Pipedrive v2 client + field-resolution code.
PATTERNS.md maps every new file to its Phase 1 analog with verbatim conventions.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — install deps, models, migration, test scaffolding</name>
  <files>pyproject.toml, app/models.py, alembic/versions/, tests/conftest.py, tests/test_pipedrive_integration.py, tests/test_sync.py</files>
  <read_first>
    - app/models.py (existing Talent/TalentProduct conventions — add to this file, do not rewrite)
    - app/database.py (Base, SessionLocal)
    - alembic/versions/324116cbf0dd_initial_schema_users_talents_talent_.py (migration header/upgrade/downgrade pattern; current head revision id is 324116cbf0dd)
    - tests/conftest.py (full file — StaticPool test_engine, Base.metadata.create_all, db_session/client/auth_client fixtures)
    - .planning/phases/02-pipedrive-integration-core-dashboard/02-RESEARCH.md (Pattern 3 schema; Wave 0 Gaps)
    - .planning/phases/02-pipedrive-integration-core-dashboard/02-PATTERNS.md (app/models.py + migration + Test Fixtures sections)
  </read_first>
  <action>
    Run `uv add apscheduler rapidfuzz` (apscheduler==3.11.2, rapidfuzz==3.14.5 per RESEARCH.md; both audited [OK]) and add `respx` as a dev dependency for httpx mocking (`uv add --dev respx`).
    Add three models to app/models.py using SQLAlchemy 2.0 Mapped/mapped_column, importing Float and Integer in addition to existing imports. Follow RESEARCH.md Pattern 3 column names exactly:
    - Deal: id PK, pipedrive_id (unique, index), title, value (Float default 0.0), currency (default "MXN"), stage_id, stage_name, status (open/won/lost), talent_id (nullable FK→talents.id, NO cascade per D-17), commission_amount (Float default 0.0), is_sin_cotizar (Boolean default False, PIPE-03), loss_reason (nullable), brand_category (nullable), expected_collection_date (nullable String), stage_entered_at (nullable DateTime), update_time (String, for updated_since filter), add_time (nullable String).
    - DealStageEvent: id PK, deal_pipedrive_id (Integer index), talent_id (nullable FK), from_stage (nullable), to_stage, from_status (nullable), to_status, detected_at (DateTime server_default func.now()).
    - SyncLog: id PK, source (default "pipedrive"), started_at (DateTime), finished_at (nullable DateTime), status (running/success/error), records_synced (Integer default 0), error_message (nullable).
    Generate the Alembic migration: `uv run alembic revision --autogenerate -m "add deals, deal_stage_events, sync_logs"`, with down_revision pointing to 324116cbf0dd. Verify the generated upgrade() creates all three tables with the unique index on deals.pipedrive_id and index on deal_stage_events.deal_pipedrive_id; downgrade() drops in reverse order (sync_logs, deal_stage_events, deals).
    Add conftest.py fixtures (do not break existing ones): a `seed_talent_products` fixture creating a couple of Talent + TalentProduct rows with known pipedrive_product_id values, a `seed_deals` fixture creating Deal/DealStageEvent rows for service tests, and a `mock_pipedrive_transport` fixture returning an `httpx.MockTransport` (or respx router) serving recorded v2-shaped JSON (deals list with cursor pagination, dealFields with options arrays, deals/products bulk, stages). Use the exact v2 response shapes from RESEARCH.md Pattern 1/2 (nested custom_fields.<hash>.value, additional_data.next_cursor, options:[{id,label}]).
    Create stub test files tests/test_pipedrive_integration.py and tests/test_sync.py with the test function names referenced in VALIDATION.md (test_resolve_custom_fields, test_zero_value_deal_sin_cotizar, etc.) — write them as real failing/passing assertions where the implementation already exists from Tasks 2-3 of THIS plan, or leave xfail-marked stubs ONLY for behavior implemented later in this same plan. Do not leave permanently-skipped tests.
  </action>
  <verify>
    <automated>uv run python -c "from app.models import Deal, DealStageEvent, SyncLog; print('models ok')" && uv run alembic upgrade head && uv run python -c "import apscheduler, rapidfuzz; print('deps ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `app/models.py` contains `class Deal`, `class DealStageEvent`, `class SyncLog`
    - `app/models.py` Deal has `is_sin_cotizar`, `commission_amount`, `talent_id` (nullable), `loss_reason`, `brand_category`, `expected_collection_date` columns
    - `uv run alembic upgrade head` exits 0 and creates tables `deals`, `deal_stage_events`, `sync_logs`
    - New migration file's `down_revision` equals `'324116cbf0dd'`
    - `pyproject.toml` lists `apscheduler` and `rapidfuzz` dependencies
    - `tests/conftest.py` defines a fixture providing an `httpx.MockTransport` (or respx) for Pipedrive responses
    - `uv run python -c "import apscheduler, rapidfuzz"` exits 0
  </acceptance_criteria>
  <done>Models, migration, deps, and test fixtures exist; alembic upgrade succeeds; mocked-Pipedrive test infrastructure is in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pipedrive v2 client + sync job (deal data → SQLite)</name>
  <files>app/integrations/base.py, app/integrations/pipedrive.py, app/sync/__init__.py, app/sync/jobs.py, app/scripts/match_talent_products.py, tests/test_pipedrive_integration.py, tests/test_sync.py</files>
  <read_first>
    - .planning/phases/02-pipedrive-integration-core-dashboard/02-RESEARCH.md (Pattern 1/2, Pitfalls 1-5, Code Examples: bulk products, incremental sync, auto-match skeleton)
    - .planning/phases/02-pipedrive-integration-core-dashboard/02-PATTERNS.md (integrations + app/sync/jobs.py + match_talent_products.py sections)
    - app/scripts/seed_talents.py and app/scripts/seed_admin.py (session-scoped batch + idempotent upsert conventions)
    - app/config.py (settings.PIPEDRIVE_API_TOKEN / PIPEDRIVE_DOMAIN consumption)
    - app/models.py (Deal/DealStageEvent/SyncLog from Task 1)
    - tests/conftest.py (mock_pipedrive_transport fixture from Task 1)
  </read_first>
  <behavior>
    - Test: PipedriveClient uses `x-api-token` header (NOT ?api_token= query param) — assert the request header is set, never a query param (Pitfall 1).
    - Test: resolve_custom_field returns the LABEL string for an enum field (e.g. "Presupuesto insuficiente"), not the integer option id (Pitfall 2, PIPE-04).
    - Test: resolve_custom_field returns the raw date string for the date-typed "fecha de cobro esperada" field.
    - Test: sync_pipedrive upserts deals; a deal with value 0 gets is_sin_cotizar=True (PIPE-03); commission_amount == value * 0.70 for non-zero (PIPE-02).
    - Test: a deal whose product_id matches a talent_products.pipedrive_product_id gets that talent_id; an unmatched product → talent_id is None, deal still persisted (D-17/Pitfall 4).
    - Test: FIRST sync of a deal creates NO DealStageEvent (Pitfall 3); a subsequent sync that changes stage_id creates exactly one DealStageEvent.
    - Test: a second sync_pipedrive call while a SyncLog with status="running" exists is a no-op / returns without a second API fetch (Pitfall 5 concurrency guard).
    - Test: stage_id resolves to one of the 6 funnel stage names in order (PIPE-05).
  </behavior>
  <action>
    Create app/integrations/base.py + app/integrations/pipedrive.py implementing RESEARCH.md Pattern 1 verbatim: `_client()` builds `httpx.Client(base_url=f"https://{settings.PIPEDRIVE_DOMAIN}.pipedrive.com/api/v2", headers={"x-api-token": settings.PIPEDRIVE_API_TOKEN}, timeout=30.0)`; `_paginate()` generator (limit=500, additional_data.next_cursor); `_get_with_retry()` with Retry-After-aware 429 backoff (max 5 attempts). Add `get_deals(client, updated_since=None)`, `get_deal_products_bulk(client, deal_ids)` (chunks of 100 via /deals/products?deal_ids=), `get_products(client)`, `get_stages(client)`, and `build_field_maps()`/`resolve_custom_field()` from Pattern 2 (build BOTH hash→name and per-field option_id→label maps). Do NOT log full request objects (Security: token must never appear in logs).
    Create app/sync/__init__.py (empty package) and app/sync/jobs.py with `sync_pipedrive(db: Session) -> SyncLog` accepting the session as a parameter (per PATTERNS.md — callable from scheduler, endpoint, and tests). Implementation order: (1) concurrency guard — check latest SyncLog.status=="running" with a staleness timeout; if running, no-op return; (2) write SyncLog row status="running", started_at=now, commit immediately; (3) build field maps + stage map (stage_id→name/order_nr) from /stages; (4) paginate /deals (omit updated_since on first sync = no prior success SyncLog; else updated_since=last success finished_at as RFC3339); (5) bulk-fetch deal products; (6) for each deal: resolve talent via talent_products.pipedrive_product_id (None if unmatched, D-17), compute commission_amount = value*0.70 and is_sin_cotizar = (value==0), resolve loss_reason/brand_category/expected_collection_date via resolve_custom_field; (7) diff stage_id/status vs existing Deal row → insert DealStageEvent ONLY when existing_deal is not None (Pitfall 3), set stage_entered_at on stage change; (8) upsert Deal; (9) commit; (10) update SyncLog status="success"/finished_at/records_synced, wrapping the whole body in try/except writing status="error", error_message=str(exc) (never repr the request — Security). Use the 6 razones de pérdida and 6 categorías de marca labels from PROJECT.md / 02-CONTEXT.md specifics.
    Create app/scripts/match_talent_products.py per RESEARCH.md skeleton + seed_talents.py/seed_admin.py conventions: `session_factory=SessionLocal` param, rapidfuzz `process.extractOne(talent.name, product_names, scorer=fuzz.WRatio, processor=utils.default_process)`, THRESHOLD=85, upsert TalentProduct.pipedrive_product_id, print MATCHED lines + D-18 unmapped-products report + unmatched-talents report to stdout, `if __name__ == "__main__": run()`.
    Write the behavior tests above into tests/test_pipedrive_integration.py (client/field-resolution) and tests/test_sync.py (sync job behaviors) using the mock_pipedrive_transport fixture.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pipedrive_integration.py tests/test_sync.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `app/integrations/pipedrive.py` contains the literal string `x-api-token` and contains `def resolve_custom_field`
    - `app/sync/jobs.py` contains `def sync_pipedrive` taking a `db` session parameter
    - `app/scripts/match_talent_products.py` contains `process.extractOne` and a printed unmapped-products report
    - `uv run pytest tests/test_pipedrive_integration.py tests/test_sync.py -x` exits 0
    - Test asserts a $0 deal → `is_sin_cotizar=True` passes
    - Test asserts enum custom field resolves to a Spanish label string (not an int) passes
    - Test asserts unmatched-product deal persists with `talent_id IS NULL` passes
    - Test asserts first-sync creates no DealStageEvent passes
  </acceptance_criteria>
  <done>Pipedrive v2 client, sync job (commission/Sin-cotizar/custom-fields/event-diffing/concurrency guard), and auto-match script exist and pass all behavior tests against mocked Pipedrive responses.</done>
</task>

<task type="auto">
  <name>Task 3: Sync endpoint, scheduler, dashboard shell + "Última sync" indicator</name>
  <files>app/routers/sync.py, app/sync/scheduler.py, app/main.py, frontend/index.html, frontend/css/styles.css, frontend/js/dashboard.js, tests/test_sync.py</files>
  <read_first>
    - app/main.py (current router include order + static mount comment — additive only, static mount stays LAST)
    - app/routers/talents.py (APIRouter(dependencies=[Depends(get_current_user)]) pattern + 404 pattern)
    - .planning/phases/02-pipedrive-integration-core-dashboard/02-PATTERNS.md (sync.py, scheduler.py, frontend index.html / dashboard.js / styles.css sections)
    - .planning/phases/02-pipedrive-integration-core-dashboard/02-UI-SPEC.md (Copywriting Contract: Sincronizar ahora / toast / failure banner / Última sync; Spacing 16px card + 12px 16px kpi; Typography kpi-val weight 500)
    - frontend/login.html (HTML file conventions, Google Fonts link)
    - frontend/js/auth.js (apiFetch wrapper — reuse verbatim)
    - frontend/css/styles.css (existing :root tokens + .card padding 18px to change to 16px)
    - .planning/reference/mockup.html (nav/tabbar/live-pill markup to port)
  </read_first>
  <action>
    Create app/routers/sync.py: `router = APIRouter(prefix="/sync", tags=["sync"], dependencies=[Depends(get_current_user)])`. Add `POST /sync/pipedrive` returning 202 ACCEPTED — first check the concurrency guard (latest SyncLog.status=="running") and if a sync is in flight return a body indicating "already running" (still 202, no second task scheduled); otherwise schedule `background_tasks.add_task(_run_sync_in_background)` where `_run_sync_in_background` opens its own SessionLocal, calls sync_pipedrive, and closes in finally. Add `GET /sync/status` returning the latest SyncLog (status, finished_at, records_synced, error_message) as a Pydantic SyncStatus model — used by the frontend for the "Última sync: hace X min" indicator and the D-24 failure banner.
    Create app/sync/scheduler.py per RESEARCH.md: module-level `BackgroundScheduler`, `_run_pipedrive_sync()` (own SessionLocal + try/finally), `start()` adds the job `"interval", hours=1, id="sync_pipedrive", replace_existing=True` then `scheduler.start()`, and `shutdown()` calls `scheduler.shutdown(wait=False)`.
    Edit app/main.py additively: add `lifespan` asynccontextmanager that calls `sync_scheduler.start()` on enter and `sync_scheduler.shutdown()` on exit, pass `lifespan=lifespan` to FastAPI(), include the new `sync.router`, and keep the static-frontend `app.mount("/", ...)` as the LAST registration (preserve the existing comment).
    Create frontend/index.html: dashboard shell reusing the mockup's `.nav` (logo + `.nav-right` with `.live-pill` repurposed to "Última sync: hace -- min" per D-21) and `.tabbar` (Resumen / Por talento / Funnel tabs with setPage). Include the Google Fonts link (DM Sans 400/500, DM Mono 400) and `<script src="/js/dashboard.js">`. Add a "Sincronizar ahora" button (`.btn.primary`) on the Resumen tab. Render Plans 02/03 sections as empty containers this plan does not populate (just the tab scaffolding + sync controls).
    Extend frontend/css/styles.css: port the component classes listed in PATTERNS.md styles.css section from mockup `<style>` (nav, tabbar, tab, live-pill, live-dot, kpi-grid, section, section-title, btn etc.) merging with existing :root (identical var names — additive). Apply UI-SPEC deviations: change existing `.card { padding: 18px }` → `16px`; port `.kpi` padding as `12px 16px`; port `.kpi-val` with `font-weight:500` (NOT 600).
    Create frontend/js/dashboard.js: reuse `apiFetch` from auth.js (do not redefine the 401 redirect). Implement `setPage(name)` tab switching, `loadSyncStatus()` polling `GET /sync/status` to update the `.live-pill` text ("Última sync: hace X min", green dot if recent / amber if stale-or-failed) and render the D-24 failure banner ("No se pudo sincronizar — mostrando datos de hace X horas") when status=="error", and `triggerSync()` for the "Sincronizar ahora" button: set label "Sincronizando...", disable, POST /sync/pipedrive via apiFetch; on the "already running" response show toast "Ya hay una sincronización en curso"; on accepted, poll /sync/status and on completion show toast "Sync completado — {N} deals actualizados", then re-enable. Use exact copy strings from the UI-SPEC Copywriting Contract.
    Add to tests/test_sync.py: `test_trigger_sync_requires_auth(client)` asserts 401 unauthenticated; `test_trigger_sync_returns_202(auth_client)` asserts POST /sync/pipedrive returns 202; `test_sync_status_endpoint(auth_client)` asserts GET /sync/status shape.
  </action>
  <verify>
    <automated>uv run pytest tests/test_sync.py -x && uv run python -c "from app.main import app; print('app boots')"</automated>
  </verify>
  <acceptance_criteria>
    - `app/routers/sync.py` defines `router` with `dependencies=[Depends(get_current_user)]` and a `POST` route at `/pipedrive`
    - `app/main.py` contains `lifespan` and includes `sync.router`; the `app.mount("/", StaticFiles...` line is the last router/mount registration
    - `app/sync/scheduler.py` adds an interval job with `hours=1` and `id="sync_pipedrive"`
    - `frontend/index.html` contains the text `Sincronizar ahora` and a `.live-pill` element
    - `frontend/js/dashboard.js` calls `apiFetch("/sync/pipedrive"` and references `/sync/status`
    - `frontend/css/styles.css` contains `padding: 16px` for `.card` and `.kpi-val` with `font-weight:500` (no `font-weight:600` on `.kpi-val`)
    - `test_trigger_sync_requires_auth` returns 401; `test_trigger_sync_returns_202` returns 202
    - `uv run python -c "from app.main import app"` exits 0 (scheduler wiring imports cleanly)
  </acceptance_criteria>
  <done>User can click "Sincronizar ahora", trigger an async sync, see the "Última sync" indicator and (on failure) the warning banner; hourly background sync is wired via lifespan; sync endpoints are auth-protected.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full Pipedrive sync slice: v2 client, sync job, hourly scheduler, "Sincronizar ahora" button + "Última sync" indicator, and the one-time talent↔product auto-match script. This checkpoint verifies the FIRST REAL sync against the live SEG Pipedrive account (RESEARCH.md Open Questions 1-3: real stage names/IDs, real custom field types, and confirming credentials work).</what-built>
  <how-to-verify>
    1. Confirm `PIPEDRIVE_API_TOKEN` and `PIPEDRIVE_DOMAIN` are set in `.env` with real SEG credentials. If not yet available, set them now (Pipedrive → Settings → Personal preferences → API).
    2. Run the auto-match script: `uv run python -m app.scripts.match_talent_products`. Review the printed MATCHED lines, the "Unmapped Pipedrive products" report, and "Talents with no match" report. Confirm the matches look correct; correct any mismatches via the Phase 1 `/talents/{id}/products` endpoints (D-19).
    3. Start the app: `uv run uvicorn app.main:app --reload`. Log in, open the dashboard at `/`, click "Sincronizar ahora".
    4. Confirm the button shows "Sincronizando...", then the "Última sync" indicator updates and a completion toast appears.
    5. Inspect the DB: `uv run python -c "from app.database import SessionLocal; from app.models import Deal; db=SessionLocal(); print(db.query(Deal).count(), 'deals'); [print(d.title, d.value, d.stage_name, d.talent_id, d.is_sin_cotizar, d.loss_reason, d.brand_category) for d in db.query(Deal).limit(10)]"`. Confirm: real deals synced, commission computed, $0 deals flagged Sin cotizar, loss_reason/brand_category show Spanish labels (NOT integers — Pitfall 2), unmatched deals have talent_id=None.
    6. Confirm the 6 stage names from Pipedrive match (or are mapped to) Llamada → Cotización → Negociación → Contrato → En ejecución → Cobranza (PIPE-05). If Pipedrive uses different stage labels, note them so Plan 02's funnel uses the real names.
  </how-to-verify>
  <resume-signal>Type "approved" once a real sync populates correct deal data (labels not integers, commission correct, Sin cotizar flagged), or describe issues (wrong matches, integer labels, auth 401, missing custom fields) to fix before proceeding.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pipedrive API ↔ app | Outbound: app sends `x-api-token` secret; inbound: untrusted JSON deal data parsed into ORM rows |
| Browser ↔ /sync/*, /dashboard/* | Authenticated user triggers sync / reads status; JWT cookie crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | Pipedrive token in logs/errors | mitigate | `app/sync/jobs.py` writes `SyncLog.error_message = str(exc)` only; integration layer never logs full request objects or `response.request.headers` (token-bearing). httpx default logging level kept (no body logging). |
| T-02-02 | Elevation of Privilege | Unauthenticated access to /sync/* | mitigate | `app/routers/sync.py` uses `dependencies=[Depends(get_current_user)]`; `test_trigger_sync_requires_auth` asserts 401. |
| T-02-03 | Denial of Service | "Sync now" spammed → unbounded Pipedrive API burst (own token budget) | mitigate | Concurrency guard (SyncLog.status=="running" check in jobs.py + sync.py); second trigger is a no-op. Doubles as abuse protection. |
| T-02-04 | Tampering | Malformed/oversized Pipedrive JSON corrupts deal rows | accept | Server-controlled API (SEG's own Pipedrive, not user input); normalization uses `.get()` with defaults; low risk for a single-tenant internal tool. |
| T-02-SC | Tampering | pip installs (apscheduler, rapidfuzz) | mitigate | RESEARCH.md Package Legitimacy Audit: both [OK] via slopcheck + PyPI JSON + known GitHub repos. No [ASSUMED]/[SUS] packages — no blocking checkpoint required. |
</threat_model>

<verification>
- `uv run pytest` green (Wave 0 tests + sync/integration tests).
- `uv run alembic upgrade head` creates deals/deal_stage_events/sync_logs.
- App boots with scheduler lifespan; `/sync/pipedrive` is auth-protected (401 without cookie, 202 with).
- Human checkpoint: real Pipedrive sync populates correct deal data (Spanish labels, correct commission, Sin cotizar flagged, unmatched→null talent).
</verification>

<success_criteria>
- Deals sync from Pipedrive into SQLite with stage, value, product→talent, 70% commission, Sin cotizar flag, and custom fields (PIPE-01/02/03/04/05).
- Manual "Sincronizar ahora" (async, 202) + hourly scheduler both call sync_pipedrive.
- "Última sync" indicator and failure banner driven by SyncLog.
- One-time auto-match script populates talent_products.pipedrive_product_id with a manual-review report.
</success_criteria>

<output>
Create `.planning/phases/02-pipedrive-integration-core-dashboard/02-01-SUMMARY.md` when done.
</output>
