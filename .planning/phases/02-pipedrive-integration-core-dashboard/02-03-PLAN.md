---
phase: 02-pipedrive-integration-core-dashboard
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - app/services/kpis.py
  - app/services/funnel.py
  - app/schemas/dashboard.py
  - app/routers/dashboard.py
  - frontend/index.html
  - frontend/css/styles.css
  - frontend/js/dashboard.js
  - tests/test_kpis.py
  - tests/test_funnel.py
  - tests/test_dashboard.py
autonomous: false
requirements: [DASH-02]

must_haves:
  truths:
    - "The Por talento tab lets the user select a talent and shows that talent's KPIs computed only from their deals (talent_id == selected)"
    - "The Por talento tab shows the selected talent's individual funnel (6 stages, count/amount) from their deals only"
    - "The Por talento tab shows 'Oportunidades perdidas': a per-reason summary count line plus a list of lost deals each with a razón-de-pérdida pill (label, never an integer) (D-25)"
    - "The Por talento tab shows a 'Categorías de marca' donut + legend measuring % by deal count across the 6 brand categories (D-26/D-27)"
    - "The Por talento section order is KPIs → Funnel → Deals activos → Categorías de marca → Oportunidades perdidas (D-28); 'Fuente de leads' does not appear"
    - "Selecting a talent with no lost deals shows 'Sin oportunidades perdidas este periodo'; with no categorized deals shows 'Sin categorías de marca registradas todavía'"
  artifacts:
    - path: "app/services/kpis.py"
      provides: "talent_detail(db, talent_id): per-talent KPIs, lost-opportunities (grouped by reason), brand-category donut data"
      contains: "def talent_detail"
    - path: "app/schemas/dashboard.py"
      provides: "TalentDetail, LostOpportunity, LostReasonSummary, BrandCategorySlice schemas"
      contains: "class TalentDetail"
    - path: "app/routers/dashboard.py"
      provides: "GET /dashboard/talents/{id} auth-protected, 404 on unknown talent"
      contains: "talents/{talent_id}"
    - path: "frontend/js/dashboard.js"
      provides: "loadTalentDetail(id) + renderers for talent selector, donut, lost opportunities"
      contains: "loadTalentDetail"
  key_links:
    - from: "frontend/js/dashboard.js"
      to: "/dashboard/talents/"
      via: "apiFetch GET on talent-card selection in Por talento tab"
      pattern: "dashboard/talents/"
    - from: "app/services/kpis.py"
      to: "Deal.loss_reason"
      via: "lost-opportunity grouping by resolved razón-de-pérdida label"
      pattern: "loss_reason"
    - from: "app/services/kpis.py"
      to: "Deal.brand_category"
      via: "brand-category donut grouped by deal count (D-27)"
      pattern: "brand_category"
---

<objective>
Build the per-talent dashboard slice and render the Por talento tab. This is a vertical slice on top of Plan 02-02's global dashboard layer: SQLite (existing Deal rows) → talent_detail() in services/kpis.py + per-talent funnel in services/funnel.py → schemas/dashboard.py (TalentDetail) + GET /dashboard/talents/{id} → frontend Por talento tab (talent selector, KPIs, individual funnel, deals activos, brand-category donut, lost opportunities).

Purpose: DASH-02 (Phase 2 portion) becomes live — per-talent KPIs, individual funnel, lost opportunities with reason (D-25), and brand category breakdown (D-26/D-27). The revenue projection / collection calendar / campaign table parts of DASH-02 are explicitly deferred to Phase 4 (need Trello data) per ROADMAP.
Output: talent_detail() in app/services/kpis.py (+ per-talent funnel helper in funnel.py), new TalentDetail/LostOpportunity/LostReasonSummary/BrandCategorySlice schemas, GET /dashboard/talents/{id} endpoint, Por talento tab rendering (talent selector, donut, lost-opportunities list) in frontend, and tests.
</objective>

<execution_context>
@/Users/alexiszamoraia/Developer/projects/lumixia/clients/santillan-ent/seg-dashboard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexiszamoraia/Developer/projects/lumixia/clients/santillan-ent/seg-dashboard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pipedrive-integration-core-dashboard/02-CONTEXT.md
@.planning/phases/02-pipedrive-integration-core-dashboard/02-RESEARCH.md
@.planning/phases/02-pipedrive-integration-core-dashboard/02-PATTERNS.md
@.planning/phases/02-pipedrive-integration-core-dashboard/02-UI-SPEC.md
@.planning/phases/02-pipedrive-integration-core-dashboard/02-VALIDATION.md
@.planning/phases/02-pipedrive-integration-core-dashboard/02-02-PLAN.md

<interfaces>
<!-- Contracts established by Plans 02-01 and 02-02 that this plan extends. -->

From app/models.py (Plan 02-01):
- Deal(... talent_id: int|None, status ["open"/"won"/"lost"], stage_name, value, commission_amount, is_sin_cotizar, loss_reason: str|None [already a resolved Spanish label per Pitfall 2], brand_category: str|None, ...)
- Talent(id, name, active, category, photo_url, products)

From app/services/kpis.py (Plan 02-02 — EXTEND this file, do not rewrite):
- global_kpis(db), talent_ranking(db) already exist — ADD talent_detail(db, talent_id) here.

From app/services/funnel.py (Plan 02-02 — EXTEND):
- STAGES constant (6 canonical stages) + funnel_overview(db) already exist — ADD a per-talent funnel helper, e.g. funnel_overview(db, talent_id=None) extended to filter by talent, or a new talent_funnel(db, talent_id). Reuse STAGES; do not redefine the stage list.

From app/schemas/dashboard.py (Plan 02-02 — EXTEND):
- KpiTile, StageBucket, BottleneckInfo, DashboardSummary, FunnelOverview already exist — reuse KpiTile/StageBucket; ADD TalentDetail and the new lost-opportunity / brand-category models.

From app/routers/dashboard.py (Plan 02-02 — EXTEND):
- router (prefix="/dashboard", auth-protected), GET /summary, GET /funnel already exist — ADD GET /talents/{talent_id}. Use the talents.py 404 pattern: talent = db.get(Talent, talent_id); if talent is None: raise HTTPException(404, "Talent not found").

From frontend/index.html + frontend/js/dashboard.js (Plans 02-01/02-02 — EXTEND):
- Por talento tab container exists (currently empty/scaffold); setPage already switches to it. ADD the talent selector + detail sections and a loadTalentDetail(id) function wired into talent-card clicks and the Por talento tab activation.

The 5 razones de pérdida (02-CONTEXT.md <specifics>, exact labels): Presupuesto insuficiente, No respondieron, Eligieron otro talento, Campaña cancelada, Sin fit estratégico.
The 6 categorías de marca (02-CONTEXT.md <specifics>, exact labels): Moda/Retail, Alimentos/Restaurantes, Agencias, Medios y Entretenimiento, Educación/Gobierno, Otros.
loss_reason / brand_category on Deal rows are ALREADY resolved Spanish label strings (Plan 02-01 resolve_custom_field) — group by them directly, never re-resolve integers.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: talent_detail service (per-talent KPIs, lost opps, brand donut) + per-talent funnel + schemas</name>
  <files>app/services/kpis.py, app/services/funnel.py, app/schemas/dashboard.py, tests/conftest.py, tests/test_kpis.py, tests/test_funnel.py</files>
  <read_first>
    - app/services/kpis.py (Plan 02-02 — global_kpis/talent_ranking conventions; EXTEND with talent_detail)
    - app/services/funnel.py (Plan 02-02 — STAGES constant + funnel_overview; EXTEND for per-talent filter)
    - app/schemas/dashboard.py (Plan 02-02 — KpiTile/StageBucket to reuse; EXTEND with new models)
    - app/models.py (Deal.loss_reason / Deal.brand_category — already resolved labels)
    - .planning/phases/02-pipedrive-integration-core-dashboard/02-CONTEXT.md (D-25/D-26/D-27/D-28; <specifics> 5 razones + 6 categorías exact labels)
    - .planning/phases/02-pipedrive-integration-core-dashboard/02-PATTERNS.md (services/kpis.py per-talent section: filter Deal.talent_id == talent_id; lost = status=='lost' grouped by loss_reason; brand donut grouped by brand_category % by COUNT)
    - .planning/phases/02-pipedrive-integration-core-dashboard/02-UI-SPEC.md (Copywriting Contract: lost-reason summary format "{N} por {razón}", donut legend "{category} — {pct}% ({count} deals)", empty states)
    - tests/conftest.py (seed_deals fixture from Plan 02-02 — extend it with lost deals + brand_category values if not already covered)
  </read_first>
  <behavior>
    - Test (test_kpis::test_talent_detail_only_own_deals): talent_detail(db, talent_id) computes KPIs (pipeline, won, commission) summing ONLY deals where talent_id == that id; deals of other talents and talent_id=None deals are excluded.
    - Test (test_kpis::test_lost_opportunities_grouped): for a talent with lost deals, talent_detail returns a per-reason summary list (each {reason_label, count}) using the resolved Spanish labels (e.g. "Presupuesto insuficiente"), and an itemized lost-deal list each with brand/title + amount + the resolved loss_reason label (never an integer — Pitfall 2).
    - Test (test_kpis::test_brand_category_by_count): the brand-category breakdown is % by COUNT of deals (D-27), not by revenue; percentages across categories sum to ~100 (within rounding) when the talent has categorized deals; categories with 0 deals are omitted.
    - Test (test_kpis::test_talent_detail_empty): a talent with no lost deals returns an empty lost-opportunities list (frontend shows the empty-state copy); a talent with no categorized deals returns empty brand-category data.
    - Test (test_funnel::test_talent_funnel_filters): the per-talent funnel returns the 6 STAGES in order with counts from only that talent's open deals.
  </behavior>
  <action>
    Extend tests/conftest.py seed_deals (if needed): ensure at least one talent has >=2 lost deals with differing loss_reason labels and several deals with brand_category set, so the lost-opportunity grouping and donut tests have data.

    Extend app/services/funnel.py: make the per-talent funnel available without duplicating STAGES — either add an optional talent_id param to funnel_overview (apply .filter(Deal.talent_id == talent_id) when provided, default None = global) OR add talent_funnel(db, talent_id) that reuses the STAGES loop. Per-talent funnel only needs the 6-stage count/amount (bottleneck stays global on the Funnel tab; do not compute a per-talent bottleneck unless trivially free).

    Extend app/services/kpis.py with talent_detail(db, talent_id) -> dict:
    - KPIs: filter Deal.talent_id == talent_id for all aggregates (pipeline total = SUM(value) of that talent's deals; cerrados = SUM/COUNT where status=='won'; comisión = SUM(commission_amount)). Reuse the KpiTile shape from Plan 02-02. Per-talent figures EXCLUDE talent_id IS NULL deals (Pitfall 4).
    - Lost opportunities (D-25): deals = db.query(Deal).filter(Deal.talent_id == talent_id, Deal.status == "lost").all(). Build a per-reason summary: group by Deal.loss_reason (a resolved label) producing [{reason, count}] over the 5 known razones; and an itemized list [{title/brand, amount, loss_reason}]. Use Deal.loss_reason directly (already a label per Plan 02-01 — never re-resolve integers).
    - Brand categories (D-26/D-27): db.query(Deal.brand_category, func.count(Deal.id)).filter(Deal.talent_id == talent_id, Deal.brand_category.isnot(None)).group_by(Deal.brand_category); compute pct = count / total_categorized * 100 (by COUNT, not revenue — D-27); return slices [{category, count, pct}] omitting zero-count categories.

    Extend app/schemas/dashboard.py:
    - LostReasonSummary(BaseModel): reason: str, count: int.
    - LostOpportunity(BaseModel): title: str, amount: float, loss_reason: str | None = None.
    - BrandCategorySlice(BaseModel): category: str, count: int, pct: float.
    - TalentDetail(BaseModel): talent_id: int, name: str, category: str | None = None, kpis: list[KpiTile], funnel: list[StageBucket], lost_summary: list[LostReasonSummary], lost_opportunities: list[LostOpportunity], brand_categories: list[BrandCategorySlice].

    Write the behavior tests above into tests/test_kpis.py (per-talent KPIs, lost grouping, brand-by-count, empty) and the per-talent funnel test into tests/test_funnel.py.
  </action>
  <verify>
    <automated>uv run pytest tests/test_kpis.py tests/test_funnel.py -x</automated>
  </verify>
  <acceptance_criteria>
    - app/services/kpis.py contains "def talent_detail" filtering by Deal.talent_id == talent_id
    - app/services/kpis.py lost-opportunity logic groups by Deal.loss_reason and brand logic groups by Deal.brand_category
    - app/services/funnel.py provides a per-talent funnel (either funnel_overview talent_id param or talent_funnel) reusing the existing STAGES constant (STAGES not duplicated)
    - app/schemas/dashboard.py contains "class TalentDetail", "class LostOpportunity", "class BrandCategorySlice"
    - Test test_talent_detail_only_own_deals asserts other talents' and null-talent deals are excluded — passes
    - Test test_brand_category_by_count asserts percentages are by deal count and sum ~100 — passes
    - Test test_lost_opportunities_grouped asserts reason labels are Spanish strings (not ints) — passes
    - uv run pytest tests/test_kpis.py tests/test_funnel.py -x exits 0
  </acceptance_criteria>
  <done>talent_detail computes per-talent KPIs, lost opportunities (per-reason summary + itemized list with label pills), and brand-category breakdown (% by deal count), plus a per-talent funnel reusing the shared STAGES; backed by new schemas and passing unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: GET /dashboard/talents/{id} endpoint</name>
  <files>app/routers/dashboard.py, tests/test_dashboard.py</files>
  <read_first>
    - app/routers/dashboard.py (Plan 02-02 — router + /summary + /funnel; EXTEND with /talents/{id})
    - app/routers/talents.py (404 pattern: db.get(Talent, id) -> HTTPException 404)
    - app/services/kpis.py (talent_detail from Task 1), app/services/funnel.py (per-talent funnel)
    - app/schemas/dashboard.py (TalentDetail from Task 1)
    - tests/conftest.py (client, auth_client, seed_deals), tests/test_talents.py (require-auth pattern)
  </read_first>
  <action>
    Extend app/routers/dashboard.py: add GET /dashboard/talents/{talent_id} (response_model=TalentDetail) on the existing router (already prefix="/dashboard", auth-protected). Signature: def get_talent_detail(talent_id: int, db: Session = Depends(get_db)). First do the 404 guard: talent = db.get(Talent, talent_id); if talent is None: raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="Talent not found"). Then delegate to kpis.talent_detail(db, talent_id) and the per-talent funnel helper, assembling a TalentDetail (set funnel from the per-talent stage buckets). Do NOT add a new APIRouter — extend the existing one.
    Add to tests/test_dashboard.py: test_talent_detail_requires_auth(client) asserts 401 unauthenticated on /dashboard/talents/1; test_talent_detail_endpoint(auth_client, seed_deals) asserts 200 + TalentDetail shape (kpis, funnel of 6 stages, lost_summary, lost_opportunities, brand_categories present) for a seeded talent; test_talent_detail_404(auth_client) asserts 404 for a non-existent talent id.
  </action>
  <verify>
    <automated>uv run pytest tests/test_dashboard.py -x && uv run python -c "from app.main import app; print('app boots')"</automated>
  </verify>
  <acceptance_criteria>
    - app/routers/dashboard.py defines a GET route at /talents/{talent_id} using response_model=TalentDetail on the existing /dashboard router
    - The endpoint raises HTTPException 404 with detail "Talent not found" for unknown talent ids (talents.py pattern)
    - No second APIRouter is created (extends the Plan 02-02 router)
    - test_talent_detail_requires_auth returns 401; test_talent_detail_404 returns 404; test_talent_detail_endpoint returns 200 with the TalentDetail shape (6-stage funnel)
    - uv run pytest tests/test_dashboard.py -x exits 0 and app imports cleanly
  </acceptance_criteria>
  <done>Auth-protected GET /dashboard/talents/{id} returns a validated TalentDetail (KPIs, per-talent funnel, lost opportunities, brand categories) and 404s on unknown talents; contract tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Render Por talento tab (selector, KPIs, funnel, deals, donut, lost opps)</name>
  <files>frontend/index.html, frontend/css/styles.css, frontend/js/dashboard.js</files>
  <read_first>
    - frontend/index.html (Por talento tab container — EXTEND; reuse the .kpi-grid/.funnel-row markup added in Plan 02-02)
    - frontend/js/dashboard.js (Plans 02-01/02-02 — apiFetch, setPage, renderKpis, funnel renderer; EXTEND with loadTalentDetail + selector/donut/lost-opp renderers)
    - frontend/css/styles.css (Plan 02-02 added .kpi/.funnel-row/.alert etc.; ADD .talent-selector/.talent-card/.tc-avatar/.tc-name/.tc-deals, .deal-row family, .pill, .donut-wrap/.donut-legend)
    - .planning/phases/02-pipedrive-integration-core-dashboard/02-PATTERNS.md (frontend sections: talent-selector + deal-row + donut markup; styles.css class list; UI-SPEC deviations — deal-amt/heading weight 500)
    - .planning/phases/02-pipedrive-integration-core-dashboard/02-UI-SPEC.md (Copywriting Contract: lost-reason summary "{N} por {razón}", donut legend "{category} — {pct}% ({count} deals)", empty states "Sin oportunidades perdidas este periodo" / "Sin categorías de marca registradas todavía"; D-28 section order; Color: accent reserved)
    - .planning/reference/mockup.html (talent-selector/talent-card, deal-row, donut-wrap/donut-legend reference; note .donut-wrap/.donut-legend are defined but UNUSED — author new donut markup)
  </read_first>
  <action>
    Extend frontend/css/styles.css: port the remaining component classes from PATTERNS.md styles.css section — .talent-selector, .talent-card, .tc-avatar, .tc-name, .tc-deals, .deal-row, .deal-l, .deal-r, .deal-amt, .deal-brand, .deal-dot, .deal-tipo, .pill, .donut-wrap, .donut-legend (verbatim values from PATTERNS.md). Apply UI-SPEC deviations: any new "heading"-role class (.deal-amt, .deal-brand) at weight 500 (not 600); reuse existing :root tokens. Do not redefine classes already ported in Plan 02-02.

    Extend frontend/index.html — Por talento tab container, in EXACT D-28 order: (1) a horizontal .talent-selector of .talent-card elements (populated from /dashboard/summary ranking or /talents), (2) a KPI section (.kpi-grid reusing Plan 02-02 .kpi tiles), (3) a "Funnel" .section (.card of 6 .funnel-row — reuse Plan 02-02 funnel markup), (4) a "Deals activos" .section (.card of .deal-row), (5) a "Categorías de marca" .section (.card containing a .donut-wrap with an SVG/conic-gradient donut + .donut-legend), (6) an "Oportunidades perdidas" .section (.card with a per-reason summary line above a .deal-row list). Do NOT add a "Fuente de leads" section (REMOVED per D-28). Give dynamic containers stable ids (e.g. talent-selector, talent-kpis, talent-funnel, talent-deals, brand-donut, brand-legend, lost-summary, lost-list).

    Extend frontend/js/dashboard.js (reuse apiFetch/showToast/setPage; reuse renderKpis + the funnel renderer from Plan 02-02 where possible):
    - Populate the talent selector (from the ranking already loaded for Resumen, or a fetch); clicking a .talent-card calls loadTalentDetail(talentId) and sets the card active state.
    - loadTalentDetail(id): apiFetch("/dashboard/talents/" + id); if !res return; parse JSON; renderKpis into talent-kpis (reuse Plan 02-02 renderer), render the 6-stage funnel into talent-funnel, render .deal-row deals into talent-deals, draw the donut into brand-donut + brand-legend (each legend row: "{category} — {pct}% ({count} deals)" per UI-SPEC; donut via conic-gradient computed from slice pcts), render lost opportunities: a summary line built from lost_summary as comma-joined "{count} por {reason}" (e.g. "3 por Presupuesto insuficiente, 2 por No respondieron") above a .deal-row list where each row shows a .pill with the loss_reason label.
    - Empty states: if lost_opportunities is empty render "Sin oportunidades perdidas este periodo" in lost-list; if brand_categories is empty render "Sin categorías de marca registradas todavía" in place of the donut. Use EXACT UI-SPEC copy.
    - Wire into setPage: when the Por talento tab is shown, populate the selector and auto-load the first/top talent's detail.
  </action>
  <verify>
    <automated>grep -q "dashboard/talents/" frontend/js/dashboard.js && grep -q "loadTalentDetail" frontend/js/dashboard.js && grep -q "Sin oportunidades perdidas este periodo" frontend/js/dashboard.js && grep -q "Sin categorías de marca registradas todavía" frontend/js/dashboard.js && grep -q "donut-legend" frontend/css/styles.css && ! grep -qi "Fuente de leads" frontend/index.html</automated>
  </verify>
  <acceptance_criteria>
    - frontend/css/styles.css defines .talent-selector, .talent-card, .deal-row, .pill, .donut-wrap, .donut-legend classes
    - frontend/index.html Por talento container has sections in EXACT order: talent-selector, KPIs, Funnel, Deals activos, Categorías de marca (donut), Oportunidades perdidas; no "Fuente de leads" section present
    - frontend/js/dashboard.js defines loadTalentDetail, calls apiFetch("/dashboard/talents/" + id), and renders the donut legend rows as "{category} — {pct}% ({count} deals)"
    - frontend/js/dashboard.js lost-opportunities summary line uses the "{count} por {reason}" format and each lost-deal row shows a .pill with the loss_reason label
    - frontend/js/dashboard.js contains the empty-state copy "Sin oportunidades perdidas este periodo" and "Sin categorías de marca registradas todavía"
    - Clicking a .talent-card calls loadTalentDetail and the Por talento tab auto-loads the first talent on activation
  </acceptance_criteria>
  <done>Por talento tab renders a talent selector and, for the selected talent, per-talent KPIs, individual funnel, active deals, a brand-category donut (% by deal count) with legend, and lost opportunities (per-reason summary + label-pill list) in the D-28 order; empty states and copy honor the UI-SPEC; "Fuente de leads" is absent.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The Por talento dashboard tab: a talent selector plus, for the selected talent, per-talent KPIs, individual 6-stage funnel, active deals, a "Categorías de marca" donut (% by deal count) with legend, and "Oportunidades perdidas" (per-reason summary + itemized list with razón-de-pérdida pills). This checkpoint confirms the per-talent data is correct, the section order matches D-28, and the donut/lost-opportunities render per the UI-SPEC.</what-built>
  <how-to-verify>
    1. Start the app (uv run uvicorn app.main:app --reload), log in, sync if needed, and open the Por talento tab.
    2. Confirm a horizontal talent selector renders; click a talent and confirm the detail sections update.
    3. Confirm section order is exactly: KPIs → Funnel → Deals activos → Categorías de marca → Oportunidades perdidas. Confirm there is NO "Fuente de leads" section (removed per D-28).
    4. KPIs/funnel sanity: confirm the per-talent KPIs and funnel reflect ONLY that talent's deals (switch to another talent and confirm the numbers change). Cross-check a talent's pipeline figure against the Resumen ranking row for the same talent.
    5. Categorías de marca: confirm the donut + legend render, each legend row formatted "{Categoría} — {pct}% ({count} deals)", percentages by DEAL COUNT (not revenue, D-27). If the talent has no categorized deals, confirm "Sin categorías de marca registradas todavía".
    6. Oportunidades perdidas: confirm a per-reason summary line (e.g. "3 por Presupuesto insuficiente, 2 por No respondieron") above a list of lost deals, each with a .pill showing the razón de pérdida as a SPANISH LABEL (never an integer — Pitfall 2). If the talent has no lost deals, confirm "Sin oportunidades perdidas este periodo".
    7. Confirm typography/spacing: heading-role text at weight 500, cards at 16px padding, only the reserved accent usage (no stray orange).
  </how-to-verify>
  <resume-signal>Type "approved" once the Por talento tab renders correct per-talent data in the D-28 order with the donut by deal count and label (not integer) loss-reason pills, or describe issues (wrong section order, integer loss reasons, donut by revenue, cross-talent leakage) to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ /dashboard/talents/{id} | Authenticated user reads a single talent's revenue/funnel/lost-deal data; JWT cookie crosses here; talent_id is user-supplied path input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02C-01 | Information Disclosure | Unauthenticated access to /dashboard/talents/{id} exposing per-talent revenue | mitigate | Endpoint inherits the existing /dashboard router's dependencies=[Depends(get_current_user)] (ASVS V4); test_talent_detail_requires_auth asserts 401. |
| T-02C-02 | Input Validation | Malformed/out-of-range talent_id path param | mitigate | FastAPI coerces talent_id: int (rejects non-int with 422); unknown id returns 404 via db.get + HTTPException (ASVS V5); test_talent_detail_404 asserts 404. |
| T-02C-03 | Tampering | Service output violates response contract / leaks raw ORM fields | mitigate | Endpoint declares response_model=TalentDetail; Pydantic validates and filters output shape at the boundary. |
| T-02C-04 | Information Disclosure | Loss-reason integer option-id leaks instead of label | accept | loss_reason is stored as a resolved label by Plan 02-01 (Pitfall 2 mitigated upstream); this plan groups by the stored label and never re-resolves. Verified by test_lost_opportunities_grouped asserting Spanish strings. |

No new package installs in this plan — no supply-chain (T-*-SC) checkpoint required.
</threat_model>

<verification>
- uv run pytest tests/test_kpis.py tests/test_funnel.py tests/test_dashboard.py -x green.
- uv run pytest full suite green (no regression in Plans 02-01/02-02).
- App boots; GET /dashboard/talents/{id} auth-protected (401 without cookie), 404 on unknown talent, 200 + TalentDetail on a real talent.
- Human checkpoint: Por talento tab renders correct per-talent data in D-28 order, donut by deal count, label loss-reason pills.
</verification>

<success_criteria>
- DASH-02 (Phase 2 portion): Por talento tab shows per-talent KPIs, individual funnel, lost opportunities (per-reason summary + labeled list, D-25), and brand-category donut (% by deal count, D-26/D-27).
- Section order matches D-28 (KPIs → Funnel → Deals activos → Categorías de marca → Oportunidades perdidas); "Fuente de leads" absent.
- Per-talent figures exclude talent_id IS NULL deals (Pitfall 4); loss reasons render as labels not integers (Pitfall 2).
- Empty states and UI-SPEC copy/typography/spacing/color honored.
- (Revenue projection / collection calendar / campaign table parts of DASH-02 remain deferred to Phase 4 per ROADMAP — out of scope here.)
</success_criteria>

<output>
Create `.planning/phases/02-pipedrive-integration-core-dashboard/02-03-SUMMARY.md` when done.
</output>
