---
phase: 03-google-sheets-leads-integration
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/services/leads.py
  - app/schemas/leads.py
  - app/routers/leads.py
  - frontend/index.html
  - frontend/js/leads.js
  - tests/test_leads.py
autonomous: true
requirements: [DASH-04, SHEET-02]
must_haves:
  truths:
    - "The Leads tab displays all synced leads as a list with a colored Score_Calidad pill (0-40 red, 41-70 amber, 71-100 green)"
    - "The Leads tab shows a 'Leads por talento' bar section with count and calificados per talent, including talents with zero leads"
    - "The leads list is filterable by talent, source, and status via GET /leads query params"
    - "GET /leads returns leads with talent_name and status_display resolved at the service layer"
  artifacts:
    - path: "app/services/leads.py"
      provides: "leads_list(db, talent_id, status, fuente) -> list[dict] with talent_name + status_display"
      contains: "def leads_list"
    - path: "app/routers/leads.py"
      provides: "GET /leads list endpoint with talent_id/status/fuente filters"
      contains: "response_model=list[LeadRow]"
    - path: "frontend/index.html"
      provides: "Leads tab nav + #page-leads section"
      contains: "page-leads"
    - path: "frontend/js/leads.js"
      provides: "loadLeads/render functions with escaped output and score pills"
      contains: "loadLeads"
  key_links:
    - from: "frontend/js/leads.js"
      to: "/leads"
      via: "apiFetch in loadLeads"
      pattern: "apiFetch\\(\"/leads"
    - from: "frontend/js/leads.js"
      to: "/leads/summary"
      via: "apiFetch for per-talent bars"
      pattern: "/leads/summary"
    - from: "app/routers/leads.py"
      to: "app.services.leads.leads_list"
      via: "list_leads delegates to service"
      pattern: "leads_list"
    - from: "frontend/index.html"
      to: "frontend/js/leads.js"
      via: "script tag + setPage('leads') tab"
      pattern: "leads.js"
---

<objective>
Build the Leads tab UI vertical slice: the `GET /leads` list endpoint (filterable
by talent/source/status), the leads_list service function, the LeadRow response
schema, the #page-leads section in index.html, and frontend/js/leads.js that
renders the per-talent bars and the leads list with colored score pills.

Purpose: Deliver DASH-04 — a user can open the Leads tab and see/filter all
synced leads classified by talent, source, and status. Completes the
classification surface for SHEET-02.

Output: leads_list service, GET /leads endpoint, page-leads HTML + nav tab,
leads.js, and list/filter tests.
</objective>

<execution_context>
@/Users/alexiszamoraia/Developer/projects/lumixia/clients/santillan-ent/seg-dashboard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexiszamoraia/Developer/projects/lumixia/clients/santillan-ent/seg-dashboard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-google-sheets-leads-integration/03-CONTEXT.md
@.planning/phases/03-google-sheets-leads-integration/03-RESEARCH.md
@.planning/phases/03-google-sheets-leads-integration/03-PATTERNS.md
@.planning/phases/03-google-sheets-leads-integration/03-01-SUMMARY.md

<interfaces>
<!-- Contracts produced by Plan 03-01 that this plan consumes. -->

From app/services/leads.py (Plan 03-01):
  QUALIFIED_STATUS = "✅ Aprobado - Respuesta enviada"
  STATUS_DISPLAY = {"✅ Aprobado - Respuesta enviada": "Aprobado",
                    "🚫 Remitente bloqueado": "Bloqueado", "En revisión": "En revisión"}
  def leads_summary(db) -> dict   # {"leads_totales": int, "calificados": int}
  def leads_by_talent(db) -> list[dict]  # rows: talent_id,name,total,calificados,is_sin_talento

From app/schemas/leads.py (Plan 03-01):
  class LeadRow(BaseModel): id, sheet_row_id, remitente_nombre, remitente_email, asunto,
    fecha_recepcion(datetime|None), talent_id(int|None), talent_name(str|None),
    status_filtrado, status_display, fuente, score_calidad(int|None), bloqueado, convertido_a_prospecto
  class LeadsSummary, class TalentLeadBar

From app/routers/leads.py (Plan 03-01): router with prefix="/leads", dependencies=[Depends(get_current_user)], GET /summary already exists.

Frontend conventions (frontend/js/dashboard.js — leads.js MUST mirror, do NOT redefine):
  - apiFetch (from auth.js) handles 401 → /login.html redirect; reuse it, never redefine
  - escHtml(str) — escape ALL Sheet-sourced strings before innerHTML interpolation (CR-02 stored-XSS mitigation)
  - showToast(message), formatRelativeTime(isoTimestamp)
  - CSS vars for score colors: --redT, --amberT, --greenT (and --redD/--amberD/--greenD backgrounds)
  - setPage(name, e) dispatches per-tab loaders (lines 40-58): add a `leads` branch
  - DOMContentLoaded init at end (lines 814-817)

Existing CSS classes in frontend/css/styles.css (already present — reuse, no new CSS needed):
  .source-row .source-icon .source-info .source-name .source-count
  .source-bar-wrap .source-bar-track .source-bar-fill .source-pct
  .deal-row .deal-l .deal-dot .deal-brand .deal-tipo .deal-r .pill
  .kpi .kpi-grid .kpi-label .kpi-val .kpi-sub .card .section .section-title

Existing index.html structure: nav (line 11), tabbar with 3 tabs (lines 18-22), pages
#page-overview/#page-talent/#page-funnel, scripts auth.js then dashboard.js (lines 153-154).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: leads_list service + GET /leads filterable endpoint</name>
  <files>app/services/leads.py, app/routers/leads.py, tests/test_leads.py</files>
  <read_first>
    - app/services/leads.py (existing leads_summary/leads_by_talent/STATUS_DISPLAY from Plan 03-01)
    - app/services/kpis.py (query patterns; relationship access)
    - app/routers/leads.py (existing /summary endpoint + router declaration)
    - app/routers/dashboard.py (filter query param + 404 guard pattern lines 115-130)
    - app/schemas/leads.py (LeadRow shape)
    - tests/test_leads.py (existing test conventions from Plan 03-01)
  </read_first>
  <behavior>
    - leads_list(db) with no filters returns every Lead as a dict including talent_name (None when talent_id is None) and status_display (mapped via STATUS_DISPLAY, falls back to raw status_filtrado if unmapped)
    - leads_list(db, talent_id=X) returns only leads for talent X
    - leads_list(db, status="✅ Aprobado - Respuesta enviada") returns only those leads
    - leads_list(db, fuente="Gmail") filters by source
    - GET /leads returns 200 + list for authed client; 401 without auth
    - GET /leads?talent_id=<nonexistent> returns 404
  </behavior>
  <action>
    Add `leads_list(db: Session, talent_id: int | None = None, status: str | None = None, fuente: str | None = None) -> list[dict]` to app/services/leads.py. Query Lead (optionally joinedload talent), apply filters: if talent_id is not None → filter Lead.talent_id == talent_id; if status → filter Lead.status_filtrado == status; if fuente → filter Lead.fuente == fuente. Order by Lead.fecha_recepcion descending (nulls last) then Lead.sheet_row_id descending. For each Lead build a dict matching LeadRow: id, sheet_row_id, remitente_nombre, remitente_email, asunto, fecha_recepcion, talent_id, talent_name = (lead.talent.name if lead.talent else None), status_filtrado, status_display = STATUS_DISPLAY.get(lead.status_filtrado, lead.status_filtrado), fuente, score_calidad, bloqueado, convertido_a_prospecto.

    Add `GET /leads` (the empty-path route, response_model=list[LeadRow]) to app/routers/leads.py: signature `list_leads(talent_id: int | None = None, status: str | None = None, fuente: str | None = None, db: Session = Depends(get_db))`. If talent_id is not None, validate it exists via db.get(Talent, talent_id) and raise HTTPException 404 "Talent not found" if missing (same guard pattern as dashboard.py). Delegate to leads_service.leads_list(db, talent_id=talent_id, status=status, fuente=fuente) and return [LeadRow(**row) for row in rows]. Add HTTPException, status imports and LeadRow + Talent imports as needed.

    Extend tests/test_leads.py with: test_leads_list_all (returns all with talent_name + status_display), test_leads_list_filter_talent, test_leads_list_filter_status, test_leads_endpoint_auth (200 with auth_client), test_leads_endpoint_unauth (401 with client), test_leads_endpoint_filter_talent_404.
  </action>
  <verify>
    <automated>uv run pytest tests/test_leads.py -k "list or endpoint" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "def leads_list" app/services/leads.py` returns 1
    - leads_list resolves display label via STATUS_DISPLAY: `grep -F "STATUS_DISPLAY.get(" app/services/leads.py` matches
    - `app/routers/leads.py` declares the list route with `response_model=list[LeadRow]`: `grep -F "response_model=list[LeadRow]" app/routers/leads.py` matches
    - GET /leads with a nonexistent talent_id returns 404 (covered by test_leads_endpoint_filter_talent_404)
    - The targeted tests pass: command in `<verify>` exits 0
  </acceptance_criteria>
  <done>leads_list service + filterable, auth-protected GET /leads endpoint exist with talent_name/status_display resolution and tests passing.</done>
</task>

<task type="auto">
  <name>Task 2: Leads tab HTML (#page-leads) + nav tab + leads.js render</name>
  <files>frontend/index.html, frontend/js/leads.js, frontend/js/dashboard.js</files>
  <read_first>
    - frontend/index.html (tabbar lines 18-22, page sections, script order lines 153-154)
    - frontend/js/dashboard.js (escHtml line 30, apiFetch usage, setPage lines 40-58, renderKpis lines 247-263, source-row usage, DOMContentLoaded lines 814-817)
    - frontend/css/styles.css (confirm .source-row/.source-bar-fill/.deal-row/.pill/.kpi classes exist)
    - .planning/reference/mockup.html (#page-leads structure: 3 KPI tiles, "Leads por talento" source-rows, "Leads recientes" deal-rows)
    - .planning/phases/03-google-sheets-leads-integration/03-CONTEXT.md (D-36 score colors, Claude's Discretion on sorting/filter depth)
  </read_first>
  <action>
    In frontend/index.html: add a 4th tab to the tabbar after "Funnel": `<div class="tab" onclick="setPage('leads', event)">Leads</div>`. Add a new `<div class="page" id="page-leads">` section (placed after #page-funnel, before the script tags) containing: (1) a `.section` with a `.kpi-grid` id="leads-kpi-grid" for the Leads-tab KPI tiles (Leads totales / Calificados / and a third tile such as "Por mail" or total source count); (2) a `.section` with `.section-title` "Leads por talento" and a `.card` id="leads-by-talent" for the source-bar rows; (3) a `.section` with filter controls — a `.section-title` "Leads recientes" plus simple `<select>` dropdowns id="filter-talent", id="filter-status", id="filter-fuente" (Vanilla JS, follows existing patterns; URL-param state optional) — and a `.card` id="leads-list" for the lead rows. Add `<script src="/js/leads.js"></script>` AFTER the dashboard.js script tag (so it can reuse escHtml/apiFetch/showToast defined there).

    Create frontend/js/leads.js. It MUST reuse escHtml, apiFetch, showToast, formatRelativeTime from dashboard.js (do not redefine). Implement:
    - `scorePillColor(score)` returning the CSS var pair per D-36: score 0-40 → {bg:"var(--redD)", text:"var(--redT)"}, 41-70 → amber (--amberD/--amberT), 71-100 → green (--greenD/--greenT); null/undefined → a neutral grey (var(--bg4)/var(--text2)).
    - `loadLeadsSummary()` — apiFetch("/leads/summary"); on !res return (401 redirected); on !res.ok showToast error; render the 3 KPI tiles into #leads-kpi-grid (Leads totales, Calificados, and a derived third) and render the per-talent bars into #leads-by-talent using .source-row/.source-icon (initials)/.source-info(.source-name + .source-count "{total} leads · {calificados} calificados")/.source-bar-wrap(.source-bar-track > .source-bar-fill width = total/maxTotal*100%)/.source-pct. Include talents with zero leads (render with 0). Render the "Sin talento asignado" bucket row last when present. ALL talent names and counts escaped via escHtml.
    - `loadLeads(filters)` — build query string from #filter-talent/#filter-status/#filter-fuente values, apiFetch(`/leads${qs}`); render each lead into #leads-list as a .deal-row: .deal-l(.deal-dot + .deal-brand "{remitente_nombre} · {remitente_email}" + .deal-tipo "{talent_name or 'Sin talento asignado'} · {asunto}") and .deal-r containing a status .pill (color by status: Aprobado→green, Bloqueado→red, En revisión→amber via status_display) and the score pill (scorePillColor). EVERY interpolated Sheet string (remitente_nombre, remitente_email, asunto, talent_name, status_display) MUST pass through escHtml. Empty state: "Sin leads" message when the list is empty.
    - Populate the filter dropdowns: #filter-status from the three STATUS_DISPLAY labels (+ an "Todos" option), #filter-talent from the per-talent bars data (+ "Todos"), #filter-fuente with "Todos"/"Gmail". Wire change listeners to call loadLeads().
    Add a `leads` branch to setPage in dashboard.js that calls loadLeadsSummary() then loadLeads() (edit dashboard.js setPage lines 51-58). Do NOT auto-load leads on DOMContentLoaded (only when the tab is opened).
  </action>
  <verify>
    <automated>uv run python -c "import re,sys; h=open('frontend/index.html').read(); j=open('frontend/js/leads.js').read(); assert 'page-leads' in h and \"setPage('leads'\" in h and 'leads.js' in h, 'html wiring'; assert 'loadLeads' in j and 'escHtml' in j and 'apiFetch' in j, 'leads.js wiring'; print('ok')"</automated>
    <human-check>Open the dashboard, log in, click the Leads tab: the per-talent bars render with count·calificados, the leads list shows rows with colored score pills (red/amber/green per score), and the talent/status/source dropdowns filter the list. Confirm no raw HTML appears in any lead subject/name (XSS escaped).</human-check>
  </verify>
  <acceptance_criteria>
    - `frontend/index.html` contains `id="page-leads"`, a tab with `setPage('leads'`, and `<script src="/js/leads.js">`: all three grep matches present
    - `frontend/js/leads.js` defines `loadLeads`, `loadLeadsSummary`, `scorePillColor`: `grep -c "function loadLeads\|function loadLeadsSummary\|function scorePillColor" frontend/js/leads.js` returns 3
    - leads.js escapes Sheet content: `grep -c "escHtml" frontend/js/leads.js` returns at least 4 (one per interpolated Sheet field)
    - leads.js does NOT redefine apiFetch/escHtml: `grep -c "function escHtml\|function apiFetch" frontend/js/leads.js` returns 0
    - dashboard.js setPage has a leads branch: `grep -F "name === \"leads\"" frontend/js/dashboard.js` matches
    - Automated wiring check in `<verify>` exits 0
  </acceptance_criteria>
  <done>The Leads tab renders per-talent bars and a filterable leads list with D-36 score pills; all Sheet-sourced strings are HTML-escaped; tab loads only on activation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sheet content → browser DOM | Lead subject/sender/talent strings (originally from untrusted email) rendered in the Leads tab |
| Browser → /leads API | Authenticated client requests with user-supplied filter params |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03B-01 | Tampering (Stored XSS) | frontend/js/leads.js | mitigate | All Sheet-sourced strings (asunto, remitente_nombre, remitente_email, talent_name, status_display) pass through escHtml before innerHTML interpolation — same CR-02 mitigation as dashboard.js deal rendering |
| T-03B-02 | Elevation of Privilege | /leads list endpoint | mitigate | Inherited router-level `dependencies=[Depends(get_current_user)]`; GET /leads returns 401 without a valid JWT |
| T-03B-03 | Input Validation | GET /leads filters | mitigate | FastAPI coerces talent_id:int (422 on non-int); nonexistent talent_id → explicit 404; status/fuente used only as parameterized SQLAlchemy filter values (no raw SQL) |
</threat_model>

<verification>
- `uv run pytest tests/test_leads.py -x -q` passes (list + filter + endpoint tests)
- `uv run pytest -x -q` full suite green
- Automated index.html/leads.js wiring assertion passes
- Human-check: Leads tab renders bars + list + score pills, filters work, no XSS
</verification>

<success_criteria>
- Leads tab displays all synced leads with D-36 colored score pills
- "Leads por talento" bars show count + calificados, including zero-lead talents
- Leads list filterable by talent, source, and status
- GET /leads serves talent_name + status_display, auth-protected, 404 on bad talent_id
- DASH-04 satisfied
</success_criteria>

<output>
Create `.planning/phases/03-google-sheets-leads-integration/03-02-SUMMARY.md` when done
</output>
