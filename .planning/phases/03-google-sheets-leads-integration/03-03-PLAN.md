---
phase: 03-google-sheets-leads-integration
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - app/schemas/dashboard.py
  - app/routers/dashboard.py
  - frontend/index.html
  - frontend/js/dashboard.js
  - tests/test_dashboard.py
autonomous: true
requirements: [DASH-04]
must_haves:
  truths:
    - "GET /dashboard/summary includes leads_totales and calificados counts sourced from the leads table"
    - "The Resumen tab shows wired-up 'Leads totales' and 'Calificados' KPI tiles with real counts (no longer placeholders)"
    - "When no leads exist yet, the tiles render 0 rather than crashing"
  artifacts:
    - path: "app/schemas/dashboard.py"
      provides: "DashboardSummary extended with leads_totales + calificados"
      contains: "leads_totales"
    - path: "app/routers/dashboard.py"
      provides: "get_summary populates leads KPIs from leads_service.leads_summary"
      contains: "leads_summary"
    - path: "frontend/index.html"
      provides: "Leads totales + Calificados tiles in the Resumen tab"
      contains: "leads-overview-grid"
    - path: "frontend/js/dashboard.js"
      provides: "renderLeadsOverviewKpis wired into loadSummary"
      contains: "leads_totales"
  key_links:
    - from: "app/routers/dashboard.py"
      to: "app.services.leads.leads_summary"
      via: "get_summary calls leads_service.leads_summary(db)"
      pattern: "leads_summary"
    - from: "frontend/js/dashboard.js"
      to: "/dashboard/summary"
      via: "loadSummary reads data.leads_totales/data.calificados"
      pattern: "leads_totales"
---

<objective>
Wire the Resumen tab "Leads totales" and "Calificados" KPI tiles (D-37):
extend GET /dashboard/summary to include leads_totales + calificados from the
leads service, add the two tiles to the Resumen tab, and render them in
loadSummary.

Purpose: Complete the leads visibility surface by surfacing lead counts on the
main Resumen tab (DASH-04 — the Resumen Leads KPIs previously deferred per D-28).

Output: extended DashboardSummary schema + get_summary, Resumen tab tiles,
dashboard.js rendering, and dashboard summary tests.
</objective>

<execution_context>
@/Users/alexiszamoraia/Developer/projects/lumixia/clients/santillan-ent/seg-dashboard/.claude/get-shit-done/workflows/execute-plan.md
@/Users/alexiszamoraia/Developer/projects/lumixia/clients/santillan-ent/seg-dashboard/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-google-sheets-leads-integration/03-CONTEXT.md
@.planning/phases/03-google-sheets-leads-integration/03-PATTERNS.md
@.planning/phases/03-google-sheets-leads-integration/03-01-SUMMARY.md

<interfaces>
<!-- Contracts this plan consumes and extends. -->

From app/services/leads.py (Plan 03-01):
  def leads_summary(db) -> dict   # {"leads_totales": int, "calificados": int}

Existing app/schemas/dashboard.py (extend DashboardSummary):
  class DashboardSummary(BaseModel):
      kpis: list[KpiTile]
      ranking: list[RankingRow]
      activity: list[ActivityItem]
      has_data: bool

Existing app/routers/dashboard.py get_summary (lines 46-79):
  - returns DashboardSummary(kpis=[], ranking=[], activity=[], has_data=False) when _has_data is False
  - else builds kpi_tiles/ranking_rows/activity_items and returns DashboardSummary(... has_data=True)
  - NOTE: _has_data(db) checks Deal rows only; leads may exist independently of deals

Existing frontend/index.html Resumen tab (#page-overview): a #kpi-grid (4 deal tiles)
populated by renderKpis(); add a separate leads-overview tile container.

Existing frontend/js/dashboard.js:
  - loadSummary() lines 404-441: reads /dashboard/summary, handles !data.has_data empty state, calls renderKpis/renderRanking/renderActivity
  - renderKpis(kpis) lines 247-263 builds .kpi tiles; escHtml available
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend DashboardSummary schema + get_summary with leads KPIs</name>
  <files>app/schemas/dashboard.py, app/routers/dashboard.py, tests/test_dashboard.py</files>
  <read_first>
    - app/schemas/dashboard.py (DashboardSummary class lines 46-50)
    - app/routers/dashboard.py (get_summary lines 46-79, _has_data lines 40-43)
    - app/services/leads.py (leads_summary from Plan 03-01)
    - tests/test_dashboard.py (existing dashboard summary test conventions)
  </read_first>
  <behavior>
    - GET /dashboard/summary includes leads_totales and calificados from leads_summary
    - When deals exist and leads exist, counts reflect the leads table
    - When no deals exist (has_data=False), leads_totales/calificados still return real lead counts (leads are independent of deals) and the endpoint does not crash
  </behavior>
  <action>
    In app/schemas/dashboard.py add two fields to DashboardSummary with safe defaults: `leads_totales: int = 0` and `calificados: int = 0`.

    In app/routers/dashboard.py add `from app.services import leads as leads_service`. In get_summary, compute `leads_data = leads_service.leads_summary(db)` and pass `leads_totales=leads_data["leads_totales"]`, `calificados=leads_data["calificados"]` into BOTH the has_data=True return AND the early has_data=False return (so the Leads tiles populate even before the first Pipedrive sync — leads sync independently). Keep all existing deal KPI logic unchanged.

    Extend tests/test_dashboard.py with test_dashboard_summary_leads_kpis: seed leads (via the seed_leads fixture) and assert GET /dashboard/summary returns leads_totales == expected total and calificados == count of QUALIFIED_STATUS leads; add a case asserting the leads counts are present even when no deals are seeded.
  </action>
  <verify>
    <automated>uv run pytest tests/test_dashboard.py -k "leads" -x -q && uv run pytest -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/schemas/dashboard.py` DashboardSummary contains `leads_totales: int = 0` and `calificados: int = 0`: `grep -c "leads_totales\|calificados" app/schemas/dashboard.py` returns at least 2
    - `app/routers/dashboard.py` imports leads service and calls leads_summary: `grep -F "leads_service.leads_summary" app/routers/dashboard.py` matches
    - Both return paths in get_summary pass leads_totales: `grep -c "leads_totales=" app/routers/dashboard.py` returns 2
    - test_dashboard_summary_leads_kpis passes and the full suite is green: commands in `<verify>` exit 0
  </acceptance_criteria>
  <done>DashboardSummary carries leads_totales + calificados sourced from leads_summary on both return paths; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Resumen tab Leads tiles + dashboard.js rendering</name>
  <files>frontend/index.html, frontend/js/dashboard.js</files>
  <read_first>
    - frontend/index.html (#page-overview KPI section lines 36-41)
    - frontend/js/dashboard.js (loadSummary lines 404-441, renderKpis lines 247-263, escHtml line 30)
    - frontend/css/styles.css (.kpi/.kpi-grid/.kpi-label/.kpi-val/.kpi-sub classes)
    - .planning/reference/mockup.html (#page-overview "Leads totales" + "Calificados" tile markup)
    - .planning/phases/03-google-sheets-leads-integration/03-CONTEXT.md (D-37)
  </read_first>
  <action>
    In frontend/index.html #page-overview, add a new `.section` (after the existing 4-tile #kpi-grid section) containing a `.kpi-grid` id="leads-overview-grid" with two tiles: "Leads totales" (variant blue) with a value span id="leads-totales-val" and "Calificados" (variant green) with a value span id="calificados-val". Use the existing .kpi/.kpi-label/.kpi-val/.kpi-sub markup; tiles start showing "--" until loadSummary populates them.

    In frontend/js/dashboard.js add `renderLeadsOverviewKpis(leadsTotales, calificados)` that sets #leads-totales-val.textContent = leadsTotales and #calificados-val.textContent = calificados (use textContent, not innerHTML — these are integers; textContent is the safe default). Call renderLeadsOverviewKpis(data.leads_totales ?? 0, data.calificados ?? 0) inside loadSummary in BOTH the has_data branch AND the !data.has_data empty-state branch (so the Leads tiles show real counts even when there are no deals yet — leads are independent). Use the nullish-coalescing default 0 so an older API response without the fields renders 0 rather than "undefined".
  </action>
  <verify>
    <automated>uv run python -c "h=open('frontend/index.html').read(); j=open('frontend/js/dashboard.js').read(); assert 'leads-overview-grid' in h and 'leads-totales-val' in h and 'calificados-val' in h, 'html tiles missing'; assert 'renderLeadsOverviewKpis' in j and 'data.leads_totales' in j, 'js wiring missing'; print('ok')"</automated>
    <human-check>Open the dashboard, log in, view the Resumen tab: the "Leads totales" and "Calificados" tiles show real integer counts (not "--" and not "undefined"), matching the totals seen on the Leads tab.</human-check>
  </verify>
  <acceptance_criteria>
    - `frontend/index.html` contains `id="leads-overview-grid"`, `id="leads-totales-val"`, `id="calificados-val"`: all three grep matches present
    - `frontend/js/dashboard.js` defines `renderLeadsOverviewKpis` and reads `data.leads_totales`: `grep -c "renderLeadsOverviewKpis\|data.leads_totales" frontend/js/dashboard.js` returns at least 2
    - renderLeadsOverviewKpis uses textContent (not innerHTML): `grep -F "textContent" frontend/js/dashboard.js` matches within the new function
    - Automated wiring assertion in `<verify>` exits 0
  </acceptance_criteria>
  <done>Resumen tab "Leads totales" and "Calificados" tiles render real counts via renderLeadsOverviewKpis on both load paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /dashboard/summary | Authenticated client request for aggregated counts |
| leads table → DashboardSummary response | Computed integer aggregates (no raw Sheet strings) cross to the client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03C-01 | Elevation of Privilege | /dashboard/summary | mitigate | Inherited router-level `dependencies=[Depends(get_current_user)]`; endpoint already returns 401 without a valid JWT |
| T-03C-02 | Information Disclosure | leads KPI tiles | accept | Only aggregate integer counts (leads_totales, calificados) are exposed — no PII (email/subject) crosses to the Resumen tiles; the Leads tab (03-02) handles row-level display with escaping |
| T-03C-03 | Tampering (XSS) | renderLeadsOverviewKpis | mitigate | Values rendered via textContent (not innerHTML); integers only, no Sheet strings interpolated |
</threat_model>

<verification>
- `uv run pytest tests/test_dashboard.py -k "leads" -x -q` passes
- `uv run pytest -x -q` full suite green
- Automated index.html/dashboard.js wiring assertion passes
- Human-check: Resumen Leads tiles show real counts matching the Leads tab
</verification>

<success_criteria>
- GET /dashboard/summary includes leads_totales + calificados from the leads table
- Resumen "Leads totales" and "Calificados" tiles wired with real counts (D-37)
- Tiles render 0 (not crash / not "undefined") when no leads exist
- DASH-04 (Resumen leads KPI portion) satisfied
</success_criteria>

<output>
Create `.planning/phases/03-google-sheets-leads-integration/03-03-SUMMARY.md` when done
</output>
